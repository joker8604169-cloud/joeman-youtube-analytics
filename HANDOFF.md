# 專案交接文件

> 供新對話視窗接手用。最後更新：**2026-08-02**
>
> 🔴 **接手第一件事**：讀 `BUILD.md`（逐頁施工單）。本文件講「為什麼」，`BUILD.md` 講「怎麼做」。
> 專案路徑：`C:\Users\Ace\Desktop\結訓專題`
> GitHub：https://github.com/joker8604169-cloud/joeman-youtube-analytics
>
> ⚠️ **文中數字為撰寫當日快照，資料每日自動更新，引用前請重新查詢。**
> 人工抽樣得到的精確率、召回率則為固定值，不會變動。

---

## 🔴 第零章：最高原則——業配與流量的因果方向（2026-08-05 定案）

> **本節優先於本專案所有其他文件、講稿與討論。任何與本節牴觸的段落，一律以本節為準。**

### 原則

**業配不會帶動流量。業配與流量之間只有一個方向：內容先有流量，廠商才進場投放。**

### 理由是機制，不是統計

**沒有任何人會為了看業配而點開一支影片。** 廠商買的是曝光，不是流量；
而業配在拍攝前就談定，**那個時間點這支影片的觀看數還不存在**——時序上不可能是業配造就流量。

這個理由不依賴任何檢定，不會因為資料每日更新而動搖，也不需要更大的樣本。
**它是領域前提，不是本專案的分析結果。**

### 允許的寫法（不必加任何但書）

- 「業配是流量的結果，不是原因」
- 「業配不會帶來流量，是廠商挑選已經有流量的內容」
- 「這個差距是選擇效應」

### 🔴 禁止出現的寫法

- 「業配帶不帶流量，我測不到」
- 「這份資料答不出來」
- 「測不到，不是沒有」
- 任何把「業配 → 觀看」當成開放問題處理的三層／兩層框架

**理由**：那是拿統計檢定去替一個機制上不存在的因果路徑保留可能性。
在台上它不是嚴謹，是**替不存在的東西留門**，反而誘導評審去問一個沒有答案也不需要答案的問題。

### 這條原則的邊界

| ✅ 允許 | ❌ 不允許 |
|---|---|
| 斷言方向：流量 → 業配 | 用它推論業配的**金額、合作深度**，或對訂閱、留存的影響 |
| 把廠商投放集中在高流量內容當成事實陳述 | 免除樣本門檻（類型 ≥20、系列 ≥10、成對比較 ≥30） |
| 直接說「廠商挑選已經有流量的內容」 | 免除第七章第 7 點的信賴區間紀律——那條規則對所有比率比較仍然有效 |

🔴 **也不要用「拆掉一個誤會」的方式來鋪陳。** 沒有人會認為業配帶來流量，
連疑惑或聯想都不會有；**主動提出這個假說再去反駁它，只會讓聽的人覺得你的想法很奇怪**。
不要有以這個方向為出發點的思考、圖表或講稿。

⚠️ **2023/11/06 事件那段的「測不到顯著影響」不受本節影響。**
那講的是事件對廠商投放意願的衝擊，是另一個命題，該段的保留語氣是正確的。

---

## 一、專案目標

透過 YouTube Data API v3 抓取台灣 YouTuber **Joeman** 頻道
（`UCPRWWKG0VkBA0Pqa4Jr5j0Q`）的全部公開影片，進行資料清洗與分析，
最終以 **Power BI** 製作 7 頁報表並發表（**10 分鐘、單人簡報**）。

資料需**每日自動更新**，已透過 GitHub Actions 達成。

### 需分析的維度（使用者原始需求）
發布時間、觀看次數、影片類型、是否為特定系列、有無業配、按讚數、留言數，
長影片與 Shorts 分開整理。

---

## 二、目前進度

> 最後更新 **2026-08-06**。發表在 2026 年 8 月上旬。

### ✅ 已完成

| 項目 | 狀態 |
|---|---|
| **Power BI 實作** | 🎉 **八頁全部完工並驗證**（七頁報表 ＋ 第 8 頁結尾頁） |
| 資料抓取（`src/fetch.py`） | 約 3,507 支影片、81 個播放清單，配額 235/10,000 |
| 資料清洗分類（`src/transform.py`） | 24 欄輸出，7 個規則表 |
| 業配判定與人工驗證 | **分格式各抽 40 支純隨機核對**（2026-08-05 重測，見第八章） |
| GitHub 部署 | 每日 06:00 自動更新；2026-08-05 手動觸發驗證通過 |
| Power BI 資料來源 | 已切換為 GitHub raw 網址，不再讀本機路徑 |
| DAX 量值 | 約 40 個，全部已貼入並驗證 |
| 文件 | 四份 md ＋ `簡報執行指南.docx` 全部同步並進版控 |

### ⏳ 未完成

| # | 工作 | 預估 |
|---|---|---|
| 1 | **講稿數字重抄**（資料每日更新，發表前一天重新整理後照畫面抄一次） | 30 分 |
| 2 | **簡報排練**（三個關鍵點擊：第 3 頁格式三步、第 4 頁年份三停點＋資料範圍切換、第 5 頁點系列） | 1.5 小時 |
| 3 | 每頁存一張 PNG 截圖備援 | 15 分 |
| 4 | `.pbix` 複製到雲端或隨身碟（`.gitignore` 排除 `*.pbix`，不會進版控） | 5 分 |

### 發表後再處理

- **規則缺口**：2026-08-05 重測時發現的 14 支漏抓，多為資訊欄有推銷但規則沒命中
  （Oral-B、TheraBreath、INAX、Logitech、麥卡倫、醫美診所），另有兩支是「發布早於上市日」的樣機。
  🔴 **發表前不要改規則**——改了要重跑全部並重驗每一頁的數字，而且不影響現在的結論
  （推估值已把漏抓算進去）。
- `sponsor_evaluate.py` 的輸出寫「**模型偵測**」，那是誤稱（本專案沒有機器學習，
  判定是規則表 ＋ 加權計分），建議改為「規則偵測」。只影響終端輸出，不動任何數字。

---

## 三、目前的關鍵數據

```
影片總數      3,505（長片 3,025｜Shorts 480）
總觀看        14.32 億
訂閱數        287 萬
業配影片      540 支（長片 513）
長片業配率     2022 年起 53.2%，全期僅 17.0%
推估實際業配   長片 672 支（偵測 513 ÷ 召回率 76.3%）
有系列歸屬     1,131 支
《Joe是要對決》 311 支（正片 307），佔全頻道觀看 21.1%
未分類        415 支（11.8%）
每日快照      7 日（2026-07-21 起至 2026-08-05）
```

⚠️ **`自營商品導流` 這個欄位已不列入關鍵數據**——它 2024 年起飽和到 99–100%，
量的是「說明欄有沒有那組罐頭連結」而不是「這支影片在賣自家東西」，見第 5-1 節。

⚠️ **這些數字每天會變**，引用前請重新查詢。

---

## 四、專案結構

```
結訓專題/
├── config.py                  頻道 ID、路徑設定
├── requirements.txt
├── .env                       API 金鑰（不入版控）
├── .gitignore                 排除 .env、data/raw、*.pbix
├── README.md
├── HANDOFF.md                 本文件
├── .github/workflows/
│   └── daily-update.yml       每日 06:00（台灣時間）自動執行
├── src/
│   ├── fetch.py               API 抓取，原始 JSON 落地
│   ├── transform.py           清洗、分類、輸出全部 CSV
│   ├── sponsor_sample.py      業配抽樣（分層，測精確率）
│   ├── sponsor_sample_recall.py  漏抓率抽樣（純隨機，可指定格式）
│   ├── sponsor_evaluate.py    準確度評估（Wilson 信賴區間）
│   └── make_background.py     產生 8 頁背景圖與座標表
├── rules/                     7 個規則表（皆為 CSV，可直接編輯）
├── assets/                    背景圖 PNG ＋ 座標表 CSV
└── data/
    ├── raw/                   API 原始 JSON（12 MB，不入版控）
    └── processed/             分析輸出
```

### 執行方式

```bash
python -m src.fetch                      # 抓取（235 units）
python -m src.transform                  # 清洗（不需 API）
python -m src.sponsor_evaluate           # 長片準確度
python -m src.sponsor_evaluate Shorts    # Shorts 準確度
python -m src.make_background            # 全部 8 頁背景圖
python -m src.make_background 4          # 只產生第 4 頁
```

### 兩個執行上的限制

**API 配額**：每次 `fetch` 消耗 **235 units**，每日上限 10,000
→ 一天最多重跑 42 次，實務上撞不到，但連續除錯時要留意。

**頻道識別防呆**：`fetch.py` 會用 handle 解析頻道並**核對 channel ID**，
不符即中止。若 Joeman 更改 handle（目前為 `@joeman`），抓取會失敗，
需到 `config.py` 修改 `CHANNEL_HANDLE`。`EXPECTED_CHANNEL_ID` 不要改，
那是防止抓錯頻道的保險。

---

## 五、資料輸出

| 檔案 | 內容 |
|---|---|
| `videos.csv` | 影片主檔，24 欄 |
| `daily_stats.csv` | 每日快照（日期 × 影片 × 觀看/讚/留言） |
| `data_notes.csv` | 資料特徵與判準，**第 2 頁報表用** |
| `limitations.csv` | 限制清單 14 項，**第 7 頁報表用** |
| `review_queue.csv` | **需人工檢視的項目**（新系列、未分類、業配臨界） |
| `excluded_videos.csv` | 被排除的影片與原因 |
| `sponsor_review_sample.csv` | 長片分層抽樣的人工標記（**永久資產，勿刪**） |
| `sponsor_recall_sample.csv` | 長片隨機抽樣的人工標記（**永久資產**） |
| `sponsor_recall_sample_shorts.csv` | Shorts 隨機抽樣的人工標記（**永久資產**） |

### 5-1 `videos.csv` 資料字典（24 欄）

| 欄位 | 型別 | 說明 | 可否用於視覺 |
|---|---|---|---|
| `video_id` | 文字 | YouTube 影片 ID，唯一鍵 | 計數用 |
| `title` | 文字 | 影片標題 | ✅ |
| `published_at_taipei` | 文字 | 發布時間（台灣時區，含時分秒） | 參考用 |
| `published_date` | **日期** | 發布日期 | ✅ 時間軸用這個 |
| `published_year` | 整數 | 發布年份 | ✅ 年度圖表用這個 |
| `published_hour` | 整數 | 發布時段 0–23 | ✅ |
| `duration_seconds` | 整數 | 影片長度（秒） | ✅ |
| `video_format` | 文字 | `長片` 或 `Shorts` | ✅ **關鍵篩選欄** |
| `category` | 文字 | 影片類型（14 類，含「未分類」） | ✅ |
| `category_source` | 文字 | 分類依據：`playlist`／`title_series`／`keyword`／`override`／`none` | ⚠️ 查核用，不放視覺 |
| `series` | 文字 | 系列名稱，無歸屬為空白 | ✅ |
| `playlists` | 文字 | 所屬播放清單，以 `\|` 分隔 | ⚠️ 查核用 |
| `is_sponsored` | **True/False** | 業配判定結果 | ✅ **關鍵篩選欄** |
| `sponsor_source` | 文字 | 判定來源：`api`／`keyword`／`override`／`none` | ⚠️ 查核用 |
| `sponsor_score` | 數值 | 業配加權分數（門檻 2 分） | ⚠️ 抽樣時用 |
| `sponsor_hits` | 文字 | 命中了哪些規則，以 `\|` 分隔 | ⚠️ **查核用，勿放視覺** |
| `ppd_api_flag` | **True/False** | YouTube 官方「含付費宣傳」旗標 | ⚠️ 全頻道僅 3 支為真，參考價值低 |
| `is_self_promo` | **True/False** | 是否含自營商品導流 | ⚠️ **近年已飽和，勿做趨勢視覺**（見下） |
| `self_promo_products` | 文字 | 自營商品名稱，以 `\|` 分隔 | ⚠️ 查核用 |
| `view_count` | 整數 | 觀看數（抓取當下累計） | ✅ |
| `like_count` | 整數 | 按讚數，**空值代表關閉顯示**（4 支） | ✅ |
| `comment_count` | 整數 | 留言數，**空值代表關閉留言**（1 支） | ✅ |
| `youtube_category_id` | 整數 | YouTube 官方分類 ID | ❌ 與自訂類型不同，未使用 |
| `url` | 文字 | 影片網址 | ✅ 表格中設為 Web URL |

⚠️ **標記「查核用」的欄位是給人追溯判定依據的**，放進圖表會變成無意義的文字堆。

### ⚠️ `is_self_promo` 近年已飽和（2026-08-05 查證）

長片的自營率：2022 年 **0%** → 2023 年 34.2% → 2024 年 **98.8%** → 2025 年 **99.2%** → 2026 年 **100%**。

這不是「每支影片都在賣自家商品」，而是**說明欄從 2024 年起固定掛著課程／團購／會員的連結**，
規則命中的是那組罐頭文字。與第十三章第 3 點是同一類問題，只是這次沒有污染判定結果
（自營商品本來就不算業配），所以一直沒被發現。

**後果**：這個欄位**不能用來做任何趨勢視覺**——畫出來會是一條衝到 100% 的線。
它仍可用於單一影片的查核。目前沒有任何視覺使用它，報表不受影響。

### 5-2 `review_queue.csv` 的使用方式

每次清洗都會重新產生，列出需人工介入的項目：

| 類型 | 處理方式 |
|---|---|
| 未對應的播放清單 | 補進 `rules/playlist_map.csv` |
| 疑似新系列 | 補進 `rules/series_title_patterns.csv` |
| 近期未分類影片 | 視情況補 `rules/category_rules.csv`，或維持「未分類」 |
| 業配判定臨界 | 抽查是否為漏抓，必要時調整規則 |

**建議每週看一次**（GitHub Actions 的執行日誌也會印出來）。
只看近 90 天的影片，所以清單通常很短——目前只有 1 項。

---

## 六、規則系統（7 個表，全部在 `rules/`）

| 檔案 | 用途 |
|---|---|
| `playlist_map.csv` | 81 個播放清單 → 系列／類型 |
| `series_title_patterns.csv` | 標題正則 → 系列（24 條） |
| `category_rules.csv` | 關鍵字 → 影片類型（依 priority 排序） |
| `sponsor_rules.csv` | 業配加權計分規則 |
| `self_promo_rules.csv` | 自營商品識別（課程／團購／會員／9好吃） |
| `description_boilerplate.csv` | 說明欄罐頭文字剔除 |
| `manual_overrides.csv` | 人工覆寫（最高優先） |

### 分類優先序
```
manual_overrides > playlist_map > series_title_patterns > category_rules > 未分類
```

### ⚠️ 規則 CSV 的致命陷阱
正則含逗號（如 `.{0,20}`）**必須用雙引號包起來**，否則會被 CSV 解析器切斷且**不報錯**。
`read_csv_rules()` 已加入欄位數檢查會中止，但寫新規則時仍要注意。

---

## 七、關鍵決策與理由（新視窗必讀）

### 1. 業配判定採加權計分，門檻 2 分
- 權重 3＝強訊號（折扣碼、贊助聲明），單獨命中即成立
- 權重 2＝導購用語，權重 1＝弱訊號
- **權重 3 的規則 `match_field` 設為 `both`（標題＋說明欄）**，其餘只查說明欄
  - 理由：Shorts 有 85.7% 說明欄空白，導購訊息只寫在標題

### 2. 自營商品不算業配
使用者定義：**只有外部廠商的商業委託才算**。
- ✅ 算：付費、分潤（折扣碼/團購連結）、**廠商提供樣機**
- ❌ 不算：自營商品（課程、團購平台、會員、9好吃益生菌）、**發表會邀請**
- 樣機算但發表會不算的理由：上市前樣機通常是排他性行銷合作，發表會是廣泛邀請媒體

### 3. 商業分析一律排除 Shorts
Shorts 召回率僅 14.6%，**無法可靠測量**。已測試品牌雜湊標籤補強，
但驗證顯示命中率僅 40%（15 支非業配 vs 10 支業配），會損害精確率，故未採用。

### 4. Shorts 的四面向處理原則
| 面向 | 處理 |
|---|---|
| 產能結構 | ✅ 納入（核心發現） |
| 觀看表現 | ✅ 納入但分開比較 |
| 業配／商業 | ❌ 排除 |
| 系列集數 | ❌ 排除（Shorts 多為宣傳片） |

### 5. 可比性判準（跨年比較）
以兩個快照實測 8 日成長中位數界定：
```
2019: 14 次   2023: 188 次   2024: 577 次   2025: 2,785 次   2026: 11,234 次
```
**2023 年及以前才納入跨年觀看比較。** 按讚數呈現一致梯度（2019 年 88% 零成長
vs 2026 年 2%），交叉驗證此判準非單一指標雜訊。

#### 2026-08-02 壓力測試：判準通過，且證實 2025 才是真正的斷崖

用 07-21 → 08-01（11 天）三個快照重算，與原本的 8 日數字完全一致
（188×11/8=258 vs 實測 251、577×11/8=793 vs 738、2785×11/8=3829 vs 3576）。

**11 日相對成長中位數（長片）**

```
2010–2019  0.000–0.015%   已停止成長
2020–2022  0.011–0.018%   基本持平
2023       0.049%   ← 2022 的 2.7 倍
2024       0.168%   ← 2023 的 3.4 倍
2025       0.552%   ← 2024 的 3.3 倍
2026       2.746%   ← 2025 的 5.0 倍
```

每往後一年約是前一年的 3–5 倍，是乾淨的指數衰減，**不是雜訊**。

**決定性檢定**：假設各年再依目前速率成長一整年（**刻意高估**，忽略衰減——
第十三章第 7 點警告過這不能當預測值，但拿來當**壓力測試**剛好：連上限都不翻盤就安全）：

| 比較 | 現在 | 完全累積後上限 | 位移 | 判定 |
|---|---|---|---|---|
| 2019→2023 | −23.4% | −22.1% | +1.2pp | ✅ 結論不變 |
| 2019→2024 | −34.1% | −30.5% | +3.7pp | ✅ 結論不變 |
| **2019→2025** | **−22.3%** | **−8.1%** | **+14.2pp** | 🔴 **結論可能翻盤** |

**2025 是真正的界線**——它現在看起來下滑 22.3%，完全累積後可能只剩 8.1%，
「單片戰力下滑」的結論會幾乎消失。

⚠️ **嚴格說資料支持把界線放寬到 2024**（2019→2024 連上限都只位移 3.7pp）。
**但決定維持 2023**，理由：
① 保守沒有成本卻有價值——「我用了比資料所允許的更嚴格的標準」是很強的防守位置；
② 2024 的成長率是 2023 的 3.5 倍，主張它可比需要額外解釋，而第 3 頁只有 110 秒；
③ 2024 的中位觀看（478,189）是近年最低點，實線畫到 2024 會讓折線收在谷底，
形狀反而失真。

### 5b. 「新系列」的界線＝《對決》停播日，不是曆年（2026-08-02 改）

`系列世代` 原本定義為「首播年份 ≥2025」，改為「**首播日晚於 2025-11-10**」——
那是《Joe是要對決》最後一集（第 307 集），標題就是「是時候該放手了⋯停播原因跟心路歷程」。

**理由**：原規則會把《工作室日常廢片》（首播 2025-08-27，**比停播早 75 天**）算成新系列，
不符合第 6 頁「招牌停播後的接棒」這個宗旨。改用停播日之後，
被問「你怎麼定義新系列」可以直接回答「**《對決》最後一集之後開播的**」。

| 系列 | 集數 | 首播 | 相對停播 |
|---|---|---|---|
| 我獨自穿越日本 | 10 | 2025-11-19 | **後 9 天** |
| 九讀 | 3 | 2026-01-28 | 後 79 天 |
| Joe是要跟團 | 3 | 2026-05-26 | 後 197 天 |
| 公車食刻表 | 3 | 2026-05-29 | 後 200 天 |
| ~~工作室日常廢片~~ | 7 | 2025-08-27 | **前 75 天** ← 排除 |

⚠️ 日期**刻意寫死**在 `系列世代` 裡。若用 `MAX(published_date)` 動態抓，
《對決》一旦復播，界線會往後跳、**現有新系列全部被重新歸類且不報錯**。
代價是復播後要手動改那一行。

### 5c. 第 6 頁的收尾論點已更換：業配率 → 首集業配（2026-08-02）

原本的收尾是「新系列業配率 65.4% ＞ 既有系列 56.1%，證明廠商信賴已轉移」。
**實測後不成立**：

| 定義 | 新系列 | 既有系列 | 差異 | 自助抽樣 95% 信賴區間 |
|---|---|---|---|---|
| 曆年 ≥2025 | 65.4%（17/26） | 56.1%（46/82） | +9.3pp | **[−12.0, +29.9]pp 跨越零** |
| 停播日之後 | 73.7%（14/19） | 55.1%（49/89） | +18.6pp | **[−4.7, +40.0]pp 跨越零** |

中位觀看的差異同樣不顯著（+84,521，區間 [−77,734, +424,242]）。
照本章第 7 點自訂的標準，**兩個指標都不能當結論**——這跟第十三章第 6 點的
「用信賴區間跨越零的比率差下結論」是同一個坑，只是這次踩在收尾頁。

**改用二元類別觀察**：

| | 系列數 | 第 1 集就有業配 |
|---|---|---|
| 既有系列（集數 ≥3） | 22 | **2 個（9%）** |
| **新系列** | **4** | **4 個（100%）** |

若機率相同，連續四個都命中的機率是 **0.01%**。歷史對照：
《Joeman開箱去》等到第 **91** 集（499 天）、《Joe是要看房》等到第 **25** 集（**719 天**）。

**這是類別觀察不是比率比較，不需要信賴區間就成立。**
而且它把第 5 頁與第 6 頁的「表面矛盾」從斷言變成測量值——**機制改變是 9% → 100%**。

### 5d. 第 6 頁的三組關鍵數字

**規模缺口**

| | 集數 | 總觀看 | 單集中位 | 歷時 |
|---|---|---|---|---|
| 《對決》 | 307 | 2.99 億 | 809,976 | 7.6 年 |
| 四個新系列 | 19 | 1,534 萬 | 640,117 | 0.7 年 |
| **比例** | **6.2%** | **5.1%** | **79%** | — |

**每集的成績有 79%，但總量只有 5%。缺的是集數，不是能力。**

**戰力已追平**（年份拉到 2025 起）：《對決》最後一年 45 集、中位 **646,228**；
四個新系列 19 集、中位 **640,117**——**相差 0.9%**。
同一時間窗口、同一頻道、同一批觀眾的對照，直接對上要被取代的標的。

**規模缺口的具體長相**（門檻演出）：破百萬《對決》105 集 vs 新系列 **6** 集；
**破兩百萬《對決》22 集 vs 新系列 0 集**。這段刻意講壞消息，
主動承認缺口是真的，反而讓「戰力持平」更可信。

### 6. 統計量統一用中位數
平均值易受爆款影響。**全報表一致使用中位數**，避免被質疑標準不一。

### 7. 顯著性判準
以自助抽樣法估計 95% 信賴區間，**區間跨越零則不下結論**。

---

## 八、人工驗證結果（分格式各抽 40 支純隨機）

### 長片
| 指標 | 數值 |
|---|---|
| 精確率 | **22 / 22 ＝ 100%**（95% 信賴區間 85.1–100%） |
| **召回率** | **76.3%**（95% 信賴區間 69.1–83.6%）　←　2026-08-05 重測 |
| 漏抓率 | **35.0%**（14/40，95% 信賴區間 22.1–50.5%） |
| 推估近三年業配率 | 偵測 53.0% → **推估 69.5%**（95% 區間 63.4–76.7%） |
| 抽樣 | 分層 22 ＋ **純隨機 40（全部為長片）** |

### 🔴 「長片召回率 85.3%」不是長片的數字（2026-08-05 發現）

`python -m src.sponsor_evaluate` **不帶參數時完全不篩格式**，而被當成長片抽樣的那個檔案
實際上是混合的：

| 檔案 | 文件說它是 | 實際組成 |
|---|---|---|
| `sponsor_recall_sample.csv` | 長片隨機抽樣 → 召回率 85.3% | **25 Shorts ＋ 14 長片** |
| `sponsor_review_sample.csv` | 長片分層抽樣 → 精確率 96.8% | 27 長片 ＋ 8 Shorts |

拆出長片自己的漏抓率：抽到 14 支，其中 8 支經規則調整後已被抓到，**現仍判非業配且可判定
只剩 6 支、漏抓 0 支**——`0/6`，推不出任何東西。

**而且兩個數字互相矛盾**：Shorts 專用樣本推估漏抓 **151 支**，混合樣本卻推估全格式只漏
**48.6 支**。一種格式漏的比全部還多，不可能。混合樣本裡 Shorts 的漏抓率是 2/22＝9.1%，
與專用樣本的 34.2% 差近四倍。

**後果**：`推估實際業配` ＝ 偵測數 ÷ 0.853 是**高估**——85.3% 被 Shorts 的漏抓拖低了。
若長片漏抓率接近 0，推估值應貼近偵測值本身。

**已採取的行動（2026-08-05）**

1. `sponsor_sample_recall.py` **不指定格式時直接中止**——原本會寫入
   `sponsor_recall_sample.csv`，**覆蓋掉既有的人工標記（永久資產）**；且排除清單漏了
   既有的漏抓率抽樣檔，會重複抽到已核對過的影片
2. `sponsor_evaluate.py` 依格式讀對應抽樣檔（`_長片` / `_shorts`），
   不指定格式時印出混合格式警語
3. 已產生 `sponsor_recall_sample_長片.csv`：**40 支純長片**，
   自 231 支候選（2023 年起、判定非業配、排除已核對的 10 支）純隨機抽出

### ✅ 重測結果（2026-08-05，40 支純長片，0 支無法判定）

| | 舊（混合樣本） | **新（純長片）** |
|---|---|---|
| 漏抓率 | 7.1% | **35.0%**（14/40） |
| 召回率 | 85.3% | **76.3%** |
| 精確率 | 96.8%（30/31 混合） | **22/22**（長片專屬） |
| KPI3 推估實際業配（全期） | 601 | **672** |

**方向與直覺相反**：舊樣本裡長片只剩 6 支可判定且 0 漏抓，看起來像「長片幾乎沒漏」，
實測 40 支卻是漏 14 支。**0/6 就是樣本太小，不能推任何東西。**

`DAX.md` 的 `推估實際業配` 常數已由 `0.853` 改為 **`0.763`**；
KPI4 改用 `[判定精確率_長片]` 顯示 **「22 / 22」**（不寫 100%，理由同第 6 頁 KPI4 的「4 / 4」）。

### 🔴 漏抓的 14 支透露規則缺口（發表後再處理）

多數是**資訊欄有推銷但規則沒命中**：Oral-B 電動牙刷、TheraBreath 漱口水、INAX 馬桶、
Logitech 滑鼠、麥卡倫、醫美診所⋯⋯另有兩支是「發布時間早於上市日」的樣機
（iPhone 17 系列），文字比對本來就抓不到。

⚠️ **發表前不要改規則**——改了要重跑全部並重驗所有頁面的數字。
而且**不影響現在的結論**：推估值已經把漏抓算進去了。

### Shorts
| 指標 | 數值 |
|---|---|
| 召回率 | **14.6%**（95% 信賴區間 10.5–21.7%） |
| 推估真實業配率 | **37.9%**（95% 信賴區間 25.6–52.9%） |
| 抽樣 | 40 支隨機 |

**Shorts 漏抓的三種模式**（文字比對永遠抓不到）：
1. 上市前樣機開箱 — 需產品上市日期資料庫
2. 品牌雜湊標籤 — 已驗證無效（40% 命中率）
3. 影片內口頭揭露 — 需看完影片

### ⚠️ 精確率抽樣不涵蓋 2010–2016（2026-08-03 補記）

那 63 支是**分層抽樣**，而 2010–2016 的業配影片只佔母體 **13/548 = 2.4%**，抽樣裡幾乎不可能抽到。
2026-08-03 首次人工檢查這 13 支，結果 **8 支是誤判**：

| 判定 | 影片 | 理由 |
|---|---|---|
| ✅ 真業配 | AMD打寶列車長 ×3（2012） | AMD 冠名的行銷活動，品牌名在標題裡 |
| ✅ 真業配 | 技嘉 GTX950 開箱（2015） | 分數 9，命中專屬導購連結／導購區塊標題／電商賣場連結 |
| ✅ 真業配 | 用VR玩遊戲到底是甚麼感覺？（2016） | 人工確認有贊助商 |
| ❌ 誤判 | 星海爭霸賽事與戰術解說 ×6（2010–2011） | 說明欄的「致謝贊助」是**賽事或戰隊的贊助商**，錢沒有進到創作者手上 |
| ❌ 誤判 | 《爐石小時光》第四集（2013） | 同上 |
| ❌ 誤判 | 2015年最令我印象深刻的10款遊戲（2016） | 「影片合作聲明」非業配 |

**根因**：`sponsor_rules.csv` 把「致謝贊助句型」設為權重 3（單獨命中即成立），是為了抓現代的
業配聲明，套到 2010 年的電競轉播語境就會誤傷。**沒有改規則**——改權重會影響全期判定，
代價遠大於收益；改用 `manual_overrides.csv` 逐支覆寫（8 列，`is_sponsored=False`）。

**影響**：業配總數 548 → 540；2010／2011／2013 年業配率降為 0%，2016 年降為 0.3%。
類型排序與所有結論不變，且「2018 年以前幾乎沒有業配」這個論點更乾淨。

⚠️ 這代表**這個年代的精確率仍然沒有統計量測**，只有一次全查（13 支全看過）。
若被問到，正確說法是「**2017 年以後的精確率抽樣是 22/22**；2017 年以前的 13 支我全部人工檢查過」。

### 8-1 若要重做抽樣驗證

三步驟，缺一不可：

```bash
# 步驟 1：產生抽樣清單
python -m src.sponsor_sample              # 分層抽樣（測精確率）
python -m src.sponsor_sample_recall       # 長片隨機抽樣（測漏抓率）
python -m src.sponsor_sample_recall Shorts   # Shorts 隨機抽樣

# 步驟 2：人工填寫
# 用 Excel 開啟產生的 CSV，在「實際是否業配_請填寫」欄填 是／否
# 不確定就留空或填「不確定」，評估時會自動排除

# 步驟 3：計算準確度
python -m src.sponsor_evaluate            # 長片
python -m src.sponsor_evaluate Shorts     # Shorts
```

**兩個關鍵設計**：

1. **人工標記是永久資產**。評估程式的預測值一律取自最新的 `videos.csv`，
   標記檔只提供「正確答案」。所以**改規則後可以直接重跑評估**，
   不必重新人工核對。

2. **精確率與漏抓率必須用不同抽樣**。
   - 精確率 → 從「判定為業配」中抽（分層）
   - 漏抓率 → 從「判定為非業配」中**純隨機**抽
   曾經只用分層抽樣就宣稱召回率 92%，實際純隨機驗證後只有 60.8%——
   **分層抽樣不能用來推估母體**。

---

## 九、報表設計（7 頁）

### 整份報表主軸（一句話）
> 這個頻道擴大了總產能並把部分長片資源轉投短影音，商業投放並未跟著減少——
> 看起來下降的整體業配率是偵測失效造成的。唯一真實的挑戰是招牌 IP 停播後的規模缺口，
> 而新系列的初動證明品牌動能還在。

### 頁面結構

| 頁 | 主題 | 主圖 | 側圖 | 篩選器 | 秒數 |
|---|---|---|---|---|---|
| 1 | 封面 | 播放鍵視覺（右）＋標題（左） | — | 無 | 60 |
| 2 | 資料特徵與判準 | 單張全幅表格（讀 `data_notes.csv`） | — | 無 | 90 |
| 3 | 產能結構轉移 | 長片與 Shorts 產量堆疊 | 長片中位觀看／類型組成 | 格式・門檻・年份 | 110 |
| 4 | 投放規模 | 業配數＋業配率雙軸 | **業配偵測缺口（長片 vs Shorts）**／類型組成 | **資料範圍**・年份 | 110 |
| 5 | 投放偏好 | 各類型業配率橫條（呈現分布） | **廠商進場時序（本頁主要證據）**／系列業配密度 | **系列**・**類型**・門檻 | 110 |
| 6 | 接棒動能 | 系列 IP 氣泡圖 | 系列化比例趨勢／**首次業配出現在第幾集** | 年份・門檻 | 110 |
| 7 | 本報告的限制 | 單張全幅表格（讀 `limitations.csv`） | — | 無 | Q&A 備用 |

**合計 620 秒 ≈ 10.3 分鐘**

### 敘事鏈
```
1 產能結構變了 → 「商業面受影響嗎？」
2 長片業配率穩定 55%，廠商投放未減 → 「錢集中在哪些內容？」
3 挑戰企劃 45.7% 最高，且是事後認證（證據＝進場時序）→ 「最受青睞的流量來源停播了？」
4 新系列業配率 65%，廠商跟著轉移過去
```

⚠️ 第 3 環的證據來源已修正。**支撐「事後認證」的是側上圖「廠商進場時序」的縱向比較，
不是主圖的類型橫向排序**——理由見第九章末。

### 每頁的關鍵篩選器操作（演出重點）
- **第 3 頁**：格式三步揭露（全部 → 長片 → Shorts）
- **第 4 頁**：年份拉到 2022 後切資料範圍，業配率從 53.2% 掉到 33.9%，展示指標被污染
- **第 5 頁**：系列切換，展示廠商在系列證明後才進場
  （《看房》2020 0% → 2021 0% → 2022 42.9% → 2023 40.9% → 2024 91.7%）
- **第 6 頁**：年份縮至 2025 起，對比新舊系列

### 第 5 頁的論證權重（2026-08-01 修正）

**主圖不是本頁的主要證據，側上圖才是。**

跨類型的橫向比較中，業配率與觀看數幾乎無相關（Spearman ρ = **−0.07**）。
以 2018 年起、樣本 ≥40 的長片為例：

| 類型 | 中位觀看 | 業配率 |
|---|---|---|
| 人物/訪談 | 77.0 萬 | 15.6% |
| 挑戰/企劃 | 72.1 萬 | 51.4% |

流量幾乎相同、業配率差三倍。**橫向排序反映的是「內容適不適合置入商品」，
不是「流量高低」**，所以它撐不起「業配是流量的結果」這個論證。

真正撐得起的是側上圖「廠商進場時序」——它是**縱向**比較：
同一系列、同一種內容、同一個主持人，唯一變動的是已累積的成績，
控制住了橫向比較控制不住的變因。

因此兩張圖的角色是：

| 圖 | 角色 |
|---|---|
| 主圖（各類型業配率） | 回答「錢往哪去」，呈現投放偏好的**分布** |
| 側上圖（廠商進場時序） | 本頁的**主要因果證據**，是真正的主角 |

### ⚠️ 主圖的資料陷阱：年代效應

主圖包含全部 14 個類型（不做年份排除），所以會出現兩根接近零的長條，
**它們是年代效應，不是廠商偏好**，逐項揭露時必須主動說明：

| 類型 | 長片數 | 業配率 | 年代集中在 |
|---|---|---|---|
| 遊戲實況 | 696 | 0.3% | **2015–2016 佔 91.1%** |
| 電競賽事 | 446 | 2.2% | 2010–2011 佔 64.6% |

那個年代台灣還沒有成熟的 YouTube 業配市場（長片業配率 2010–2016 全部在 5% 以下，
2017 年 5.2%、2018 年才首次進入兩位數 11.3%）。

⚠️ 連帶影響：KPI「業配率最低類型」會顯示**遊戲實況 0.3%**、
「類型間差距」會顯示 **45.4pp**。這是預期行為，不是錯誤，但**必須在台上解釋**。

🔴 **這只在門檻＝「全部」時成立。** 門檻拉到 100 萬以上時，遊戲實況只剩 1 支、
業配率變成 0.0%，且會與日常Vlog、玩具/收藏**並列最低**；門檻 200 萬時更有七個類型並列 0.0%。
`業配率最低類型` 已於 2026-08-06 加上樣本檢查與並列規則（見 `DAX.md` 第三節），
100 萬時固定顯示**玩具/收藏**。**門檻演出到 100 萬為止**，這格 KPI 才有意義。

### 一個必須主動說明的表面矛盾（2026-08-02 已量化）
第 5 頁說「廠商在系列證明後才進場」，第 6 頁說「新系列第一集就有業配」。
**解釋**：機制改變——早期廠商評估個別系列，現在評估 Joeman 這個品牌。

**這個矛盾現在有數字了**：既有系列 22 個裡只有 2 個（**9%**）第一集就有業配，
新系列 4 個**全部**都有（**100%**）。《開箱去》等到第 91 集、《看房》等了 719 天。

**這是全報告最有力的收尾，不是漏洞。**

---

## 十、背景圖規格

- 尺寸 **1920×1080**，YouTube 深色介面風格
- 配色：底 `#0F0F0F`、卡片 `#1F1F1F`、標題列 `#272727`、強調 `#FF0000`
- 字級：卡片標題 30px、**KPI 標籤 21px**、篩選器標籤 27px、頂部主標題 34px
- **KPI 卡內部分配（118px）**：上內距 16 ＋ 標籤 29 ＋ 間隔 2 ＋ **數值區 64** ＋ 下內距 7
  - 數值區必須 ≥ 64px，Power BI 才放得下 33pt（＝44px、行高 60px）的數字
  - ⚠️ **Power BI 字級單位是 pt、畫布座標是 px，pt × 1.333 = px**
- 版面：KPI 118px → 圖表區 624px → 篩選器 130px（無篩選器頁圖表區延伸至 780px）
- **背景圖只含容器與靜態文字**，KPI 數值、圖表、篩選器按鈕由 Power BI 疊上
- 座標見 `assets/layout_page{N}.csv`

### Power BI 套用方式
1. 檢視 → 頁面格式 → 16:9 → 手動改為 1920×1080
2. 格式化頁面 → 畫布背景 → 選 PNG → **影像調整「符合」、透明度 0%**

### 圖表配色規則
| 顏色 | 用途 |
|---|---|
| `#FF0000` | **每頁最多一處**的重點 |
| `#A0A0A0` | 主要資料條 |
| `#8C8C8C` | 次要資料條 |
| `#5C5C5C` 虛線 | **不可比／不可信的資料** |

---

## 十一、GitHub 自動化

```
每天台灣時間 06:00（cron: 0 22 * * * UTC）
  → checkout → 安裝套件 → python -m src.fetch → python -m src.transform
  → git add data/processed data/shorts_check_cache.json → commit → push
```

- API 金鑰存於 **GitHub Secrets** 的 `YT_API_KEY`
- `data/raw/` 已 gitignore（12 MB，每日重抓即可）
- `data/shorts_check_cache.json` **必須提交**，否則每次要重測 400+ 支 Shorts
- 首次執行已驗證成功（1 分 17 秒）

### Power BI 連接網址　🔵 **現行使用中（2026-08-03 由本機路徑切換）**

⚠️ 切換方式與括號陷阱見 `DAX.md` 第 2-0 節第 ② 步。
⚠️ 切換後 Power BI 讀的是 GitHub 上那份，**本機 `git pull` 與否不再影響報表**。

```
https://raw.githubusercontent.com/joker8604169-cloud/joeman-youtube-analytics/main/data/processed/videos.csv
https://raw.githubusercontent.com/joker8604169-cloud/joeman-youtube-analytics/main/data/processed/daily_stats.csv
https://raw.githubusercontent.com/joker8604169-cloud/joeman-youtube-analytics/main/data/processed/data_notes.csv
https://raw.githubusercontent.com/joker8604169-cloud/joeman-youtube-analytics/main/data/processed/limitations.csv
```

⚠️ **Power BI Desktop 沒有自動刷新**，要按「重新整理」。若要真正自動，
需發布到 Power BI Service 並設定排程刷新。

---

## 十二、已知限制（14 項，完整見 `limitations.csv`）

### 看不到
- 會員限定影片（API 回報 3,604 支 vs 可抓取 3,502 支，差距約 102 支）
- 歷史每日觀看數（API 只給當下累計值）
- YouTube Analytics（留存、流量來源、收益）
- 影片畫面與口頭內容
- 產品上市日期

### 判不準
- Shorts 業配偵測（召回率 14.6%）
- 未分類影片 414 支（11.8%）
- 業配的金額與合作深度
- 業配與樣機提供的界線
- 系列內部的業配成效（樣本不足）

### 不能推論
- 無對照頻道，無法排除整體市場趨勢
- 2024 年後影片仍在累積，跨年觀看比較限 2023 年前
- 2026 年僅半年資料
- 分類規則需人工維護

---

## 十三、踩過的坑（避免重蹈覆轍）

### 1. 規則 CSV 的逗號陷阱 🔴
正則 `感謝.{0,20}(贊助)` 未加引號 → 被 CSV 從逗號切斷 → **規則靜默失效**。
業配數從 173 誤判為正確值 403。已加欄位數檢查。

### 2. 假的每日快照 🔴
`transform.py` 原本用「執行時間」當快照日期，對同一份 raw JSON 重跑會產生
**數值完全相同卻標示不同日期**的假快照。已改為取自 `fetch_meta.json` 的抓取時間。

### 3. 說明欄罐頭文字污染 🔴
1,314 支影片有相同的系列連結列表。「連結」一詞命中率 35.7%、「團購」10.8%，
全是罐頭造成。必須**先剔除罐頭再比對**。

### 4. 新系列漏分類 🟡
《對決》停播後開了《公車食刻表》《Joe是要跟團》等新系列，規則認不出來，
導致「系列化比例掉到 7%」的錯誤結論（實際是 19%）。已補 9 條規則並加入
`review_queue.csv` 偵測機制。

### 5. Shorts 業配率的錯誤解讀 🔴
原以為「Shorts 業配率 3.2%，商業價值低」，實測後發現是**偵測失效**，
真實約 37.9%。連帶推翻「整體業配率下降是 Shorts 稀釋」的說法——
正確是「**整體業配率下降幾乎全是偵測失效造成的假象**」。

### 6. ~~業配溢價的過度解讀~~　❌ 本項已於 2026-08-05 移除

曾經花很多力氣分析「業配影片的觀看比非業配高」這件事，並設計了一整張圖去處理它。
**那整條路是錯的出發點**——依第零章，「業配是否帶來流量」不成立、也不需要被討論或反駁，
提出它本身就會顯得奇怪。相關的視覺、腳註與講稿都已移除（第 4 頁側上圖已換題為「業配偵測缺口」）。

📌 留一句可用的：**跨年代比較觀看數一律要控制年代**。2016 年以前的遊戲實況中位只有幾千，
和近年的企劃型長片放在一起算任何比較都會失真。這條與業配無關，是通用的資料衛生。

### 7. 年化成長率的過度外推 🟡
用 8 天成長 × 45.6 推估年化成長率，忽略衰減，對新影片是高估。
**已改用直接觀察（8 日增量中位數）取代外推。**

### 8. 標題含換行 → Power BI 把資料切錯行 🔴
三支影片的標題把雜湊標籤打在第二行（`#GalaxyAI⏎#GalaxyUnpacked`）。
pandas 依 CSV 標準用雙引號包住這種欄位，**檔案本身完全合法**。

但 **Power BI 的「文字/CSV」連接器預設產生 `QuoteStyle=QuoteStyle.None`**，
那個設定叫它無視雙引號，於是在換行處硬切成新的一列，後續欄位整體位移——
症狀是 `video_id` 出現標題碎片、`duration_seconds` 變成 Error、
`video_format` 顯示「挑戰/企劃」。

**已在源頭修掉**：`transform.py` 的 `sanitize_text_columns()` 在寫檔前
把文字欄的換行壓成空白。刻意只在**輸出端**套用，不動 `df` 本身——
分類與業配判定都拿 `title` 做正則比對，提前改動可能讓判定結果產生微小差異。

Power BI 端仍建議把 M 程式碼的 `QuoteStyle.None` 改成 `QuoteStyle.Csv` 當第二道防線。

### 9. `dtype == object` 在 pandas 3.x 靜默失效 🔴
`sanitize_text_columns()` 第一版寫成 `if df[col].dtype == object:`。
**pandas 3.x 起文字欄的 dtype 是 `str` 不是 `object`**，這個條件永遠為假，
每一欄都被跳過，**函式等於什麼都沒做，而且不報任何錯**。

更危險的是本機（3.0.3）與 CI runner（可能是 2.x）行為不一致——
本機測不出來的東西，線上卻是好的，反之亦然。

已改用 `pd.api.types.is_string_dtype(col) or col.dtype == object` 同時涵蓋兩代。
**日後寫任何型別判斷都要想到這件事。**

### 10. 本機 `data/raw` 會過期，重跑 transform 會回滾資料 🔴
`data/raw/` 被 `.gitignore` 排除、**不會同步**，但 `data/processed/` 會進版控。
所以從 GitHub 拉下來之後，本機是這個狀態：

```
data/raw/        ← 你上次本機執行 fetch 的時間（可能是好幾天前）
data/processed/  ← GitHub Actions 今天早上抓的（比較新）
```

此時直接跑 `python -m src.transform`，會**拿舊素材覆蓋掉新成品**，
而且不會有任何警告——2026-08-01 就這樣把 3,502 支變成 3,498 支。

**正確做法**：本機要重跑清洗前，先 `python -m src.fetch`（235 units），
或確認 `data/raw/fetch_meta.json` 的 `fetched_at_utc` 不比
`data/processed/data_notes.csv` 的「更新時間」舊。

真的覆蓋掉了就 `git checkout -- data/processed/` 還原。

---

## 十四、使用者的工作方式偏好（重要）

1. **建立任何檔案前都要先詢問**，包含一次性的分析暫存腳本
2. **設計討論到有定論才產出成品**，不要邊討論邊產圖（曾造成三次重做）
3. 會**主動質疑數據與邏輯**，且質疑通常是對的——被質疑時先查證再回答
4. 要求**正式的中文統計用詞**（信賴區間，不用 CI）
5. 不接受幻覺、虛構數據、無意義誇獎
6. 偏好**先看模擬再決定**，模擬用 inline 呈現（不建檔）

---

## 十五、下一步建議

### ✅ Power BI 檔案狀態：八頁全部完工並驗證（2026-08-06）

七頁報表 ＋ 第 8 頁結尾頁，約 40 個量值、兩張中斷連結表、三個計算欄位、編輯互動全部就位。
資料來源已切換為 GitHub raw 網址。

🔴 **`.gitignore` 排除 `*.pbix`，Power BI 檔案不會進版控。**
**請自行複製一份到雲端硬碟或隨身碟**——這是整個專案唯一沒有備份的東西。

### ⚠️ 發表前必須重新確認數字

資料每天自動更新，**講稿裡的所有數字都會變動**：

| 講稿中的數字 | 會變嗎 |
|---|---|
| 長片業配率 53.2%（2022 年起） | ✅ 會 |
| 《對決》佔 21.1% | ✅ 會（分母增加，佔比會緩慢下降） |
| 新系列中位觀看 59.9 萬 | ✅ 會（新影片持續累積） |
| 長片系列化比例 38% | ✅ 會 |
| 精確率 22/22、召回率 76.3%、Shorts 召回率 14.6% | ❌ 不會（人工抽樣的固定值） |

**發表前一天請開啟 Power BI 按「重新整理」，把最新數字抄進講稿。**
變動幅度通常只有零點幾個百分點，但口頭說的與畫面顯示的必須一致。

### 立即要做（發表前）
0. 🔴 **把 2026-08-06 的量值改動貼進 Power BI**——`業配率最低類型` 加了樣本檢查與並列規則
   （`DAX.md` 第三節）。驗收：門檻點 100 萬時 KPI2 應顯示**玩具/收藏**，
   若顯示「電競賽事」或「日常Vlog」代表沒貼成功
1. **講稿數字重抄**——前一天在 Power BI 按「重新整理」，照畫面把數字抄進
   **`簡報講稿.docx`**（主要講稿）與 `簡報執行指南.docx`（施工與備答手冊）
2. **排練三個關鍵點擊**：第 3 頁格式三步、第 4 頁年份三停點＋資料範圍切換、第 5 頁點系列
3. **每頁存一張 PNG** 備援；**`.pbix` 複製到雲端**

### 發表後再處理
- **規則缺口**：2026-08-05 重測發現的 14 支漏抓（資訊欄有推銷但規則沒命中）。
  🔴 發表前不要改規則——要重跑全部並重驗每一頁，且不影響現在的結論
- `sponsor_evaluate.py` 輸出的「模型偵測」是誤稱，改為「規則偵測」
- 第 3 頁主圖工具提示頁（`BUILD.md` 8-4）、折線成長率工具提示（8-5，需載入 `daily_stats`）

### 已結案的細項
- ~~KPI 卡的數值格式（萬／億）~~ 一律千分位整數，不用「萬」
- ~~第 6 頁氣泡圖要放幾個系列~~ 正片集數 ≥3，共 26 個
- ~~2023/11/06 事件是否值得做日期鑽取~~ **已查證結案（2026-08-01）**，見下方

### 2023/11/06 事件的分析結果（2026-08-01 查證，已決定納入第 4 頁口白）

**結論：測得到，而且結論是「測不到顯著影響」。** 決定**只寫進第 4 頁講稿，不加任何視覺**。

業配率是這份資料裡唯一能乾淨測量事件衝擊的指標——它不受觀看數累積時間影響。

| 觀察項 | 結果 |
|---|---|
| 停更天數 | **0 天**。事件當天照常上片，11/07 發道歉影片，11/13 恢復正常內容 |
| 發片間隔 | 事件前後最大 7 天、中位 3 天，節奏未斷 |
| 業配率（前後各 180 天） | 51.8%（n=83）→ 51.8%（n=56），差 **−0.0pp**，95% 信賴區間 [−16.8, +16.7] **跨越零** |
| 業配率（前後各 365 天） | 54.8%（n=168）→ 52.1%（n=142），差 −2.6pp，95% 信賴區間 [−13.7, +8.5] **跨越零** |
| 事件後首支業配影片 | 第 **9** 天（2023-11-15） |

⚠️ **長片產量 −32.5%（前後各 180 天）不可歸因於事件**：同期 Shorts +21.7%
（365 天 +73.1%），且 2024Q4 長片回到 60 支／季（全期最高）。那是第 3 頁的結構轉移。

⚠️ **必須同時聲明的邊界**：測得到「廠商投放意願」與「產量」，
**測不到觀看層面的衝擊**——API 只提供當下累計觀看，無歷史每日數據，
訂閱與觀看的即時反應這份資料永遠答不出來。且 n 僅 56–142，
信賴區間寬達 ±17pp，只能說「**測不到顯著變化**」，**不能說「證明沒有影響」**。

### 已明確排除的
- 大麻事件（2023/11/06）不單獨成頁
- 事件的因果推論分析（無對照組、資料期間不足）
- 時間序列分析（快照僅 3 日，需 30 日以上）

---

## 十六、修訂紀錄

### 2026-08-06｜兩處判定錯誤修正、第 5 頁 KPI2 修正、講稿重寫

| # | 修改 | 理由 |
|---|---|---|
| 1 | 🔴 **`manual_overrides.csv` 新增 `qstdz2GV4SI`（`is_sponsored=False`）** | 《Joeman大麻風暴》紀錄片說明欄寫「**本影片與臺灣新北地方檢察署合作**」，命中權重 3 的 `影片合作聲明` 單條成立。對象是**地檢署**、屬反毒公益合作，**無商業委託**。同一條規則 2026-08-03 已因《2015年最令我印象深刻的10款遊戲》覆寫過一次，此支為漏網。發現途徑：門檻點 100 萬時遊戲實況從 0.4% 衝到 33.3%，回查只剩 3 支、其中 1 支被判業配 |
| 2 | **`playlist_map.csv`【第九人稱】：`遊戲實況` → `人物/訪談`**，並補 `series_name=第九人稱` | 該播放清單 3 支全是社會議題紀錄片（詐騙群組臥底、大麻風暴、黃牛產業鏈），與遊戲實況無關。第四支《一個月工作 400 小時的人生》不在清單內、靠標題規則已在人物/訪談——改完四支一致 |
| 3 | **`業配率最低類型` 加 `&& [樣本數_長片] > 0` 與第二組排序 `[@樣本], DESC`** | ① `[僅長片]` 不含觀看門檻，高門檻時會有類型「通過 ≥20 閘門但一支不剩」，其業配率是 BLANK，而 `TOPN ASC` 把 BLANK 當成比 0 還小 → 100 萬門檻下 KPI2 顯示**電競賽事**，但它破百萬 0 支。② 修好後變成三個類型並列 0.0%，取誰是任意的且**會隨每日資料變動**。詳見 `DAX.md` 第三節 |
| 4 | 全文數字重算並同步 | 影片總數 3,504→**3,505**、Shorts 479→**480**、業配 541→**540**（長片 514→**513**）、推估實際 674→**672**、2022 起業配率 53.4%→**53.2%**、未分類 414→**415**、類型間差距 45.0→**45.4pp**、遊戲實況 0.7%→**0.3%**、電競賽事 2.5%→**2.2%**、新系列中位觀看 634,262→**640,117**、《對決》最後一年 644,344→**646,228**（相差 1.6%→**0.9%**）、新系列總觀看 1,506 萬→**1,534 萬**、單集中位比例 78%→**79%**、破百萬新系列 5→**6 集** |
| 5 | **新增 `簡報講稿.docx`** | 依使用者要求重寫全部八頁講稿，用字改為非專業人士可懂。每頁固定「主故事 → 圖表故事 → 講稿」，講稿以「話術 → 操作 → 話術 → 操作」交錯編排（33 個操作點、10 個踩雷提醒）。**`簡報執行指南.docx` 未更動**，兩份定位不同：舊的是施工與備答手冊，新的是純講稿 |
| 6 | Spearman ρ 重算仍為 **−0.07** | 第 5 頁「主圖撐不起本頁論證」的依據未受上述改動影響，結論不變 |

⚠️ 第 1、2 點改的是 `rules/`，**必須重跑 `transform` 才會生效**。本機 `data/raw` 過期不可直接跑
（第十三章第 10 點），正確路徑是 commit → push → GitHub Actions 手動觸發 → Power BI 重新整理。
第 3 點是量值改動，貼進 Power BI 即時生效，但 **`.pbix` 不進版控，記得存檔並備份**。

⚠️ **主線結論完全不變**：產能結構轉移、廠商投放未減、規模缺口與接棒動能四個論點都不受影響，
第 6 頁的 2/22 與 4/4 一個字都沒動。

### 2026-08-01｜第 5 頁論證權重、事件查證、數字更新

| # | 修改 | 理由 |
|---|---|---|
| 1 | 第 5 頁主圖降為配角、側上圖升為主要證據（第九章） | 跨類型橫向比較中業配率與觀看數無相關（Spearman ρ = −0.07）；人物/訪談 74.5 萬／16.0% vs 挑戰/企劃 72.0 萬／51.4%，流量相同但業配率差三倍。橫向排序反映的是內容適不適合置入商品，不是流量高低。縱向的「廠商進場時序」控制住了內容型態、主持人與系列本身，唯一變動的是已累積的成績，才是有效的因果證據 |
| 2 | 新增主圖年代效應警語（第九章） | 主圖含全部 14 類型，遊戲實況 0.7%（2015–2016 佔 90.7%）與電競賽事 2.5%（2010–2011 佔 64.6%）會呈現為接近零的長條。2010–2016 長片業配率全在 5% 以下、2018 年才破兩位數 11.3%——是年代效應不是廠商偏好。連帶使 KPI「業配率最低類型」顯示遊戲實況、「類型間差距」顯示 45.0pp，屬預期行為但必須在台上解釋 |
| 3 | 敘事鏈第 3 環：挑戰企劃 54%／美食 14% → **45.7%**，證據改標為進場時序 | 原數字以 8 種口徑測試均無法重現（見 `DAX.md` 修訂紀錄第 5 點），且證據來源本身標錯 |
| 4 | 2023/11/06 事件查證結案，新增完整結果（第十五章） | 原列為「尚未討論的細項」。查證結論：**測得到，且結論是測不到顯著影響**——停更 0 天、事件前後各 180 天業配率 51.8%→51.8%（差 −0.0pp，95% 信賴區間 [−16.8, +16.7] 跨越零）、第 9 天即有業配上架。已決定寫入第 4 頁口白、不加視覺 |
| 5 | 事件分析加註「測不到觀看衝擊」 | API 只提供當下累計觀看、無歷史每日數據，觀看與訂閱的即時反應永遠無法測量。且 n 僅 56–142、信賴區間寬達 ±17pp，只能說「測不到顯著變化」，不能說「證明沒有影響」 |
| 6 | 長片產量 −32.5% 明確標示不可歸因於事件 | 同期 Shorts +21.7%（365 天 +73.1%）、2024Q4 長片回到 60 支／季為全期最高，屬第 3 頁的結構轉移 |
| 7 | 第 4 頁資料範圍切換 27% → **30.4%**；新系列中位觀看 59.2 萬 → **59.9 萬** | 資料每日更新造成的漂移 |
| 8 | 《看房》進場時序補齊完整年序（0%→0%→42.9%→40.9%→91.7%）；《對決》81% → **86.3%** | 同上 |
| 9 | 文件開頭加註「數字為當日快照」 | 資料每日自動更新，避免日後引用過期數字 |
| 10 | **第 5 頁背景圖重產為三格篩選器**（系列 480／類型 480／觀看數門檻 534） | 決議「14 個類型全部保留、用篩選器逐項加入演出年代效應」，但原背景圖只印了兩格標籤。`make_background.py` 的 slicers 是參數化的，第 3 頁早已在跑三格版。舊版 PNG 保留在 commit `f32695a` |
| 11 | **7 張背景圖 KPI 卡重產**：標籤 27px→**21px**、數值區 48px→**64px** | 實測 msjh 字型行高後發現 48px 的數值區最多只能放 26pt，數值僅比標籤大 1.26 倍，KPI 視覺層次過弱。標籤縮回本章原本就記載的 21px 後，數值可放 33pt（2.1 倍），**圖表區完全未動**。寬度從來不是問題——最寬的值「挑戰/企劃」在 44px 時只佔 194px，可用 339px |
| 13 | **`transform.py` 新增 `sanitize_text_columns()`**，輸出前清掉文字欄的換行；現有 `videos.csv` 已就地套用（3 列標題變動，其餘完全未動） | 三支影片標題含換行，Power BI 的 CSV 連接器預設 `QuoteStyle.None` 會據此切錯行，造成欄位整體位移。詳見第十三章第 8～10 點——同時記錄了 pandas 3.x 的 `dtype == object` 靜默失效，以及本機 `data/raw` 過期會讓重跑 transform 回滾資料 |
| 12 | **新增 `BUILD.md` 逐頁施工單** | `DAX.md` 只寫「用什麼圖、要說什麼話」，沒寫「欄位拖到哪個欄位區、格式怎麼設」。施工單補這一層，並記錄實作時才會發現的缺口（見下） |

**`BUILD.md` 記錄的實作缺口**（`DAX.md` 原本沒有的）：
- 折線圖的線條樣式**不支援條件式格式**，第 3 頁側上圖的「2024 後改灰虛線」
  必須拆成兩個量值畫成兩條線
- 缺 `業配標籤` 計算欄位（否則第 4 頁側上圖的軸會顯示 True／False）
- 缺 `主圖色_最高標紅`、`系列總觀看` 兩個量值
- 第 4 頁年份區間**必須預設 2023–2026**，否則 KPI 顯示 17.2% 而非 53.0%
- 三處 Y 軸必須手動鎖定範圍，否則自動縮放會反駁口白

### 觀看數門檻的演出風險：高門檻時的截斷選擇效應 🔴

門檻拉到 **200 萬**時，第 3 頁側上折線的**尾段會往上驟升**（2026 年跳到 352 萬），
看起來像「近年影片表現變好」。**那是假象**，機制有三層：

**① 樣本剩兩支**

| 年 | 長片數 | ≥200 萬 | 存活率 | 最低 | 中位 | 最高 |
|---|---|---|---|---|---|---|
| 2019 | 169 | **15** | 8.9% | 2,012,434 | **2,652,492** | 10,576,826 |
| 2022 | 179 | 11 | 6.1% | 2,034,570 | 2,254,682 | 3,444,530 |
| **2026** | 63 | **2** | 3.2% | **3,452,566** | **3,521,521** | 3,590,476 |

**② 右偏分布下，存活者通常擠在門檻附近**
2019 年最低的存活者是 **201 萬——剛好壓線**。這種「勉強過線」的影片在存活者裡佔多數，
把中位數拉到貼近門檻。**2026 年一支勉強過線的都沒有**，最低的就已經是 345 萬。

**③ 真正的原因：時間截斷造成雙重選擇**
2026 年的影片只有 0–7 個月累積時間。要在這麼短的時間衝破 200 萬必須是**爆發型**
（那支 07-11 發的影片，**22 天累積 359 萬**）。而「最終會到 200 萬、但現在停在 150 萬」
的影片全部被門檻擋在外面。

> **對近期年份，門檻篩出來的不是「該年的前 3%」，而是「該年累積最快的那幾支」。**

**這跟第 4 頁的選擇效應是同一類錯誤**：篩選條件本身與被測量的變數相關，
樣本就不再代表母體。Q&A 被問到時可以直接說「這跟我在第 4 頁拆掉的是同一種偏誤」。

**實務結論**：**門檻演出到「100 萬」為止**（各年還有 8–54 支）。
真要點到 200 萬就補一句「每年只剩兩三支，右邊那條上翹的尾巴是樣本太少」。

### 2026-08-02｜Power BI 實作期間的修正

| # | 修改 | 理由 |
|---|---|---|
| 1 | 「總產能**三年**增加 78%」→「**2019 到 2025 年**增加 78%」 | 實測所有三年區間都對不上（2022→2025 是 +63.9%、2021→2024 是 +67.8%、2020→2023 是 +56.4%）。78%／−22%／0→169 這組數字**橫跨六年** |
| 2 | 第 3 頁新增「2015 年 530 支」的必講說明 | 全期圖表上最高的是 2015 年（530 支）不是 2025 年（300 支），只說「總產能提高」會被打臉（從 2015 算是 **−43%**）。解釋：2015 的 530 支裡遊戲實況佔 435 支（82%），是日更實況時期，與企劃型長片不可比 |
| 3 | 第 3 頁側下改為 **100% 堆疊區域圖** | 絕對數量會被 2015 年的 530 支壓垮，其他年份全變貼地細線。改比例後四個世代的轉型才看得出來。且分工正確——主圖負責總量、側下負責比例 |
| 4 | 第 3 頁背景圖重產，篩選器寬度 380/690/420 → **260/480/754** | 年份區間原本只有 420px，「介於」樣式的滑桿擠不出來（第 4 頁踩過同一個坑，解法是「標籤在左、控制項在右」，需要約 610px） |
| 5 | 第 4 頁年份預設改為**全期 2010–2026**，演出改為三停點 | 使用者決定敘事從 2010 年開始逐步推進。實測發現**全期時「資料範圍」切換只差 1.6pp**（Shorts 2021 年才出現），所以**演出順序被資料結構鎖定**：必須先拉年份到 2022，才切資料範圍 |
| 6 | 關鍵數據欄的「近三年 53.0%」→「2022 年起 53.3%，全期僅 17.2%」 | 讓兩個口徑並列，避免日後又混用 |

### 2026-08-02（下午）｜第 6 頁改版與交接

| # | 修改 | 理由 |
|---|---|---|
| 7 | **第 6 頁 KPI4「新系列業配率」→「首集就有業配 4/4」**、側下圖「新系列動能對照」→「**首次業配出現在第幾集**」 | 原收尾論點的信賴區間跨越零，照本文件第七章第 7 點不能下結論。改用二元類別觀察，見第七章 5c |
| 8 | **`系列世代` 界線改用《對決》停播日 2025-11-10** | 見第七章 5b |
| 9 | 新增計算欄位 `是首集`；新增量值 `首集就有業配`、`首次業配集數`、`首次業配出現在第幾集` | 支撐新的 KPI4 與側下圖 |
| 10 | 第 6 頁側上圖宣稱：「81%→38%」→「**2022 年 81.0% → 2025 年 64.1%**」 | 原講法終點是不完整年度（2026 只有 7 個月）、起點是全期最高峰，用最高點對半年會放大落差 |
| 11 | 第 6 頁主圖氣泡圖篩選：集數 ≥10 → **≥3** | ≥10 時只有一個新系列露臉，畫面撐不起「新系列」這一群 |
| 12 | 第 6 頁背景圖重產兩次 | ① 篩選器寬度 600/960→830/664（年份滑桿需橫向空間）② KPI4 與側下卡片標題更換 |
| 13 | 第 3 頁背景圖重產、完工並驗證 | 篩選器寬度 380/690/420→260/480/754；側下改 100% 堆疊區域圖；「三年 78%」修正為「2019→2025」 |
| 14 | 全報表的編輯互動確定為 **5 處**（第 3 頁 2 處、第 5 頁 1 處、第 6 頁 2 處） | 判準：只有視覺被篩選後會**錯誤或自相矛盾**時才封鎖，詳見 `BUILD.md` 第六節 |

### 2026-08-05／06｜因果邏輯定案、召回率重測、第 4 頁側上圖換題

| # | 修改 | 理由 |
|---|---|---|
| 1 | **新增第零章：最高原則**，置於全文最前 | 業配不帶動流量，只有「流量→業配」一個方向。理由是機制不是統計——沒有人為了看業配而點影片，且業配在拍攝前談定、當時觀看數還不存在。**且不得以「拆掉這個誤會」的方式鋪陳**，那會顯得奇怪 |
| 2 | 依第零章清除全報表的相關敘述 | 報表主軸、`DAX` 9-4 結論、第十三章第 6 點整條移除；`業配中位觀看`／`非業配中位觀看`／`觀看差距` 三個量值連同視覺一併移除 |
| 3 | 🔴 **長片召回率重測：85.3% → 76.3%** | 發現 `sponsor_recall_sample.csv` 實為 25 Shorts ＋ 14 長片的混合樣本，`sponsor_evaluate` 不帶參數也不篩格式。重抽 40 支純長片實測漏抓率 14/40。詳見第八章 |
| 4 | **精確率改為 22/22**（原 96.8% 為混合樣本 30/31） | 拆出長片專屬 22 支全對，95% 信賴區間 85.1–100%。KPI4 顯示「22 / 22」不寫 100%，理由同第 6 頁 KPI4 的「4 / 4」 |
| 5 | `sponsor_sample_recall.py` **不指定格式時中止** | 原本會寫入 `sponsor_recall_sample.csv`，**覆蓋既有人工標記（永久資產）**；排除清單也漏了既有的漏抓率抽樣檔 |
| 6 | 第 4 頁側上圖**換題**：同系列內中位觀看 → **業配偵測缺口（長片與 Shorts）** | 舊圖回答的是「廠商挑影片的顆粒度」，屬第 5 頁主題，在投放規模這頁沒有工作；而這頁最核心的宣稱「整體業配率下降是偵測失效」原本只有口白沒有視覺 |
| 7 | 第 4 頁 **KPI2 鎖長片**與 KPI3 配對、系列篩選器移除、篩選器列回復兩格 | KPI2 浮動時切「全部」變 397（含 Shorts），與 KPI3 的 674（純長片回推）並排會被讀成「漏 37 支」，那是假的 |
| 8 | **背景 PNG 新增靜態圖例功能**（`card()` 的 `legend` 參數） | 側圖內容區只有 205px，Power BI 的圖例吃掉頂端 30px 就會開始掉資料標籤。印在標題列右側零佔位，截圖備援也帶得走 |
| 9 | **量值全面改名**為觀眾看得懂的版本 | 工具提示會直接顯示量值名稱。`業配數_受範圍影響`→`業配影片數（依資料範圍）` 等 6 個 |
| 10 | `is_self_promo` 資料字典降級為查核用 | 2024 年起飽和到 99–100%，量的是「說明欄有沒有罐頭連結」而非「是否在賣自家東西」 |
| 11 | 新增 `DAX.md` **9-7 Q&A 備答**四題 | 精確率／召回率是什麼、信賴區間怎麼讀、判定是規則不是模型、已知的那一支誤判 |
| 12 | 第 5 頁演出補上與第 4 頁紅線重複的**銜接台詞** | 側上圖預設狀態與第 4 頁主圖紅線完全相同，要主動承認並轉成「同一把尺從全頻道縮放到單一系列」 |
| 13 | `簡報執行指南.docx` 全面同步並首次進版控 | 第 2、4、5、8 頁與敘事鏈都受上述改動影響 |

### 2026-08-03｜第 6 頁完工、業配誤判覆寫、版控落差

| # | 修改 | 理由 |
|---|---|---|
| 1 | **第 6 頁七個物件全部完工並驗證**，剩編輯互動兩處 | 主圖散佈圖、側上直條、側下橫條、動態註記卡片皆已對過驗收值 |
| 2 | 第 6 頁主圖**關閉類別標籤**、既有系列改 `#8C8C8C`、新系列改**菱形** | Power BI 散佈圖無法逐點控制標籤，全開時四顆紅點有兩顆被碰撞隱藏。三條控制路徑（條件式格式／唯一零寬字串／排序優先權）全部實測失敗，詳見 `BUILD.md` 第 6 頁主圖那一節 |
| 3 | **`長片系列化比例` 加 `WithSeries + 0`** | 2010 年有 200 支長片、有系列 0 支，`CALCULATE` 回傳 BLANK 使 `DIVIDE` 也是 BLANK，**整個 2010 年從側上圖消失**，X 軸從 2011 開始 |
| 4 | **`首次業配出現在第幾集` 門檻 40 → 50**（9 列 → 8 列） | 205px 塞不下 9 列，右緣長出捲軸，**被推到摺線下的正是《開箱去》第 91 集**——收尾台詞最重要的數字 |
| 5 | 新增 `系列長片數_不受門檻`，第 6 頁主圖與側下圖改用它當篩選條件 | 原本用 `[系列正片數]`，而它內建觀看數門檻——點「100 萬」時四個新系列的集數各剩 0–2 集，被 `≥3` 全部濾掉，**紅點整群消失** |
| 6 | **`類型間差距` 改為文字量值顯示 `pp`** | 兩個百分比相減是**百分點差**，顯示成「45.0%」會被讀成「差了 45%」。且這個版本的 Power BI 格式下拉**沒有「自訂」選項**，只能用 `FORMAT()` 組字串 |
| 7 | 第 5 頁主圖加樣本門檻 `[僅長片] >= 20` | 主圖原本不設門檻，**財經/投資 66.7%（6 支）會排在第一**，而 KPI1 因為有 `>=20` 說「挑戰/企劃 45.7% 最高」——畫面與口白直接矛盾，且標紅的會是第二根 |
| 8 | `主圖色_最高標紅` 加 `ALLSELECTED` | 原版在橫條圖裡**每一列都變紅**：`[業配率最高類型]` 內部的 `VALUES` 遵守當下篩選內容，在單一類型的列上永遠回傳自己 |
| 9 | **`manual_overrides.csv` 新增 8 列**，2010–2016 業配誤判 | 見第八章。業配總數 548 → 540 |
| 10 | 全報表文字色彩統一：**軸文字 `#AAAAAA`、資料標籤 `#F1F1F1`** | 第 3、4 頁文件原寫軸值 `#F1F1F1`，與實作不符。第四節第 ① 點的 `#F1F1F1` 是可讀性告急時的例外條款，實測 9pt `#AAAAAA` 對比 7:1，用不到 |

### ~~🔴 版控落差~~（2026-08-03 發現，**已於 2026-08-05 處理完畢**，以下保留供追溯）

```
M src/transform.py        +39 −4    sanitize_text_columns 等修正
M src/make_background.py  +33 −7    第 3/5/6 頁篩選器重新分配
M assets/bg_page3~6.png              重產的背景圖
?? BUILD.md ?? DAX.md ?? HANDOFF.md  三份文件完全不在版控裡
```

**GitHub Actions 跑的是 repo 裡的版本**，所以每日自動更新執行的是**沒有
`sanitize_text_columns()` 的舊版 transform**——GitHub 上那份 `videos.csv`
（**Power BI 實際讀取的那份**）並未套用換行清理。第十三章第 8 點說「已在源頭修掉」，
實際上只修在本機。

**處理順序**（`git checkout` 那步是破壞性的，執行前先確認）：

```bash
git checkout -- data/processed/videos.csv     # 丟棄本機 3 行差異，transform 會重產
git add src/ assets/ rules/ BUILD.md DAX.md HANDOFF.md
git commit -m "修正文字欄換行清理、背景圖版面與業配人工覆寫"
git pull
git push
```

然後到 GitHub → Actions →「每日更新頻道資料」→ **Run workflow** 手動觸發
（workflow 已設 `workflow_dispatch`），約 1 分 20 秒。跑完 `git pull`，
Power BI 按重新整理。**這時才是新版 transform ＋ 覆寫規則 ＋ 當日最新 API 資料。**

⚠️ 本機 `data/raw` 停在 2026-07-29，**不要在本機跑 `transform`**（第十三章第 10 點）。
要在本機跑就得先 `python -m src.fetch`（235 units）。

### 交接時最該知道的五件事

1. **`BUILD.md` 是主要工作文件**。第 3、4 頁那兩節是實作驗證過的版本，
   其他頁遇到相同狀況（回應式鎖字級、前 N 項並列、資料標籤被裁、循環相依）直接照抄。
2. **背景圖改過就要在 Power BI 裡重新套用畫布背景**，它不會自動更新。
3. **量值的 `VAR` 名稱只能用 ASCII，且不能跟 DAX 函式同名**（踩過 `篩選後`、`lastDate` 兩次）。
4. **凡是含日期或期間的註記一律用量值**，寫死的期間會過期，
   且與「本報表每日自動更新」這個賣點自相矛盾。
5. **第 5 頁的「類型逐項加入」演出已實測可用**（2026-08-06）——下拉選單在畫布底緣
   會自行向上展開，14 個類型都點得到。若排練時覺得**節奏**不順（而非技術問題），
   零成本的退路是改拉年份滑桿演出年代效應（`BUILD.md` 第 8-1b 節方向 C）。

**Power BI 實作進度**：🎉 **八頁全部完工並驗證**（2026-08-06）。
逐頁施工細節與實測踩坑紀錄見 `BUILD.md`。

**剩下的只有**：貼上 2026-08-06 的 `業配率最低類型`、講稿數字重抄、排練、
每頁截圖、`.pbix` 備份到雲端。

**講稿文件有兩份，別搞混**：

| 檔案 | 定位 |
|---|---|
| **`簡報講稿.docx`** | **上台唸的主講稿**（2026-08-06 新增）。八頁，每頁「主故事 → 圖表故事 → 講稿」，話術與操作交錯編排 |
| `簡報執行指南.docx` | 施工與備答手冊。含敘事鏈、時間分配、Q&A 備答、各圖表的設計理由 |
