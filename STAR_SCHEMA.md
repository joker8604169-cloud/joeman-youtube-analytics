# 星狀結構模型設計

> 資料清洗與維度建模的規格書。搭配 `DAX_星狀模型.md`（量值改寫）使用。
> 最後更新：2026-08-10
>
> ⚠️ 文中筆數為 2026-08-10 快照，資料每日自動更新。

---

## 目錄

1. [為什麼要改成星狀結構](#一為什麼要改成星狀結構)
2. [模型全貌](#二模型全貌)　—　含代理鍵編號規則
3. [資料清洗規則](#三資料清洗規則)
4. [資料表字典](#四資料表字典)
5. [關聯設定](#五關聯設定)
6. [Power BI 載入步驟](#六power-bi-載入步驟)
7. [執行與自動化](#七執行與自動化)
8. [維護指引](#八維護指引)

---

## 一、為什麼要改成星狀結構

舊模型是**一張 24 欄的扁平寬表** `videos`。它能跑，但有四個結構性問題，
而且每一個都已經在這個專案裡真的發生過：

| # | 問題 | 實際發生過的症狀 | 星狀結構怎麼解 |
|---|---|---|---|
| 1 | **空白值沒有型別保證** | `series` 匯入後可能是 null 也可能是空字串，`ISBLANK` 只抓得到前者。抓不到時 `長片系列化比例` 從 36.6% 變成 **100% 且不報錯**（`DAX.md` 七-7） | `dim_系列` 有一個真實成員「（無系列）」，每一列都指向它。判斷改成 `[有系列] = TRUE()`，**不存在空白判斷** |
| 2 | **人工驗證常數散落在量值裡** | 召回率 0.763 寫在兩個量值裡，改了一處沒改另一處，同一頁出現 674 與 603 兩個數字（`DAX.md` 三-第 4 頁） | 常數變成 `dim_影片格式` 的資料行，來源是 `rules/format_audit.csv`，**只有一份** |
| 3 | **屬性層級的計算被迫逐列重算** | `系列世代`、`是首集` 兩個計算欄位各跑一次 `ALLEXCEPT`，對 3,506 列做分組迭代 | 系列的屬性算在 `dim_系列`（**30 列**），影片的屬性算在 `dim_影片`，各算一次 |
| 4 | **排序欄與被排序欄循環相依** | `格式排序` 建成 DAX 計算欄位會報「偵測到循環相依性」，只能繞到 Power Query 做（`BUILD.md` 四-③） | 排序欄是維度表裡的實體資料行，模型看不到任何計算式 |

另外兩個是純粹的建模品質：

- **每日快照終於接得上**。舊模型 `daily_stats` 沒載入，因為要跟 `videos` 建關聯又
  要跟自己的日期軸並存，扁平表接不乾淨。星狀結構下它就是第二張事實表，
  與 `fact_影片` **共用 `dim_日期` 與 `dim_影片` 兩個一致性維度**。
- **查核用欄位與分析用欄位分家**。`sponsor_hits`、`playlists`、`self_promo_products`
  這些「給人追溯判定依據」的欄位全部收進 `dim_影片`，不再和 `view_count` 混在同一張表裡。

### 這次改版**沒有**改的東西

🔴 **判定邏輯完全沒有動。** 分類優先序、業配加權計分、Shorts 網址實測、排除規則、
七張 `rules/` 規則表——`src/transform.py` 一行沒改，`videos.csv` 一格沒變。

本次改動是**在 `transform` 之後多加一層建模**：

```
data/raw/*.json ──[transform.py]──> videos.csv ──[build_star.py]──> star/*.csv
    原始 API            判定與分類        分析寬表        清洗與維度建模      Power BI 模型
                                            ↑
                                    人工抽樣程式仍讀這一份
```

分成兩層的理由：`videos.csv` 是三支抽樣驗證程式（`sponsor_sample`／
`sponsor_sample_recall`／`sponsor_evaluate`）的輸入，而那些抽樣結果是**永久資產**。
建模層若直接改寫 `transform`，等於把已驗證的判定結果與模型設計綁在一起，
日後調整模型會有動到判定的風險。

---

## 二、模型全貌

```
                          ┌──────────────┐
                          │   dim_日期    │  6,209 列（2010-01-01 – 2026-12-31）
                          └──────┬───────┘
                        發布日期 │        │ 快照日期
              ┌──────────────────┴──┐     │
┌───────────┐ │                     │     │
│dim_影片格式│─┤                     │     │
└───────────┘ │                     │     │
┌───────────┐ │     ┌───────────┐   │  ┌──┴──────────┐
│ dim_類型   │─┼─────│ fact_影片  │───┼──│fact_每日快照 │ 38,529 列
└───────────┘ │     │  3,506 列  │   │  └──┬──────────┘
┌───────────┐ │     └─────┬─────┘   │     │
│ dim_系列   │─┤           │         │     │
└───────────┘ │           │         │     │
┌───────────┐ │     ┌─────┴─────────┴─────┴──┐
│dim_業配判定│─┘     │       dim_影片          │ 3,506 列
└───────────┘       └────────────────────────┘

中斷連結（不建關聯）：門檻、資料範圍
純呈現表（不建關聯）：資料特徵、報告限制、資料品質
```

| 資料表 | 種類 | 粒度 | 列數 |
|---|---|---|---|
| `fact_影片` | 事實 | **一支影片一列** | 3,506 |
| `fact_每日快照` | 事實 | **一天一支影片一列** | 38,529 |
| `dim_日期` | 維度（一致性） | 一天一列 | 6,209 |
| `dim_影片` | 維度（一致性） | 一支影片一列 | 3,506 |
| `dim_影片格式` | 維度 | 一種格式一列 | 2 |
| `dim_類型` | 維度 | 一個類型一列 | 14 |
| `dim_系列` | 維度 | 一個系列一列（＋無系列） | 30 |
| `dim_業配判定` | 維度（junk） | 一組判定結果×來源一列 | 4 |

### 2-1 代理鍵的編號規則（append-only）

維度表每天重新產生，代理鍵也跟著重編。**編號依「該成員第一次出現的日期」排序**，
不是字母序、也不是資料量排序。

| 維度 | 鍵的排序依據 |
|---|---|
| `dim_影片` | 發布日期，同日再依 `video_id` |
| `dim_類型` | 該類型第一支影片的發布日 |
| `dim_系列` | 該系列第一支影片的發布日（`0` 保留給「（無系列）」） |
| `dim_業配判定` | 該組合第一次出現的發布日 |
| `dim_影片格式` | 由 `rules/format_audit.csv` 固定為 1／2 |

🔴 **為什麼不用字母序或資料量排序（2026-08-10 改）**

字母序：頻道開一個名字排在中間的新系列，後面所有系列的鍵往後移一位，
**事實表 3,506 列的外鍵全部改變**，每日更新那筆 commit 從「多三列」變成「整份檔案重寫」。
資料量排序更糟：兩個類型的支數交叉就互換，**沒有新成員也會漂**。

改用首次出現日之後，新成員的日期必然最新 → 鍵一律往後追加。實測（模擬同時新增
一個新系列、一個新類型、一個新判定組合）：

```
共同影片 3,506 支
  影片鍵／格式鍵／類型鍵／系列鍵／業配判定鍵    值改變的列數：0
→ fact_video.csv 實際 diff：0 列變動 ＋ 3 列新增
```

⚠️ **唯一會插隊的情況**是補規則讓一批舊影片被重新歸類（新成員的首次出現日落在過去）。
那本來就是全表重算的事件，一年不會發生幾次。

⚠️ 鍵**不保證跨版本永久不變**，只保證「一般情況下不變」。任何時候事實表與維度表
都是同一次執行的產物、一起提交，所以**數字永遠是對的**——鍵的穩定性只影響 diff 大小。

📌 `dim_類型` 的 `類型排序`（給切片器用的顯示順序）**刻意與 `類型鍵` 脫鉤**：
排序欄照全期長片數遞減、「未分類」排最後，會隨資料變動；鍵不會。

---

**為什麼 `fact_影片` 的粒度是「一支影片」而不是「一支影片一天」**：
報表的四個分析主軸（產能結構、投放規模、投放偏好、接棒動能）問的全是
「這支影片是什麼、成績多少」，時間維度只用到**發布日**。
每日快照是另一個粒度、另一個問題（成長速度），所以是第二張事實表。
硬把兩者塞進同一張表會讓 `COUNTROWS` 變成「影片數 × 快照天數」，所有計數全錯。

---

## 三、資料清洗規則

由 `src/build_star.py` 執行，結果寫入 `data/processed/star/data_quality.csv`
（**可直接放進報表第 2 頁當佐證**）。有任一項為「異常」時程式以非零狀態碼結束，
CI 會失敗，不會產出壞掉的模型。

### 3-1 值的正規化

| 規則 | 做法 | 為什麼 |
|---|---|---|
| 文字欄位 | 全形空白轉半形、換行／定位字元壓成單一空白、去頭尾空白 | 標題含換行會讓 Power BI 的 `QuoteStyle.None` 切錯行（`HANDOFF.md` 十三-8） |
| 空字串 | 一律轉成缺值 | 讓「空白」在整個模型裡**只有一種表示法**，這是問題 #1 的根治 |
| 多值欄位（`\|` 分隔） | 去除空片段與重複、統一分隔符 | `playlists`／`sponsor_hits`／`self_promo_products` |
| 布林欄位 | 容忍 `True/False/1/0/是/否/y/n`，統一輸出 `True`／`False` | Power BI 才會自動判成 True/False 型別，不必手動改 |
| 日期 | 一律轉成 `yyyy-MM-dd` | 匯入後直接是日期型別，不需在 Power Query 改 |

### 3-2 🔴 刻意**不**做的清洗

| 欄位 | 不做什麼 | 為什麼 |
|---|---|---|
| `按讚數`（4 支）、`留言數`（1 支） | **不補 0** | 空值代表「創作者關閉了按讚顯示／留言功能」，不是「零個讚」。補 0 會製造假資料。Power BI 的 SUM／MEDIAN 自動略過空值，除法遇空值回傳空白而非錯誤 |
| `業配分數`（9 支） | **不補 0** | 那 9 支走人工覆寫，根本沒有計分，補 0 會被誤讀成「算過，得 0 分」 |
| `series`／`sponsor_hits`／`自營商品` 的大量空白 | **不填補** | 代表該影片沒有系列歸屬／沒命中任何規則，是真實資訊 |
| 觀看數為 0 的影片 | **不在這一層處理** | 已於 `transform` 階段排除並記錄在 `excluded_videos.csv`，重複處理會讓追溯斷掉 |

### 3-3 完整性檢查（16 項，全部會寫進 `data_quality.csv`）

| 類別 | 檢查 | 不通過時 |
|---|---|---|
| 主鍵 | `video_id` 唯一、不可為空 | 去重（保留最後一列）／剔除 |
| 主鍵 | 每日快照複合主鍵（快照日期＋影片）唯一 | 去重（保留最後一列） |
| 型別 | 發布日期可解析為日期 | 剔除該列並記錄 |
| 值域 | 發布時段 0–23、觀看數非負、影片長度非負 | 標記為異常並中止 |
| 列舉 | 影片格式只有「長片」「Shorts」 | 標記為異常並中止 |
| 參照 | `fact_影片` 四個外鍵都對得到維度成員 | 標記為異常並中止 |
| 參照 | 每日快照的影片存在於影片維度 | 剔除孤兒快照 |
| 涵蓋 | 所有發布日期落在 `dim_日期` 範圍內 | 標記為異常並中止 |
| 涵蓋 | `rules/format_audit.csv` 涵蓋所有影片格式 | 標記為異常並中止 |

📌 **「剔除孤兒快照」這一項是有作用的**：`transform` 排除觀看數為 0 的影片時，
會順手把歷史快照裡的同一支影片清掉；萬一哪次沒清乾淨，這裡會擋下來。

---

## 四、資料表字典

### 4-1 `fact_video.csv` → `fact_影片`

| 欄位 | 型別 | 說明 |
|---|---|---|
| `影片鍵` | 整數 | FK → `dim_影片`。同時是本表的主鍵（一支影片一列） |
| `發布日期` | **日期** | FK → `dim_日期[日期]` |
| `格式鍵` | 整數 | FK → `dim_影片格式` |
| `類型鍵` | 整數 | FK → `dim_類型` |
| `系列鍵` | 整數 | FK → `dim_系列`，`0` ＝（無系列） |
| `業配判定鍵` | 整數 | FK → `dim_業配判定` |
| `觀看數` | 整數 | 抓取當下累計 |
| `按讚數` | 整數 | **可為空**（關閉顯示） |
| `留言數` | 整數 | **可為空**（關閉留言） |

### 4-2 `dim_video.csv` → `dim_影片`

| 欄位 | 型別 | 用於視覺？ |
|---|---|---|
| `影片鍵` | 整數 | 鍵 |
| `video_id` | 文字 | 查核用 |
| `影片標題` | 文字 | ✅ 表格／工具提示 |
| `影片網址` | 文字 | ✅ 表格中設為 Web URL |
| `發布時間` | 日期時間 | 參考用 |
| `發布時段` | 整數 0–23 | ✅ |
| `影片長度秒` | 整數 | ✅ |
| `是首集` | **True/False** | ✅ **第 6 頁 KPI4 用** |
| `分類依據` | 文字 | ⚠️ 查核用，不放視覺 |
| `業配分數` | 數值 | ⚠️ 查核用 |
| `業配命中規則` | 文字 | ⚠️ **查核用，勿放視覺** |
| `官方付費宣傳旗標` | True/False | ⚠️ 全頻道僅 3 支為真 |
| `含自營商品導流` | True/False | ⚠️ **近年已飽和，勿做趨勢視覺** |
| `自營商品` | 文字 | ⚠️ 查核用 |
| `所屬播放清單` | 文字 | ⚠️ 查核用 |
| `YouTube官方分類ID` | 整數 | ❌ 未使用 |

### 4-3 `dim_format.csv` → `dim_影片格式`（2 列）

| 欄位 | 長片 | Shorts |
|---|---|---|
| `格式鍵` | 1 | 2 |
| `影片格式` | 長片 | Shorts |
| `格式排序` | 1 | 2 |
| `納入商業分析` | True | **False** |
| `業配召回率` | **0.763** | **0.146** |
| `業配召回率信賴區間` | 69.1–83.6% | 10.5–21.7% |
| `業配精確率` | **22 / 22** | （未量測） |
| `業配精確率信賴區間` | 85.1–100% | — |
| `抽樣數` | 40 | 40 |
| `偵測可信度說明` | 說明欄文字量充足… | 85.7% 說明欄完全空白… |

⭐ **這張表是這次改版的核心價值之一。** 人工抽樣得到的常數從 DAX 裡搬出來變成資料，
`格式召回率` 與 `判定精確率_長片` 兩個量值改成讀資料行，重做抽樣只要改一個 CSV。

🔴 **來源是 `rules/format_audit.csv`，必須與 `src/transform.py` 的 `AUDIT` 字典保持一致。**
兩邊都改，或改完跑一次 `python -m src.build_star` 確認沒有落差。

### 4-4 `dim_category.csv` → `dim_類型`（14 列）

| 欄位 | 說明 |
|---|---|
| `類型鍵` | 鍵 |
| `影片類型` | 14 類，含「未分類」 |
| `已分類` | True/False，「未分類」為 False |
| `類型排序` | 依全期長片數遞減，**「未分類」固定排最後** |
| `全期長片數` | 靜態快照，**僅供排序與查核，不受任何篩選影響** |

### 4-5 `dim_series.csv` → `dim_系列`（30 列）

| 欄位 | 說明 |
|---|---|
| `系列鍵` | `0` ＝（無系列） |
| `系列名稱` | 含成員 **「（無系列）」**，2,374 支影片指向它 |
| `有系列` | True/False。⭐ **判斷有無系列一律用這一欄** |
| `系列世代` | `無系列`／`既有系列`／`新系列` |
| `系列首播日` | 該系列**第一支長片**的發布日 |
| `系列長片集數_全期` | 靜態快照，僅供排序與查核 |
| `系列世代判準` | 文字，例如「首播 2025-11-19，晚於《對決》停播日 2025-11-10 9 天」 |

**系列世代的判定**（與舊 DAX 計算欄位逐條等價，已由 `verify_star.py` 逐列驗證）：

1. 沒有系列歸屬 → `無系列`
2. 該系列第一支**長片**晚於 **2025-11-10**（《Joe是要對決》第 307 集，停播日）→ `新系列`
3. 其餘 → `既有系列`

⚠️ 第 3 點包含「只有 Shorts、沒有長片」的系列——舊 DAX 的
`IF ( firstPub > duelEnd, ... )` 在 `firstPub` 為空時走 FALSE 分支，結果是「既有系列」。
`build_star.py` 照樣處理，數字才對得起來。

🔴 **停播日刻意寫死在 `build_star.py` 的 `DUEL_END_DATE`，不用 `MAX(published_date)` 動態抓。**
動態寫法在《對決》若復播時界線會往後跳，現有的新系列會全部被重新歸類且不報錯。

**目前的四個新系列**：我獨自穿越日本（10 集）、九讀（3）、Joe是要跟團（3）、公車食刻表（3）。
《工作室日常廢片》首播 2025-08-27、比停播早 75 天，正確歸在既有系列。

### 4-6 `dim_sponsor.csv` → `dim_業配判定`（4 列）

| 業配判定鍵 | 是否業配 | 業配標籤 | 判定來源 | 判定來源說明 |
|---|---|---|---|---|
| 1 | True | 業配 | `api` | YouTube 官方付費宣傳旗標 |
| 2 | True | 業配 | `keyword` | 關鍵字加權計分達門檻 2 分 |
| 3 | False | 非業配 | `none` | 未達門檻，判定為非業配 |
| 4 | False | 非業配 | `override` | 人工覆寫（最高優先） |

📌 這是一張 **junk dimension**——把兩個低基數的旗標欄位收成一張小表，
省掉事實表兩欄，也讓「判定來源」在報表上有中文說明可用。

⚠️ 列數會隨資料變動（例如出現「覆寫成業配」的影片就會多一列）。程式自動產生，不必手動維護。

### 4-7 `dim_date.csv` → `dim_日期`（6,209 列）

| 欄位 | 說明 |
|---|---|
| `日期` | **2010-01-01 – 2026-12-31 連續日曆**，補到資料最大年度的年底 |
| `年` | ✅ **年份軸與年份區間切片器都用這一欄** |
| `季` | `Q1`–`Q4` |
| `月` / `月份` | 數字 / 「8月」 |
| `年月` / `年月排序` | `2026-08` / `202608` |
| `觀看可比性` | `可比`（≤2023）／`仍在累積`（≥2024） |
| `是否完整年度` | 資料最大年度為 False，其餘 True |

🔴 **補到 12/31 是刻意的**（讓切片器與月份軸在年中重新整理時不會少一截），
但這代表 `MAX('dim_日期'[日期])` **不是最後一支影片的發布日**。
需要「資料實際到哪一天」時一律取 `MAX('fact_影片'[發布日期])`——
詳見 `DAX_星狀模型.md` 第二節第 3 點。

### 4-8 `fact_daily.csv` → `fact_每日快照`（38,529 列）

| 欄位 | 說明 |
|---|---|
| `快照日期` | FK → `dim_日期[日期]` |
| `影片鍵` | FK → `dim_影片` |
| `觀看數` / `按讚數` / `留言數` | 該日快照值 |

**目前報表沒有任何視覺用到它**，可以不載入。若要做 `BUILD.md` 8-5 節的成長率工具提示，
載入前先看 `DAX_星狀模型.md` 第七節第 2 點的關聯基數設定。

### 4-9 `data_quality.csv` → `資料品質`

| 欄位 | 說明 |
|---|---|
| `檢查項目` / `結果` / `筆數` / `處理方式` | 16 項清洗檢查的結果 |

不建關聯。可放在第 2 頁或第 7 頁當佐證——**「我怎麼知道資料是乾淨的」有一張表可以攤開**。

---

## 五、關聯設定

**八條關聯，全部是「多對一、交叉篩選方向：單一」。**

| # | 從（多） | 到（一） | 基數 | 方向 |
|---|---|---|---|---|
| 1 | `fact_影片[影片鍵]` | `dim_影片[影片鍵]` | **多對一（見下方 🔴）** | 單一 |
| 2 | `fact_影片[發布日期]` | `dim_日期[日期]` | 多對一 | 單一 |
| 3 | `fact_影片[格式鍵]` | `dim_影片格式[格式鍵]` | 多對一 | 單一 |
| 4 | `fact_影片[類型鍵]` | `dim_類型[類型鍵]` | 多對一 | 單一 |
| 5 | `fact_影片[系列鍵]` | `dim_系列[系列鍵]` | 多對一 | 單一 |
| 6 | `fact_影片[業配判定鍵]` | `dim_業配判定[業配判定鍵]` | 多對一 | 單一 |
| 7 | `fact_每日快照[影片鍵]` | `dim_影片[影片鍵]` | 多對一 | 單一 |
| 8 | `fact_每日快照[快照日期]` | `dim_日期[日期]` | 多對一 | 單一 |

🔴 **第 1 條要手動把基數改成「多對一」。**
事實表與影片維度都是一支影片一列，Power BI 會自動偵測成**一對一**，
而一對一關聯的交叉篩選方向**預設雙向且不能關閉**。那條雙向關聯會造出路徑
`dim_日期 → fact_影片 → dim_影片 → fact_每日快照`，與 `dim_日期 → fact_每日快照`
這條直達路徑打架，Power BI 會停用其中一條。

👉 `模型` → 雙擊該關聯 → 基數選 `多對一 (*:1)`、交叉篩選方向選 `單一`。
Power BI 允許這樣設（唯一性只是資料現況，不是宣告的限制）。

**其餘設定**

- `dim_日期` 要 `標記為日期資料表`（日期資料行選 `日期`）
- `門檻`、`資料範圍`：🔴 **千萬不要建關聯**。它們必須是孤立的表，
  只靠 `SELECTEDVALUE` 讀取選取值——建了關聯，累加門檻的邏輯就失效了
- `資料特徵`（data_notes）、`報告限制`（limitations）、`資料品質`：不建關聯

---

## 六、Power BI 載入步驟

### 6-1 要載入的檔案

`常用` → `取得資料` → `Web`，逐一貼入下列網址（**現行使用 GitHub 版**）：

```
https://raw.githubusercontent.com/joker8604169-cloud/joeman-youtube-analytics/main/data/processed/star/fact_video.csv
https://raw.githubusercontent.com/joker8604169-cloud/joeman-youtube-analytics/main/data/processed/star/dim_date.csv
https://raw.githubusercontent.com/joker8604169-cloud/joeman-youtube-analytics/main/data/processed/star/dim_video.csv
https://raw.githubusercontent.com/joker8604169-cloud/joeman-youtube-analytics/main/data/processed/star/dim_format.csv
https://raw.githubusercontent.com/joker8604169-cloud/joeman-youtube-analytics/main/data/processed/star/dim_category.csv
https://raw.githubusercontent.com/joker8604169-cloud/joeman-youtube-analytics/main/data/processed/star/dim_series.csv
https://raw.githubusercontent.com/joker8604169-cloud/joeman-youtube-analytics/main/data/processed/star/dim_sponsor.csv
https://raw.githubusercontent.com/joker8604169-cloud/joeman-youtube-analytics/main/data/processed/data_notes.csv
https://raw.githubusercontent.com/joker8604169-cloud/joeman-youtube-analytics/main/data/processed/limitations.csv
```

選用：`star/fact_daily.csv`（每日快照）、`star/data_quality.csv`（資料品質）。

📌 **檔名刻意全部用英文**，網址不含中文字元，不必處理 URL 編碼。

**建置期用本機比較快**：`取得資料` → `文字/CSV` → `data\processed\star\*.csv`，
之後在 `進階編輯器` 把 `File.Contents("本機路徑")` 換成 `Web.Contents("網址")`。

🔴 **兩個沿用舊文件的陷阱，一個都沒消失：**

1. **括號數量不要動。** `Csv.Document(File.Contents("..."),[選項])` 換成
   `Csv.Document(Web.Contents("..."),[選項])`——網址後面是 **`")`** 一個括號。
   多打一個變成 `"))` 會把 `Csv.Document` 提前關掉，報 `Expression.SyntaxError`。
2. **`QuoteStyle` 要是 `QuoteStyle.Csv` 不是 `QuoteStyle.None`。**
   換來源會讓 Power BI 產生預設的 `None`，那個設定叫它無視雙引號、在換行處硬切成新的一列。
   ⚠️ 星狀表的文字欄位已在 `build_star.py` 清掉換行，理論上碰不到；
   **但 `dim_影片[影片標題]` 是最長的文字欄，還是照設，這是第二道防線。**

`關閉並套用` 後第一次會跳驗證視窗：選 **`匿名`**、層級 **`https://raw.githubusercontent.com`**。

### 6-2 資料表更名（載入後第一件事）

Power Query 會用檔名當查詢名稱。**在 Power Query 裡就改好**，
不要載入後才在模型檢視改（改了關聯與量值裡的表名不會自動跟著更新）。

| 查詢名稱 | 改成 |
|---|---|
| `fact_video` | `fact_影片` |
| `fact_daily` | `fact_每日快照` |
| `dim_date` | `dim_日期` |
| `dim_video` | `dim_影片` |
| `dim_format` | `dim_影片格式` |
| `dim_category` | `dim_類型` |
| `dim_series` | `dim_系列` |
| `dim_sponsor` | `dim_業配判定` |
| `data_notes` | `資料特徵` |
| `limitations` | `報告限制` |
| `data_quality` | `資料品質` |

### 6-3 型別檢查

`build_star.py` 已經把日期輸出成 `yyyy-MM-dd`、布林輸出成 `True`/`False`，
Power BI 多半會自動判對。**匯入後逐一確認這幾欄**：

| 資料表 | 欄位 | 應為 |
|---|---|---|
| `fact_影片` | `發布日期` | **日期** |
| `dim_日期` | `日期` | **日期** |
| `dim_系列` | `系列首播日` | **日期** |
| `dim_系列` | `有系列` | **True/False** |
| `dim_影片` | `是首集` | **True/False** |
| `dim_業配判定` | `是否業配` | **True/False** |
| `dim_影片格式` | `納入商業分析` | **True/False** |
| `dim_影片格式` | `業配召回率` | **小數** |

⚠️ **中文欄名匯入後若出現亂碼**，把「檔案原點」改成 `65001: Unicode (UTF-8)`。
所有星狀 CSV 都是 UTF-8 with BOM，正常情況不會發生。

⚠️ `資料特徵` 與 `報告限制` 各有一個**索引資料行**步驟（供表格排序用），**不要刪**。

### 6-4 依資料行排序（四處）

| 資料表 | 欄位 | 依 |
|---|---|---|
| `dim_影片格式` | `影片格式` | `格式排序` |
| `dim_類型` | `影片類型` | `類型排序` |
| `門檻` | `門檻標籤` | `排序` |
| `資料範圍` | `範圍` | `排序` |

### 6-5 建立中斷連結表

照 `DAX.md` 第 2-1、2-2 節原樣建立 `門檻` 與 `資料範圍` 兩張 `DATATABLE`，
**內容完全不變、一樣不建關聯**。

### 6-6 建立量值

照 `DAX_星狀模型.md` 第四節逐一貼上。**不需要建立任何計算欄位**（第三節說明原因）。

---

## 七、執行與自動化

### 本機

```bash
python -m src.fetch        # 抓 API（需 .env 的金鑰）
python -m src.transform    # 清洗、分類、輸出 videos.csv
python -m src.build_star   # 資料清洗 ＋ 星狀重建
python -m src.verify_star  # 驗收：確認新舊模型等價
```

⚠️ **本機 `data/raw` 會過期。** 沒有先跑 `fetch` 就跑 `transform` 會用舊的 JSON
重建 `videos.csv`，資料等於回滾（`HANDOFF.md` 十三-10）。
只改建模層時**只跑 `build_star` 與 `verify_star`**，不要碰 `transform`。

### GitHub Actions

`.github/workflows/daily-update.yml` 已加入兩個步驟，接在「清洗與分類」之後：

```yaml
      - name: 建立星狀模型
        run: python -m src.build_star

      - name: 驗證星狀模型與舊模型等價
        run: python -m src.verify_star
```

🔴 **`verify_star` 失敗會讓整個工作流程失敗、不會提交。**
這是刻意的——寧可停在舊資料，也不要推一份數字對不上的模型上去。

---

## 八、維護指引

### 8-1 重做抽樣驗證之後要改哪裡

```
rules/format_audit.csv     ← 改這裡（模型的唯一來源）
src/transform.py 的 AUDIT  ← 也要改（它負責產生 data_notes.csv 與 limitations.csv）
```

⚠️ 這兩處目前是**手動同步**。改完跑一次 `python -m src.build_star`，
`dim_影片格式` 會跟著更新，DAX 一行都不用動。

### 8-2 《對決》若復播

改 `src/build_star.py` 的 `DUEL_END_DATE`，重跑 `build_star`。
`dim_系列[系列世代]` 會重算，所有相關量值自動跟著變。

### 8-3 頻道開了新系列／新類型

不用做任何事——維度表由資料自動產生，新成員會自己出現。
但**分類規則不會自己成長**，記得看 `review_queue.csv`（每週一次即可）。

### 8-4 改了模型之後一定要跑

```bash
python -m src.verify_star
```

它會逐列比對星狀模型與 `videos.csv`、逐項比對 30+ 個指標，
並靜態檢查 `DAX_星狀模型.md` 裡每一個 `'資料表'[資料行]` 是否真的存在。
**任一項不符即以非零狀態碼結束。**
