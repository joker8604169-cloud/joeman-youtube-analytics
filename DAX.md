# DAX 量值與報表實作說明

> 🔴 **2026-08-10：模型已改為星狀結構。本文件的第二節（前置設定）與第三節（量值清單）
> 已被 `DAX_星狀模型.md` 取代**——那邊的量值改寫成引用事實表／維度表，
> 並附舊→新對照總表、44 個量值的逐項驗證結果，以及量值使用狀態盤點（哪些是孤兒、哪些該刪）。
>
> **本文件其餘章節仍然有效且未被取代**：第四節（每頁的圖表與它要說的話）、
> 第五節（格式設定）、第六節（互動設定）、第七節（容易踩的坑）、
> 第八節（實作順序）、第九節（簡報執行與 Q&A 備答）。
> 量值名稱一個都沒改，所以格式設定與互動設定原樣沿用。
>
> 模型結構與資料清洗規則見 `STAR_SCHEMA.md`。

> 搭配 `HANDOFF.md` 使用。本文件記錄**每個量值為什麼這樣寫**，
> 以及它服務的是報表中的哪一句話。
> 最後更新：2026-08-01
>
> ⚠️ **文中數字為撰寫當日快照，資料每日自動更新，引用前請重新查詢。**
> 人工抽樣得到的精確率、召回率則為固定值，不會變動。

---

## 目錄

1. [設計原則](#一設計原則)
2. [前置設定](#二前置設定必做) — **含資料載入步驟**
3. [量值清單與設計邏輯](#三量值清單與設計邏輯)
4. [每頁的圖表與它要說的話](#四每頁的圖表與它要說的話)
5. [格式設定](#五格式設定)
6. [互動設定](#六互動設定編輯互動)
7. [容易踩的坑](#七容易踩的坑)
8. [實作順序建議](#八實作順序建議)
9. [簡報執行](#九簡報執行) — **時程、開場、結論、當天提醒**

---

## 一、設計原則

### 1. 所有量值都受「觀看數門檻」影響
門檻是跨頁的分析工具（看爆款的組成）。因此**基礎量值一律內建門檻篩選**，
上層量值再從基礎量值衍生，避免每個量值都重寫一次篩選邏輯。

### 2. 商業分析一律限長片
Shorts 的業配偵測召回率僅 14.6%，數字不可用。
凡是與業配有關的量值，**都明確加上 `'videos'[video_format] = "長片"`**。

### 3. 統一用中位數，不用平均
平均值易受爆紅影片拉高。全報表一致使用 `MEDIAN`，
避免被質疑「為什麼這裡用平均那裡用中位」。

### 4. 人工驗證得到的常數寫死在量值裡
精確率 22/22、召回率 76.3% 是人工抽樣的結果，程式算不出來。
這些數字寫成常數並加註來源，日後重做抽樣只要改一處。

---

## 二、前置設定（必做）

### 2-0 建立檔案與載入資料

> ⚠️ 先前的 `Joeman分析.pbix` 是測試檔已刪除，**請從頭重新製作**。

**① 建立並存檔**
開啟 Power BI Desktop → 關閉歡迎畫面 → `檔案` → `另存新檔`
存到 `C:\Users\Ace\Desktop\結訓專題\`，檔名自訂。
**先存檔的好處**：每一步都能 `Ctrl+S`，出錯可退回。

**② 載入資料**

### 🔵 兩種連線方式，**目前使用的是 GitHub 版**（2026-08-03 切換）

| | 本機路徑 | **GitHub 網址（現行）** |
|---|---|---|
| 重新整理速度 | 快、不需網路 | 需網路，慢一點 |
| 拿到的資料 | 你電腦上那份——**沒 `git pull` 就是舊的** | 永遠是最新發布的 |
| 換一台電腦開檔 | **路徑失效，重新整理會失敗** | 正常運作 |
| 對評審的說服力 | 看不出自動化 | 報表直接讀取每日自動更新的儲存庫 |

⚠️ **Power BI Desktop 會把資料存進 `.pbix`**。重新整理過後就算完全離線也照樣顯示——
**簡報當下不需要網路**，連線方式只影響你按「重新整理」的那一刻。

⚠️ `raw.githubusercontent.com` 有約 **5 分鐘的 CDN 快取**，Actions 跑完立刻重新整理
可能還是拿到舊的，等幾分鐘再按一次。

**建置期用本機比較快，切換到 GitHub 建議排練前完成**，留時間處理切換時的問題。

---

**建置期（本機）**：`常用` → `取得資料` → `文字/CSV` → 選 `data\processed\videos.csv`
預覽時確認中文沒有亂碼（亂碼就把「檔案原點」改成 `65001: Unicode (UTF-8)`），
然後按「**轉換資料**」——**不要按「載入」**，有欄位型別要修正。

**切換到 GitHub**：`轉換資料` → 選該查詢 → `常用` → **`進階編輯器`**，
把 `File.Contents("本機路徑")` 換成 `Web.Contents("網址")`，
並把 `QuoteStyle=QuoteStyle.None` 改成 **`QuoteStyle=QuoteStyle.Csv`**。

```
videos.csv      https://raw.githubusercontent.com/joker8604169-cloud/joeman-youtube-analytics/main/data/processed/videos.csv
data_notes.csv  https://raw.githubusercontent.com/joker8604169-cloud/joeman-youtube-analytics/main/data/processed/data_notes.csv
limitations.csv https://raw.githubusercontent.com/joker8604169-cloud/joeman-youtube-analytics/main/data/processed/limitations.csv
```

🔴 **括號數量不要動。** `Csv.Document(File.Contents("..."),[選項])` 換成
`Csv.Document(Web.Contents("..."),[選項])`——`videos.csv"` 後面是 **`")`** 一個括號。
實測時多打一個變成 `"))`，會把 `Csv.Document` 提前關掉，報
`Expression.SyntaxError: 必須是 Token Identifier`。

🔴 **`QuoteStyle.Csv` 是第二道防線。** 換來源會讓 Power BI 產生預設的 `QuoteStyle.None`，
那個設定叫它無視雙引號、在換行處硬切成新的一列——症狀是 `video_id` 出現標題碎片、
`duration_seconds` 變成 Error。詳見 `HANDOFF.md` 第十三章第 8 點。

⚠️ `data_notes` 與 `limitations` 各有一個**索引資料行**步驟（供表格排序用），**不要刪**。

`關閉並套用` 後第一次會跳驗證視窗：選 **`匿名`**、層級 **`https://raw.githubusercontent.com`**、
按 `連線`（儲存庫是公開的，不需要帳號）。

**③ 在 Power Query 修正型別（只做這兩件事）**

| 欄位 | 改成 |
|---|---|
| `is_sponsored`、`ppd_api_flag`、`is_self_promo` | **True/False** |
| `published_date` | **日期** |

改法：點欄位名稱**左側的型別圖示** → 選對應型別。

**④ 空值不要處理** ⚠️

`like_count`（4 支）與 `comment_count`（1 支）有空值，**不要取代為 0**。

那些空值代表「**創作者關閉了按讚顯示／留言功能**」，不是「零個讚」。
補成 0 會製造假資料。而 Power BI 的 SUM／AVERAGE 會自動略過空值，
除法遇到空值回傳空白而非錯誤，**完全不需要處理**。

同理 `series`、`playlists`、`sponsor_hits`、`self_promo_products` 的大量空白也是正常的，
代表該影片沒有系列歸屬／沒命中任何規則。

**⑤ 套用**
左上角 `關閉並套用`。載入後應為 **3,500 列上下**（每天會增加）。

**⑥ 其他兩張表**
用同樣方式載入（這兩張不需要改型別，但各需要一個索引資料行供排序）：
```
data_notes.csv      → 第 2 頁表格用
limitations.csv     → 第 7 頁表格用
daily_stats.csv     → 目前用不到，未載入
```
📌 **目前模型只有三張匯入表**（`videos`／`data_notes`／`limitations`）
＋ 兩張中斷連結表（`門檻`／`資料範圍`）。`daily_stats` 若日後要做
`BUILD.md` 第 8-5 節的成長率工具提示才需要載入。

**⑦ 設定畫布與背景（每一頁都要做）**

1. 在空白處點一下（不選取任何物件）
2. 右側`視覺效果`窗格 → **滾筒刷圖示**（格式化頁面）
3. `畫布設定` → 類型選 **16:9** → 手動改為 **寬 1920、高 1080**
4. `畫布背景` → `瀏覽` → 選 `assets\bg_page{N}.png`
5. **影像調整選「符合」、透明度拉到 0%**

⚠️ 透明度預設是 90%，不改的話背景會變得很淡。

---

### 2-1 門檻資料表（**不建關聯**）

`模型` → `新增資料表`：

```dax
門檻 =
DATATABLE (
    "門檻標籤", STRING,
    "門檻值", INTEGER,
    "排序", INTEGER,
    {
        { "全部", 0, 1 },
        { "10 萬", 100000, 2 },
        { "50 萬", 500000, 3 },
        { "100 萬", 1000000, 4 },
        { "200 萬", 2000000, 5 }
    }
)
```

建立後：選 `門檻標籤` 欄 → `資料行工具` → `依資料行排序` → 選 `排序`

**為什麼要中斷連結表？**
「超過 10 萬」是**累加門檻**，不是分類值。若用 `view_count` 直接切片，
Power BI 只能做「等於某值」或範圍滑桿，做不出五顆一鍵切換的按鈕。
中斷連結表 ＋ 量值判讀選取值，才能實現一鍵累加篩選。

### 2-2 資料範圍資料表（**不建關聯**）

```dax
資料範圍 =
DATATABLE (
    "範圍", STRING,
    "排序", INTEGER,
    {
        { "全部（含偵測失效的 Shorts）", 1 },
        { "僅長片（有效範圍）", 2 }
    }
)
```

同樣設定 `範圍` 依 `排序` 排序。

**選項名稱刻意寫長**——讓觀眾一眼知道哪個可信，不需要口頭解釋。

### 2-3 計算欄位：系列世代（在 `videos` 表）　🔴 2026-08-02 改用《對決》停播日

```dax
系列世代 =
VAR s = 'videos'[series]
VAR duelEnd = DATE ( 2025, 11, 10 )
VAR firstPub =
    CALCULATE (
        MIN ( 'videos'[published_date] ),
        ALLEXCEPT ( 'videos', 'videos'[series] ),
        'videos'[video_format] = "長片"
    )
RETURN
    IF (
        ISBLANK ( s ) || s = "",
        "無系列",
        IF ( firstPub > duelEnd, "新系列", "既有系列" )
    )
```

**`2025-11-10` 是《Joe是要對決》最後一集**（第 307 集），標題就是
「是時候該放手了⋯《Joe是要對決》停播原因跟心路歷程」。

**為什麼從「曆年 ≥2025」改成停播日**
原規則會把《工作室日常廢片》算成新系列，但它首播 2025-08-27，**比停播早 75 天**，
不符合第 6 頁「招牌停播後的接棒」的宗旨。改用停播日之後定義與論點完全對齊，
被問「你怎麼定義新系列」可以直接回答「**《對決》最後一集之後開播的**」。

四個新系列與停播日的距離：

| 系列 | 集數 | 首播 | 相對停播 |
|---|---|---|---|
| 我獨自穿越日本 | 10 | 2025-11-19 | **後 9 天** |
| 九讀 | 3 | 2026-01-28 | 後 79 天 |
| Joe是要跟團 | 3 | 2026-05-26 | 後 197 天 |
| 公車食刻表 | 3 | 2026-05-29 | 後 200 天 |
| ~~工作室日常廢片~~ | 7 | 2025-08-27 | **前 75 天** ← 已排除 |

⚠️ **刻意寫死日期，不用 `MAX(published_date)` 動態抓。**
動態寫法在《對決》若復播時界線會往後跳，**現有的新系列會全部被重新歸類且不報錯**。
代價是復播後要手動改這一行。

⚠️ `|| s = ""` 不能省。`series` 的空白值匯入後可能是 null 也可能是空字串，
`ISBLANK` 只抓得到前者。詳見第七節第 7 點。

**驗收值**（全表 3,502）：無系列 **2,372**、既有系列 **1,109**、新系列 **21**
（長片 19 支 ＋ Shorts 2 支）。

### 2-4 計算欄位：是首集（在 `videos` 表）　2026-08-02 新增

```dax
是首集 =
VAR s = 'videos'[series]
VAR firstPub =
    CALCULATE (
        MIN ( 'videos'[published_date] ),
        ALLEXCEPT ( 'videos', 'videos'[series] ),
        'videos'[video_format] = "長片"
    )
RETURN
    NOT ISBLANK ( s )
        && s <> ""
        && 'videos'[video_format] = "長片"
        && 'videos'[published_date] = firstPub
```

標記每個系列的第一支長片，供第 6 頁 KPI4 使用。
有了它，`[首集就有業配]` 只是簡單計數，不必寫巢狀迭代。

⚠️ 首集同日發多支會標記多列——實測只有《Joeman Vlog》（3 支同天），
它從無業配、不出現在任何視覺上，不影響。

---

## 三、量值清單與設計邏輯

### ▍基礎層

```dax
選定門檻 = SELECTEDVALUE ( '門檻'[門檻值], 0 )
```
> 讀取門檻切片器的選取值。未選取時回傳 0（等於不過濾）。

```dax
僅長片 =
CALCULATE ( COUNTROWS ( 'videos' ), 'videos'[video_format] = "長片" )
```
> 輔助量值，供「業配率最高類型」判斷樣本量是否足夠。

```dax
影片數 =
VAR th = [選定門檻]
RETURN CALCULATE ( COUNTROWS ( 'videos' ), KEEPFILTERS ( 'videos'[view_count] >= th ) )
```
> **所有計數的基礎。** `KEEPFILTERS` 是關鍵——它保留視覺物件本身的篩選
> （例如年份、類型），只再疊加門檻條件。若省略會覆蓋掉原有篩選。

```dax
總觀看 =
VAR th = [選定門檻]
RETURN CALCULATE ( SUM ( 'videos'[view_count] ), KEEPFILTERS ( 'videos'[view_count] >= th ) )
```

```dax
中位觀看 =
VAR th = [選定門檻]
RETURN CALCULATE ( MEDIAN ( 'videos'[view_count] ), KEEPFILTERS ( 'videos'[view_count] >= th ) )
```
> 全報表的觀看指標一律用這個，不用平均。

```dax
業配影片數 =
VAR th = [選定門檻]
RETURN
    CALCULATE (
        COUNTROWS ( 'videos' ),
        'videos'[is_sponsored] = TRUE (),
        KEEPFILTERS ( 'videos'[view_count] >= th )
    )
```

```dax
業配率 = DIVIDE ( [業配影片數] + 0, [影片數] )
```
> 用 `DIVIDE` 而非 `/`，分母為零時回傳空白而不是錯誤。
>
> 🔴 **`+ 0` 不能省（2026-08-03 實測）**：業配數為 0 的年份，`COUNTROWS` 回傳
> **BLANK 不是 0**，`DIVIDE ( BLANK, n )` 也是 BLANK，**整個年份會被圖表丟掉**。
> 症狀：第 5 頁側上圖的 2010／2011／2013／2014 四根柱子憑空消失，X 軸從 2012 開始。
> `+ 0` 把 BLANK 強制成 0；分母仍為 0 時（該年完全沒有影片）`DIVIDE` 照樣回傳 BLANK，
> **所以沒有影片的年份不會被硬塞進來**。
>
> ⚠️ 這是本專案第二次踩到同一個陷阱，第一次是 `長片系列化比例`（第 6 頁 2010 消失）。
> **通則：任何「計數 ÷ 計數」的比率量值，分子都要加 `+ 0`。**
>
> ⚠️ 這個修正會連帶讓第 4 頁主圖的紅線在那四年恢復連續（原本會斷開四個缺口）。

```dax
長片數 = CALCULATE ( [影片數], 'videos'[video_format] = "長片" )
```

```dax
Shorts 數 = CALCULATE ( [影片數], 'videos'[video_format] = "Shorts" )
```

---

### ▍第 1 頁 封面

```dax
動態資料規模 =
"分析母體 " & FORMAT ( COUNTROWS ( 'videos' ), "#,0" ) & " 支影片｜"
    & MIN ( 'videos'[published_year] ) & "–" & MAX ( 'videos'[published_year] )
    & "｜每日自動更新"
```

**為什麼要動態？**
副標題「解構一個頻道的產能結構、廠商投放與 IP 接棒」印在背景 PNG 裡，
**刻意不含任何數字**——因為資料每天更新，寫死的「3,502 支」幾天後就過期。
規模數字改由此量值呈現，資料一更新就跟著變。

擺放位置：X=140, Y=740, 寬 780, 高 46，字級 24px，`#717171`

---

### ▍第 3 頁 產能結構

```dax
長片中位觀看 = CALCULATE ( [中位觀看], 'videos'[video_format] = "長片" )
```

```dax
可比性 =
IF ( MAX ( 'videos'[published_year] ) <= 2023, "可比", "仍在累積" )
```
> **供條件格式使用。** 2024 年後的影片仍在累積觀看，跨年比較會失真。
> 用這個量值把 2024–2026 的資料點改成灰色虛線，讓不可比範圍**視覺上就能辨識**，
> 不必等口頭解釋。
>
> 判準來源：兩個快照實測的 8 日成長中位數
> （2023 年 188 次、2024 年 577 次、2025 年 2,785 次）。

```dax
流量貢獻度 =
VAR th = [選定門檻]
VAR Filtered = CALCULATE ( SUM ( 'videos'[view_count] ), KEEPFILTERS ( 'videos'[view_count] >= th ) )
VAR Total = CALCULATE ( SUM ( 'videos'[view_count] ), ALL ( 'videos'[view_count] ) )
RETURN DIVIDE ( Filtered, Total )
```
> 🔴 **`VAR` 的名稱只能用 ASCII**（英文字母、數字、底線），不能用中文。
> 原版寫成 `VAR 篩選後` / `VAR 全部`，貼進 Power BI 會回報
> 「剖析時發生下列語法錯誤：權杖無效」。詳見第七節第 6 點。
> 點「100 萬↑」時顯示 46.0%，代表**一成影片撐起近半流量**。
> `ALL('videos'[view_count])` 只移除門檻篩選，保留其他篩選（如年份）。

---

### ▍第 4 頁 投放規模

```dax
業配率（依資料範圍） =
VAR sel = SELECTEDVALUE ( '資料範圍'[範圍], "僅長片（有效範圍）" )
RETURN
    IF (
        sel = "僅長片（有效範圍）",
        CALCULATE ( [業配率], 'videos'[video_format] = "長片" ),
        [業配率]
    )
```
> **這是全報表最重要的一個互動。**
> 切到「全部」時業配率從 53.0% 掉到 30.4%，一次點擊展示**指標被 Shorts 污染**。
> 預設值設為「僅長片」，確保未選取時顯示的是有效數字。

```dax
業配影片數（依資料範圍） =
VAR sel = SELECTEDVALUE ( '資料範圍'[範圍], "僅長片（有效範圍）" )
RETURN
    IF (
        sel = "僅長片（有效範圍）",
        CALCULATE ( [業配影片數], 'videos'[video_format] = "長片" ),
        [業配影片數]
    )
```

```dax
偵測業配數_長片 = CALCULATE ( [業配影片數], 'videos'[video_format] = "長片" )
```
> **KPI2 用這個，不是 `[業配影片數（依資料範圍）]`（2026-08-05 改）。**
> 原本 KPI2 跟著資料範圍浮動：切「全部」時變成 397（長片 370 ＋ Shorts 27），
> 與 KPI3 的 434（純長片回推）並排會被讀成「偵測 397、推估 434，漏 37 支」——
> **那個 37 是假的**，長片真正漏抓 64 支，Shorts 的漏抓量級完全沒被表達。
>
> **KPI2 與 KPI3 是一對（下限 vs 推估），必須同母體**，而 KPI3 只能是長片
> （Shorts 召回率 14.6%，套上去回推出來的數字荒謬）。所以 KPI2 也鎖長片。
>
> 鎖住之後四格只有 KPI1 隨切換變動，而那正是這頁要演的東西。台詞也更利：
> 「業配率掉到 34%——但第二格**偵測到的業配數一支都沒變**，變的是分母。」

```dax
推估實際業配 = CALCULATE ( [推估實際業配_依格式], 'videos'[video_format] = "長片" )
```
> 🔴 **改成引用 `[推估實際業配_依格式]`，不要在這裡再寫一次常數（2026-08-05 實測踩過）。**
> 原本兩處各寫一次召回率，改了側上圖那條、KPI3 卻還停在舊值——
> 畫面上側上圖總長 674、KPI3 卻顯示 603，**同一頁兩個數字對不上**。
> 引用之後召回率只存在 `格式召回率` 一個地方，結構上不可能再漂開。
> 🔴 **常數於 2026-08-05 由 `0.853` 改為 `0.763`。**
> 舊值來自 `sponsor_recall_sample.csv`，那份抽樣**其實是 25 Shorts ＋ 14 長片的混合樣本**，
> 而 `sponsor_evaluate` 不帶參數時也不篩格式——85.3% 從來就不是長片的數字。
>
> **0.763 是重新抽 40 支純長片實測的召回率**（漏抓率 14/40＝35.0%，
> 召回率 95% 信賴區間 69.1–83.6%）。詳見 `HANDOFF.md` 第八章。
> 偵測值 ÷ 召回率 ＝ 推估實際總數，全期 513 ÷ 0.763 ＝ **672**。
>
> ⚠️ 召回率量在**近三年（2023 年起）**，套到全期是既有做法的延續。
> ⚠️ 若日後重做抽樣，這個常數與下方 `格式召回率` 要一起改。

### ▍第 4 頁側上圖：偵測缺口（2026-08-05 新增）

```dax
格式召回率 =
SWITCH (
    SELECTEDVALUE ( 'videos'[video_format] ),
    "長片", 0.763,
    "Shorts", 0.146,
    BLANK ()
)
```
> 兩個都是 40 支純隨機抽樣、人工核對出來的實測值，各自對應自己的格式。
> **不可互換套用**——長片與 Shorts 的可用文字量差十倍（Shorts 有 85.7% 說明欄空白）。

```dax
推估實際業配_依格式 =
VAR r = [格式召回率]
RETURN IF ( NOT ISBLANK ( r ), ROUND ( DIVIDE ( [業配影片數], r ), 0 ) )
```

```dax
沒測到的業配數 = [推估實際業配_依格式] - [業配影片數]
```
> 堆疊橫條圖的第二段。`[業配影片數]` 是測到的、這個是沒測到的，
> 兩段加起來就是推估實際總數。

```dax
判定精確率_長片 = "22 / 22"
```
> 🔴 **2026-08-05 改**。舊版 `判定精確率 = 0.968` 的 30/31 是**混合格式**樣本
> （27 長片 ＋ 8 Shorts）。拆出長片專屬是 **22/22**，95% 信賴區間 **85.1–100%**。
>
> **回傳文字「22 / 22」而不是 100%**：分母只有 22 支，寫「100%」會被讀成
> 「這套判定不會出錯」，而區間下界其實只有 85.1%。把分母攤在檯面上，
> 與第 6 頁 KPI4 顯示「4 / 4」而不是「100%」是同一條原則。
>
> 文字量值不用設格式。舊的 `判定精確率` 若無其他視覺引用可刪除。

### ▍~~業配 vs 非業配的觀看對照~~　❌ 2026-08-05 整組移除

`業配中位觀看`、`非業配中位觀看`、`觀看差距` 三個量值連同它們服務的視覺一併移除。

**理由**：那組量值的唯一用途是比較業配與非業配影片的觀看數，而依 `HANDOFF.md`
**第零章**，「業配是否帶來流量」不成立、也不需要被討論或反駁——**提出這個假說本身
就會顯得奇怪**。第 4 頁側上圖已換題為「業配偵測缺口」，見下方與 `BUILD.md`。

模型裡若已建過這三個量值，**可以留著不刪**（沒有視覺引用，不影響任何數字），
但不要再用它們設計任何圖表或講稿。

---

### ▍第 5 頁 投放偏好

```dax
類型業配率 = CALCULATE ( [業配率], 'videos'[video_format] = "長片" )
```

```dax
業配率最高類型 =
VAR t =
    ADDCOLUMNS (
        FILTER ( VALUES ( 'videos'[category] ), CALCULATE ( [僅長片] ) >= 20 ),
        "@率", [類型業配率]
    )
RETURN MAXX ( TOPN ( 1, t, [@率] ), 'videos'[category] )
```
> **`>= 20` 是刻意設的樣本門檻。**
> 若不設，財經／投資（僅 **6** 支長片）可能因為比例偶然偏高或偏低而登上 KPI，
> 造成誤導。設 20 支之後，目前結果是「挑戰／企劃 **45.7%**」。
>
> ⚠️ 改動這個數字會讓 KPI 卡顯示的類型名稱變動，與簡報講稿不一致。
> 全期長片共 14 個類型，門檻 20 目前只擋掉財經／投資一個。

```dax
業配率最低類型 =
VAR t =
    ADDCOLUMNS (
        FILTER (
            VALUES ( 'videos'[category] ),
            CALCULATE ( [僅長片] ) >= 20 && [樣本數_長片] > 0
        ),
        "@率", [類型業配率],
        "@樣本", [樣本數_長片]
    )
RETURN MINX ( TOPN ( 1, t, [@率], ASC, [@樣本], DESC ), 'videos'[category] )
```
> **背景圖 `bg_page5.png` 的 KPI2 印死的標籤是「業配率最低類型」，需要的是類型名稱**，
> 不是比率。與 `業配率最高類型` 同一套寫法，只把 `TOPN` 改為 `ASC` 取末位。
> 門檻同樣設 `>= 20`，理由同上。
>
> 🔴 **`&& [樣本數_長片] > 0` 不能省（2026-08-06 實測）**：`[僅長片]` **不含觀看數門檻**
> （這是刻意的，見 `系列長片數_不受門檻` 的判準——「誰上場」不能跟著門檻變），
> 所以門檻拉高時會有類型**通過 `>= 20` 閘門、卻一支影片都不剩**。
> 那種類型的 `[類型業配率]` 是 **BLANK**，而 `TOPN ( ..., ASC )` 把 BLANK 當成比 0 還小，
> **它會被選成「最低」**。
>
> 症狀：門檻點「100 萬」時 KPI2 顯示 **電競賽事**——但電競賽事全期 446 支長片裡
> **破百萬的是 0 支**。那格顯示的不是「業配率最低」，是「**沒有資料**」。
>
> 用 `[樣本數_長片]` 而不是 `[影片數]`：`[類型業配率]` 只算長片，判斷有無樣本的尺
> 必須跟它同格式。目前兩者結果相同（沒有任何類型是「長片 0 支但 Shorts 有」），
> 但格式一致才不會在資料變動後悄悄失準。
>
> ⚠️ `業配率最高類型` **不需要**這個修正：BLANK 排在最小端，`MAXX`／`TOPN` 取最大時
> 永遠選不到它。若為了兩個量值寫法對稱而一起加，**不會改變任何數值**，是零風險的。
> `最低類型業配率` 也不需要——`MINX` 本來就會忽略 BLANK，`類型間差距` 的 44.9pp 是對的。
>
> 🔴 **`[@樣本], DESC` 這組並列規則不能省（2026-08-06 實測）**：加了 `> 0` 之後，
> 100 萬門檻下有**三個類型並列 0.0%**（玩具/收藏 17 支、日常Vlog 4 支、遊戲實況 1 支），
> `TOPN` 只給一組排序條件時**取誰是任意的**——實測 Power BI 挑了樣本只有 4 支的日常Vlog。
>
> 三個都是真的 0.0%，所以 KPI **不會錯**，錯的是**它會變**：資料每天更新，
> 排練時看到「日常Vlog」、正式上台可能變成「玩具/收藏」，**講稿會對不上畫面**。
>
> `TOPN` 支援多組 `orderBy`，第二組設成「樣本數由多到少」之後，並列時固定取
> **證據最紮實的那一個**。門檻「全部」時遊戲實況是唯一最低，這組條件不會被觸發。
>
> | 門檻 | 只有一組排序 | 加上並列規則 |
> |---|---|---|
> | 全部 | 遊戲實況 0.3% | **遊戲實況 0.3%**（不變） |
> | 100 萬 | 日常Vlog（4 支，會變動） | **玩具/收藏（17 支，固定）** |
> | 200 萬 | 任意 | **人物/訪談（14 支，固定）** |
>
> ⚠️ **門檻 200 萬時有七個類型並列 0.0%**（人物/訪談、美食、玩具/收藏、寵物、未分類、
> 日常Vlog、遊戲實況），這張卡在該門檻下已無資訊量——不是壞掉，是爆款太少、多數類型掛零。
> `HANDOFF.md` 本來就規定**門檻演出到 100 萬為止**，照做就不會碰到。
>
> ⚠️ 門檻「全部」時顯示 **遊戲實況 0.3%**——這是**年代效應不是廠商偏好**（該類型 90.7% 集中在
> 2015–2016 年，當時業配文化尚未成形）。**這是預期行為，但必須在台上主動解釋**，
> 詳見第四節第 5 頁的資料陷阱說明。

```dax
最高類型業配率 =
VAR t =
    ADDCOLUMNS (
        FILTER ( VALUES ( 'videos'[category] ), CALCULATE ( [僅長片] ) >= 20 ),
        "@率", [類型業配率]
    )
RETURN MAXX ( t, [@率] )
```

```dax
最低類型業配率 =
VAR t =
    ADDCOLUMNS (
        FILTER ( VALUES ( 'videos'[category] ), CALCULATE ( [僅長片] ) >= 20 ),
        "@率", [類型業配率]
    )
RETURN MINX ( t, [@率] )
```
> 上面這兩個回傳的是**比率**，KPI 卡不直接用，它們是 `類型間差距` 的中間量。

```dax
類型間差距 =
FORMAT ( ( [最高類型業配率] - [最低類型業配率] ) * 100, "0.0" ) & "pp"
```
> 背景圖 KPI3 的標籤。目前值 **45.0pp**（挑戰／企劃 45.7% − 遊戲實況 0.7%）。
>
> 🔴 **必須顯示 `pp` 不是 `%`（2026-08-03 修正）**：兩個百分比相減得到的是**百分點差**，
> 顯示成「45.0%」會被讀成「差了 45%」，那是另一件事。原版寫 `[最高] - [最低]` 再套
> 百分比格式，畫面上就是 45.0%。
>
> 🔴 **回傳文字是因為這個版本的 Power BI「格式」下拉沒有「自訂」選項**，
> 無法用自訂格式字串 `0.0"pp"` 附加單位，只能用 `FORMAT()` 自己組字串。
> 代價是變成文字、不能進圖表軸——但它本來就只服務 KPI3，沒有其他引用點。
>
> ⚠️ 這個數字內含年代效應——它比較的是「近年的挑戰企劃」與「2015 年的遊戲實況」。
> 台上要嘛把它講成「投放集中度」，要嘛在說明年代效應時一併帶過，
> **不要單獨把它講成「廠商偏好的差距」**。

```dax
樣本數_長片 = CALCULATE ( [影片數], 'videos'[video_format] = "長片" )
```
> 目前值 **3,024**（全期長片）。

```dax
系列長片數_不受門檻 =
CALCULATE ( COUNTROWS ( 'videos' ), 'videos'[video_format] = "長片" )
```
> 🔴 **與 `[系列正片數]` 只差一件事：沒有內建觀看數門檻。**（2026-08-03 新增）
>
> 凡是用來決定「**哪些系列上場**」的篩選條件，都必須用這個版本。
> 用 `[系列正片數]` 的話，點「100 萬」時每個系列的集數會變成「該系列破百萬的集數」，
> 四個新系列各只剩 0–2 集，被 `≥3` 的篩選整群濾掉——**第 6 頁主圖的紅點會全部消失**。
>
> **判準：X 軸／Y 軸要跟著門檻變（那是演出的重點），但「誰上場」不能跟著變。**
>
> 使用處：第 6 頁主圖的視覺層級篩選、第 6 頁 `首次業配出現在第幾集`、第 5 頁 `系列業配密度`。

```dax
系列業配密度 =
IF ( [系列長片數_不受門檻] >= 10, [類型業配率] )
```
> ⭐ **第 5 頁側下圖的 X 軸。** 格式：**百分比，1 位小數**（不設會顯示成 `0.7` 而不是 `70.0%`）。
>
> **為什麼門檻要包進量值而不是用視覺層級篩選**：Power BI 的「前 N 項」**先算**，
> 它在量值篩選生效前就從全部系列挑出前 6 名（五個 2–3 集的 100% 小系列 ＋ 我獨自穿越日本），
> 門檻才把前五個砍掉，**實測只剩 1 列**。集數不足者回傳 BLANK 之後，
> **Top N 不會挑到 BLANK**，先後順序的問題就消失了。詳見 `BUILD.md` 第四節第 ⑥ 點。
>
> `>= 10` 的理由：少於 10 集時一支影片就讓業配率跳超過 10 個百分點，排不出可信的名次。
> 與主圖 `>= 20` 是同一個道理、不同尺度（系列的規模比類型小一個數量級）。

---

### ▍第 6 頁 接棒動能

```dax
系列正片數 = CALCULATE ( [影片數], 'videos'[video_format] = "長片" )
```
> **系列集數一律用正片計算。** Shorts 多為同標題的宣傳短片，
> 計入會虛增集數（例如《公車食刻表》正片 3 集，含 Shorts 會變成 4 集，
> 與 Joeman 自建播放清單的 3 集對不上）。

```dax
對決佔頻道觀看 =
DIVIDE (
    CALCULATE ( SUM ( 'videos'[view_count] ), 'videos'[series] = "Joe是要對決" ),
    CALCULATE ( SUM ( 'videos'[view_count] ), ALL ( 'videos' ) )
)
```
> 分母用 `ALL('videos')` 取全頻道總觀看，**不受任何篩選影響**。
> 這樣即使切了年份，這張卡仍顯示「對決佔全頻道 21.1%」的固定事實。

```dax
長片系列化比例 =
VAR LongTotal = CALCULATE ( COUNTROWS ( 'videos' ), 'videos'[video_format] = "長片" )
VAR WithSeries =
    CALCULATE (
        COUNTROWS ( 'videos' ),
        'videos'[video_format] = "長片",
        'videos'[系列世代] <> "無系列"
    )
RETURN DIVIDE ( WithSeries + 0, LongTotal )
```
> 🔴 **`WithSeries + 0` 不能省（2026-08-03 實測）**：2010 年有 200 支長片、有系列歸屬 0 支，
> `CALCULATE` 篩不到任何列時回傳 **BLANK 不是 0**，`DIVIDE ( BLANK, 200 )` 也是 BLANK，
> **第 6 頁側上圖的 2010 整年會消失**，X 軸從 2011 開始，與第 3、4 頁的 2010–2026 對不起來。
> 加 `+ 0` 強制轉成 0 之後，2010 以零高度柱子出現、軸標籤回來。
> 第 6 頁 KPI2 不受影響（非空值加 0 不變，仍是 36.6%）。
>
> 🔴 兩處修正（2026-08-02）：
> ① `VAR` 名稱改為 ASCII（原本的 `長片總數`／`有系列` 會造成語法錯誤，見第七節第 6 點）。
> ② 判斷「有沒有系列」改用已驗證的 `系列世代` 計算欄位，不用 `NOT ISBLANK('videos'[series])`
> ——`series` 的空白值匯入後可能是 null 也可能是空字串，`ISBLANK` 只抓得到前者，
> 抓不到時 1,130 支有系列會膨脹成全部 3,024 支長片，比例直接變 100%。
> **刻意只算長片。** 含 Shorts 的比例會混入格式效應
> （Shorts 幾乎都無系列歸屬），把 81%→38% 誇大成 80%→19%。

```dax
新系列中位觀看 =
CALCULATE ( [中位觀看], 'videos'[系列世代] = "新系列", 'videos'[video_format] = "長片" )
```

```dax
新系列業配率 =
CALCULATE ( [業配率], 'videos'[系列世代] = "新系列", 'videos'[video_format] = "長片" )
```

```dax
首集就有業配 =
VAR tot =
    CALCULATE ( COUNTROWS ( 'videos' ), 'videos'[是首集] = TRUE (), 'videos'[系列世代] = "新系列" )
VAR hit =
    CALCULATE ( COUNTROWS ( 'videos' ), 'videos'[是首集] = TRUE (), 'videos'[系列世代] = "新系列",
                'videos'[is_sponsored] = TRUE () )
RETURN hit & " / " & tot
```
> ⭐ **第 6 頁 KPI4，取代原本不顯著的 `[新系列業配率]`。** 回傳文字「**4 / 4**」。
>
> 這是**二元類別觀察**不是比率比較，不需要信賴區間：
> 既有系列 22 個裡只有 2 個（9%）第一集就有業配，新系列 4 個**全部**都有。
> 若機率相同，連續四次命中的機率是 **0.01%**。
>
> 顯示「4 / 4」而不是「100%」——把分母攤在檯面上，跟第 4 頁 KPI2 標「下限」同一個作風。

```dax
首次業配集數 =
VAR firstSponsored =
    CALCULATE ( MIN ( 'videos'[published_date] ), 'videos'[is_sponsored] = TRUE (),
                'videos'[video_format] = "長片" )
RETURN
    IF (
        NOT ISBLANK ( firstSponsored ),
        CALCULATE ( COUNTROWS ( 'videos' ), 'videos'[video_format] = "長片",
                    'videos'[published_date] <= firstSponsored )
    )
```
> 在以 `series` 為軸的圖表裡算出「到第一支業配為止累計第幾集」。從無業配的系列回傳空白。

```dax
首次業配出現在第幾集 =
VAR eps = [系列正片數]
VAR gen = SELECTEDVALUE ( 'videos'[系列世代] )
RETURN IF ( gen = "新系列" || eps >= 50, [首次業配集數] )
```
> 控制第 6 頁側下圖顯示哪些系列：**全部新系列 ＋ 集數 ≥50 的既有系列**，共 **8 列**。
> 🔴 **`≥40` 改為 `≥50`（2026-08-03 實測）**：40 時是 9 列，205px 的卡片塞不下
> （每列實需約 26px），右緣長出捲軸，而**被推到摺線下的正好是《Joeman開箱去》第 91 集**
> ——這頁收尾台詞最重要的那個數字。改 50 移除《網紅觀察室》（43 集、第 13 集），
> 不影響任何一句台詞。詳見 `BUILD.md` 第 6 頁側下那一節。
> 🔴 **這個量值已於 2026-08-02 從第 6 頁的 KPI 移除，改放 `[首集就有業配]`。**
>
> 原本的宣稱是「新系列業配率 ＞ 既有系列，證明廠商信賴已轉移」，但實測**不成立**：
>
> | 定義 | 新系列 | 既有系列 | 差異 | 自助抽樣 95% 信賴區間 |
> |---|---|---|---|---|
> | 曆年 ≥2025 | 65.4%（17/26） | 56.1%（46/82） | +9.3pp | **[−12.0, +29.9]pp 跨越零** |
> | 停播日之後 | 73.7%（14/19） | 55.1%（49/89） | +18.6pp | **[−4.7, +40.0]pp 跨越零** |
>
> 中位觀看的差異同樣不顯著（+84,521，信賴區間 [−77,734, +424,242]）。
> 照第七節第 7 點自訂的標準（區間跨越零則不下結論），**兩個指標都不能當結論**。
>
> 量值本身保留（可放工具提示或備答），但**不再擔任 KPI**。

---

## 四、每頁的圖表與它要說的話

### 整份報表主軸

> 這個頻道擴大了總產能並把部分長片資源轉投短影音，商業投放並未跟著減少——
> 看起來下降的整體業配率是偵測失效造成的。唯一真實的挑戰是招牌 IP 停播後的規模缺口，
> 而新系列的初動證明品牌動能還在。

### 敘事鏈

```
第 3 頁 產能結構變了      → 「商業面受影響嗎？」
第 4 頁 長片業配率穩定 55% → 「錢集中在哪些內容？」
第 5 頁 挑戰企劃 54%，且是事後認證 → 「最受青睞的來源停播了？」
第 6 頁 新系列業配率 65%，廠商跟著轉移
```

---

### 第 3 頁｜產能結構轉移（110 秒）

**一句話**：總產能提高 78%（2019→2025），其中長片產能部分轉移到短影音。

> 🔴 **「三年」是錯的（2026-08-02 實測修正）。** 78% / −22% / 0→169 這組數字
> 來自 **2019 年與 2025 年的比較，橫跨六年**。所有三年區間都對不上：
> 2022→2025 是 +63.9%、2021→2024 是 +67.8%、2020→2023 是 +56.4%。

| 位置 | 圖表 | 要說的話 |
|---|---|---|
| KPI ×4 | 影片總數／長片數／Shorts 數／長片中位觀看 | 建立規模感 |
| 主圖 | **堆疊柱**：灰＝長片、紅＝Shorts，X 軸年份 | 2019 年全是長片，2025 年 Shorts 已過半 |
| 側上 | **折線**：長片中位觀看，2024 年後改灰色虛線 | 2019→2023 下滑 23.4%；之後不可比 |
| 側下 | **堆疊面積**：類型組成 | 遊戲實況 → 挑戰企劃 → 3C 開箱，三次轉型 |

**篩選器演出（三步揭露）**
1. 全部 → 「**2019 到 2025 年，總產能增加 78%**」（169 → 300 支）
2. **點「長片」** → 「但長片其實少拍了 22%」（169 → 131 支）
3. **點「Shorts」** → 「增加的全是這個，從 0 到 169 支」

🔴 **必須主動說明 2015 年那根柱子。** 全期圖表上最高的是 2015 年的 **530 支**，
不是 2025 年的 300 支——只說「總產能提高」會被打臉（從 2015 算起是下降 43%）。

解釋：2015 年的 530 支裡，**遊戲實況佔 435 支（82%）**，那是日更實況時期；
2025 年的 131 支長片主力是《挑戰/企劃》59 支。**一支實況錄影跟一支企劃型長片的
製作成本差幾十倍，產能數字不可直接比。**

這段說明正好餵給側下那張「三次轉型」的圖——2015 的高產能是遊戲實況撐起來的。

---

### 第 4 頁｜投放規模（110 秒）

**一句話**：廠商投放沒有減少，長片業配率穩定在 55%。

| 位置 | 圖表 | 要說的話 |
|---|---|---|
| KPI ×4 | 長片業配率／偵測業配數／推估實際／判定精確率 | 同時給數字與可信度 |
| 主圖 | **雙軸**：柱＝業配影片數、線＝業配率 | 2022 年業配量達峰，但密度維持 |
| 側上 | **堆疊橫條**：業配偵測缺口（長片與 Shorts） | 長片測到 76.3%、Shorts 只測到 14.6% |
| 側下 | **橫條**：業配影片的類型組成 | 挑戰企劃貢獻最多業配影片 |

**篩選器演出（2026-08-02 改為三停點，完整台詞見 `BUILD.md` 第 4 頁）**

年份預設**全期 2010–2026**，敘事從 2010 年開始逐步推進：

| 停點 | 長片業配率 | 切「全部」後 | 落差 |
|---|---|---|---|
| 2010（預設） | 17.0% | 15.4% | **1.6pp** |
| 2020 | 45.0% | 32.4% | 12.6pp |
| **2022** | **53.2%** | **33.9%** | **19.3pp** |

🔴 **順序不可對調：先拉年份到 2022，才切資料範圍。**
全期時切「全部」只掉 1.6pp——Shorts 是 2021 年才開始拍的，在全期分母裡佔比小，
稀釋效果幾乎看不見。拉到 2022 之後才有 19.3pp，「腰斬」的畫面才成立。
**這是資料結構決定的，不是編排偏好。**

⚠️ 開場的 17.0% 是刻意的「反面數字」（全期平均混合了業配市場成形前後的兩個體制），
**必須在 10 秒內解釋掉**，否則會變成觀眾記住的數字。

**本頁口白：2023/11/06 事件（不加視覺）**

> 「2023 年底那個事件之後，他沒有停更——當天照常上片，第 9 天就有業配影片上架。
> 事件前後各半年的長片業配率都是 51.8%，差異的 95% 信賴區間跨越零，
> **廠商沒有撤。**
> 但我要講清楚我測不到什麼：觀看層面的衝擊我測不到，
> 因為 API 只給當下的累計觀看，沒有歷史每日數據。
> 所以我只能說『測不到顯著變化』，不能說『證明沒有影響』。」

⚠️ 完整數據與邊界見 `HANDOFF.md` 第十五章。時間分配見第 9-1 節（已定案）。

---

### 第 5 頁｜投放偏好（110 秒）

**一句話**：業配是流量的結果，不是原因——它是廠商投放意願的顯示性偏好。

📌 這句話依 `HANDOFF.md` 第零章**成立且不需加但書**。

> ⚠️ **本頁的論證權重已於 2026-08-01 修正：主圖是配角，側上圖才是主角。** 理由見下方。

| 位置 | 圖表 | 角色 | 要說的話 |
|---|---|---|---|
| KPI ×4 | 業配率最高類型／業配率最低類型／類型間差距／樣本數（長片） | — | 直接回答「錢往哪去」 |
| 主圖 | **橫條**：各類型業配率，最高一條標紅 | **配角**：呈現投放偏好的分布 | 挑戰／企劃 45.7% 最高 |
| 側上 | **柱狀**：廠商進場時序（隨系列篩選器連動） | **主角**：本頁的主要因果證據 | 《看房》前兩年 0%，2022 年才跳到 42.9% |
| 側下 | **橫條**：系列的業配密度排序 | 過渡到第 6 頁的系列視角 | 哪些系列最受廠商青睞 |

（KPI 名稱以背景圖 `bg_page5.png` 印死的標籤為準。）

#### 為什麼主圖撐不起這頁的論證

跨類型的**橫向**比較中，業配率與觀看數幾乎無相關（Spearman ρ = **−0.07**）。
以 2018 年起、樣本 ≥40 的長片為例：

| 類型 | 中位觀看 | 業配率 |
|---|---|---|
| 人物/訪談 | 74.5 萬 | 16.0% |
| 挑戰/企劃 | 72.0 萬 | 51.4% |

流量幾乎相同、業配率差三倍。**橫向排序反映的是「內容適不適合置入商品」，
不是「流量高低」。**

真正撐得起的是側上圖——它是**縱向**比較：同一系列、同一種內容、同一個主持人，
唯一變動的是已累積的成績，**控制住了橫向比較控制不住的變因**。

#### ⚠️ 主圖的資料陷阱：年代效應（逐項揭露時必須主動說明）

主圖含全部 14 個類型、不做年份排除，所以會出現兩根接近零的長條：

| 類型 | 長片數 | 業配率 | 年代集中在 |
|---|---|---|---|
| 遊戲實況 | 699 | 0.7% | **2015–2016 佔 90.7%** |
| 電競賽事 | 446 | 2.5% | 2010–2011 佔 64.6% |

**這是年代效應，不是廠商偏好**——長片業配率 2010–2016 全部在 5% 以下，
2017 年 5.2%、2018 年才首次進入兩位數 11.3%，當時業配文化尚未成形。

台詞：「這兩根接近零的不是廠商不投，是那個年代還沒有業配這回事。」

**篩選器演出**
- 逐項加入類型觀察分布，加到遊戲實況／電競賽事時**主動說明年代效應**
- 點《Joe是要看房》→ 側上圖顯示 0% → 0% → **42.9%** → 40.9% → **91.7%**
- 台詞：「廠商是在企劃證明自己之後才進場，不是廠商的錢造就了企劃。」
- 若被質疑是特例，**當場再點《對決》**（17.6% → **86.3%**，樣本 307 集）

⚠️ **不要點《Joeman開箱去》**——它 2016–17 年說明欄只有 158 字，
0% 是偵測失效不是事實。

---

### 第 6 頁｜接棒動能（110 秒）　🔴 2026-08-02 大幅改版（方案丁）

**一句話**：招牌停了，但接棒的三件事都到位了——**速度**（停播 9 天後開播）、
**戰力**（單集中位觀看與《對決》最後一年持平）、**廠商信任**（第一集就進場，
歷史上要等到第 91 集）。**唯一缺的是規模：19 集對 307 集。**

| 位置 | 圖表 | 要說的話 |
|---|---|---|
| KPI ×4 | 對決佔頻道觀看／長片系列化比例／新系列中位觀看／**首集就有業配** | 兩壞兩好：缺口、產線、戰力、信任 |
| 主圖 | **散佈圖**：X＝正片集數、Y＝中位觀看、大小＝總觀看 | 《對決》唯一「量大又高效」；新系列在左上——效率夠，缺集數 |
| 側上 | **柱狀**：長片系列化比例趨勢 | 2022 年 81.0% → 2025 年 64.1%，產線鬆動 |
| 側下 | **橫條**：**首次業配出現在第幾集** | 新系列 4 個全是第 1 集；《開箱去》第 91 集、《看房》第 25 集 |

#### 改版的理由：原收尾論點不成立

原本寫「新系列業配率 65.4% ＞ 既有系列 56.1%，證明廠商信賴已轉移」。
實測後**自助抽樣 95% 信賴區間 [−12.0, +29.9]pp 跨越零**，
照第七節第 7 點自訂的標準不能下結論。中位觀看的差異同樣不顯著。

改用**首集就有業配**這個二元類別觀察：

| | 系列數 | 第 1 集就有業配 |
|---|---|---|
| 既有系列（集數 ≥3） | 22 | **2 個（9%）** |
| **新系列** | **4** | **4 個（100%）** |

若機率相同，連續四個都命中的機率是 **0.01%**。**不需要信賴區間就成立。**

#### 關鍵數字

**規模缺口**

| | 集數 | 總觀看 | 單集中位 | 歷時 |
|---|---|---|---|---|
| 《對決》 | 307 | 2.99 億 | 809,917 | 7.6 年 |
| 四個新系列 | 19 | 1,506 萬 | 634,262 | 0.7 年 |
| **比例** | **6.2%** | **5.0%** | **78%** | — |

**戰力已追平**（年份拉到 2025 起）

| 2025 年起 | 集數 | 中位觀看 |
|---|---|---|
| 《對決》最後一年 | 45 | **644,344** |
| 四個新系列 | 19 | **634,262** |

相差 **1.6%**。**同一時間窗口、同一頻道、同一批觀眾的對照**，
比「新系列 vs 既有系列」乾淨得多——直接對上要被取代的那個標的。

#### 開場鉤子（口白，不靠視覺）

> 「《對決》的最後一集叫『**是時候該放手了**』，2025 年 11 月 10 日，第 307 集。
> **九天後**，新系列開播——一趟從九州最南端開到北海道最北端、三千公里的自駕，
> 後來播了 78 天共 10 集。
> **那趟旅程不可能是那九天拍的。招牌宣布結束之前，接班的內容已經在路上了。**」

⚠️ 這段**完全可從資料驗證**（發布日 ＋ 標題裡的地名 ＋ 集數跨度），不需外部知識。
Q&A 若被追問可再補「那支停播宣告就是在這趟日本行程中錄的」——
**順序不能反**：先給可重現的推論，再給觀察當佐證。

#### 收尾台詞

> 「三件事都到位了：**九天就接上**、**單集戰力持平**、**廠商第一集就進場**。
> 缺的只有規模——19 集對 307 集，總觀看 5%。
> 而《開箱去》等到第 91 集才有第一支業配，《看房》等了 719 天。
> **廠商的信任對象，已經從『某個節目』變成『這個品牌』。**」

🔴 **必須主動說明的表面矛盾**：第 5 頁說「廠商在系列證明後才進場」，
第 6 頁說「第一集就進場」。解釋是**機制改變**——早期廠商評估個別系列，
現在評估 Joeman 這個品牌。
**現在這個矛盾有數字了：9% → 100%。這是全報告最有力的收尾，不是漏洞。**

---

## 五、格式設定

| 量值 | 格式 |
|---|---|
| 業配率、業配率（依資料範圍）、流量貢獻度、對決佔頻道觀看、長片系列化比例、新系列業配率、類型業配率、最高／最低類型業配率、判定精確率、**系列業配密度** | 百分比，1 位小數 |
| 觀看差距 | **自訂格式字串** `+0.0%;-0.0%;0.0%` |
| 影片數、業配影片數、長片數、Shorts 數、推估實際業配、業配影片數（依資料範圍）、樣本數_長片、系列正片數、首次業配集數 | 整數，千分位 |
| **動態資料規模、可比性、業配率最高類型、業配率最低類型、類型間差距、主圖色_最高標紅、最新年度提醒、首集就有業配、新系列清單** | **文字，不用設格式** |
| 中位觀看、長片中位觀看、新系列中位觀看、業配中位觀看、非業配中位觀看、總觀看 | 整數，千分位 |

### 若想在 KPI 卡顯示「55.6 萬」

```dax
中位觀看_萬 = FORMAT ( DIVIDE ( [中位觀看], 10000 ), "0.0" ) & " 萬"
```

⚠️ 這會變成**文字**，不能用於圖表軸。只適合 KPI 卡片，圖表仍用原始量值。

---

## 六、互動設定（編輯互動）

Power BI 預設所有篩選器影響所有視覺物件，但有兩處需要關閉：

| 頁 | 篩選器 | 需關閉互動的物件 | 原因 |
|---|---|---|---|
| **3** | **影片格式** | **KPI ×4** | `[長片數]`／`[Shorts 數]`／`[長片中位觀看]` 內部用 `CALCULATE` 寫死格式會**覆蓋切片器**，只有 `[影片數]` 會變 → 一張動三張不動 |
| **3** | **影片格式** | **側上折線圖** | 點 Shorts 時 X 軸塌成 2021–2026（Shorts 2021 年才出現），成為自相矛盾的圖 |
| 5 | 系列 | 主圖（各類型業配率） | 主圖是類型層級的比較，被系列篩選會只剩一根柱子 |
| 6 | 年份區間 | KPI「對決佔頻道觀看」 | 分母已用 `ALL` 鎖住，**但分子仍會被篩**；拉到 2025 起會變 2.3% |
| **6** | **年份區間** | **側下圖（首次業配集數）** | 「第幾集」是歷史事實，被年份篩選後會在窗口內重算集序 |

**判準**：只有當視覺被篩選後會**錯誤或自相矛盾**時才封鎖。
年份區間在第 3、4 頁不需封鎖（四個 KPI 一致變動）；
`門檻` 與 `資料範圍` 是中斷連結表，本身不篩資料列，**也不需封鎖**。

設定方式：選取要保護的視覺物件 → `格式` → `編輯互動` → 對該篩選器點「**無**」。

---

## 七、容易踩的坑

### 1. `KEEPFILTERS` 不能省
```dax
-- ❌ 錯：會覆蓋掉視覺物件本身的年份／類型篩選
CALCULATE ( COUNTROWS ( 'videos' ), 'videos'[view_count] >= th )

-- ✅ 對：保留原有篩選，只疊加門檻
CALCULATE ( COUNTROWS ( 'videos' ), KEEPFILTERS ( 'videos'[view_count] >= th ) )
```

### 2. 中斷連結表**千萬不要**建關聯
`門檻` 與 `資料範圍` 若與 `videos` 建立關聯，篩選器會直接過濾資料表，
累加門檻的邏輯就失效了。它們必須是孤立的表，只靠量值讀取選取值。

### 3. `SELECTEDVALUE` 一定要給預設值
```dax
-- ❌ 未選取或複選時回傳空白，整張卡變空
SELECTEDVALUE ( '門檻'[門檻值] )

-- ✅ 給預設值
SELECTEDVALUE ( '門檻'[門檻值], 0 )
```

### 4. 業配相關量值忘記加「僅長片」
只要漏掉 `'videos'[video_format] = "長片"`，Shorts 的無效資料就會混進來，
業配率會從 53.0% 掉到 30.4%——**看起來像資料錯了，其實是條件漏寫**。

### 5. 系列集數用 `COUNTROWS` 而非 `DISTINCTCOUNT`
一支影片一列，直接計數即可。且**必須限長片**，否則宣傳用的 Shorts 會虛增集數。

### 6b. `VAR` 的名稱不能跟 DAX 函式同名 🔴（2026-08-02 實測）

```dax
-- ❌ 錯：'lastDate' 的語法不正確
VAR lastDate = MAX ( 'videos'[published_date] )

-- ✅ 對
VAR maxPub = MAX ( 'videos'[published_date] )
```

**`LASTDATE()` 是 DAX 的時間智慧函式。** DAX 識別碼不分大小寫，
所以 `lastDate` 就等於 `LASTDATE`，解析器把它當函式名而不是變數名。

容易誤踩的名字（全都是 DAX 函式）：

```
lastDate  firstDate  date  time  year  month  day
value  format  filter  search  union  path  rank
min  max  sum  average  count  blank  error
```

**安全的命名習慣**：加前綴或用組合詞——`maxPub`、`vLastDate`、`curYear`、`thVal`。
本文件已在用的 `th`、`sel`、`t`、`s`、`firstYear`、`LongTotal`、`WithSeries` 都安全。

### 6. `VAR` 的名稱不能用中文 🔴（2026-08-02 實測）
```dax
-- ❌ 錯：剖析時發生下列語法錯誤：權杖無效，第 3 行，位移 5，篩。
VAR 篩選後 = CALCULATE ( ... )

-- ✅ 對
VAR Filtered = CALCULATE ( ... )
```
資料表名（`'videos'`）、欄位名（`[view_count]`）、量值名（`[業配率]`）都可以用中文，
因為它們有單引號或方括號包起來。**`VAR` 的名稱沒有任何包裹符號，必須是
ASCII 英文字母、數字、底線，且開頭不能是數字。**

本文件原本有兩個量值犯這個錯（`流量貢獻度`、`長片系列化比例`），已修正。
其餘量值用的是 `VAR th`／`VAR sel`／`VAR s`／`VAR t`，本來就合法。

⚠️ `ADDCOLUMNS ( ..., "@率", [類型業配率] )` 裡的 `"@率"` 有雙引號、
後續參照的 `[@率]` 有方括號，**那是合法的**，不要一起改掉。

### 7. `series` 的空白值不要用 `ISBLANK` 判斷 🔴
`series` 沒有歸屬的影片，經 Power Query 匯入後可能是 **null**，也可能是**空字串 `""`**，
取決於連接器與 pandas／Power Query 的版本行為。`ISBLANK` 只抓得到 null。

抓不到時的症狀：2,372 支「無系列」影片被誤判成有系列，
`長片系列化比例` 從 36.6% 變成 100%，`系列世代` 全部歸到「既有系列」——
**而且不會報錯**。

解法：一律改用計算欄位 `系列世代`（它內部已用 `ISBLANK ( s ) || s = ""` 同時涵蓋兩種），
需要判斷有無系列時寫 `'videos'[系列世代] <> "無系列"`。

---

## 八、實作順序建議

1. 先做**第 4 頁**——它涵蓋了門檻表、資料範圍表、雙軸圖、KPI 卡，
   等於一次驗證所有機制。這頁通了，其他頁都是重複套用。
2. 確認背景圖對齊、量值數字正確、篩選器連動無誤
3. 再依序做第 3、5、6 頁
4. 最後做第 1、2、7 頁（封面與兩張表格，最單純）

---

## 九、簡報執行

### 9-1 時間分配（總計 620 秒 ≈ 10.3 分鐘）

| 段落 | 秒數 | 內容 |
|---|---|---|
| 開場 | 60 | 自我介紹、研究對象與動機 |
| 資料特徵 | 90 | 資料來源、判定方法、準確度 |
| 第 3 頁 產能 | 110 | 產能結構轉移 |
| 第 4 頁 投放 | 110 | 廠商投放未減 |
| 第 5 頁 偏好 | 110 | 錢往哪去、事後認證 |
| 第 6 頁 接棒 | 110 | 新系列動能 |
| 結論 | 30 | 收尾 |

超過 10 分鐘約 20 秒，**可從資料特徵頁壓縮到 75 秒**補回（→ 605 秒 ≈ 10.1 分）。

#### 🔴 第 4 頁的 110 秒怎麼分（2026-08-06 定案）

| 步驟 | 秒 |
|---|---|
| ① 開場（停在 2010，17.0%） | 25 |
| ② 拉到 2020（45.0%） | 20 |
| ③ 拉到 2022（53.2%） | 20 |
| ④ 切「全部」（掉到 33.9%） | 30 |
| ⑤ 側上圖：偵測缺口 | 20 |
| **小計** | **115** |

**已經超出 5 秒，而 2023/11/06 事件口白還要 20–30 秒。** 兩筆額度：

1. **砍掉第 ② 站**（2020 停點），直接 2010 → 2022 → 省 **20 秒**。
   損失的只是「疫情後爬升」這個細節，主線不受影響
2. **資料特徵頁壓到 75 秒** → 省 **15 秒**

**兩筆都動就是 620 − 35 ＝ 585 秒 ≈ 9.8 分鐘**，事件口白 25 秒放得進去。
⚠️ 排練時先照完整版跑一次計時，**真的超了才砍第 ② 站**——那是最無痛的一刀。

### 9-2 開場（60 秒）

> 「我分析的是 Joeman 頻道——台灣少數橫跨十六年、且內容類型完整轉型過的頻道。
> 從電競實況起家，中間轉向生活娛樂，現在又大量投入短影音。
>
> 這樣的跨度讓它適合回答一個問題：**當一個內容品牌的產能結構改變時，
> 它的商業結構會跟著改變嗎？**
>
> 資料全部來自 YouTube 公開 API，每天自動更新。我等一下會說明我怎麼確認這些數字可信。」

### 9-3 資料特徵頁（90 秒）重點

不要逐項念表格，挑三件事講：

1. **Shorts 判定用網址實測**，不是猜時長（2024 年後 Shorts 上限放寬到 3 分鐘，用時長會誤判）
2. **業配判定分格式各抽 40 支純隨機人工核對**，長片精確率 22/22、召回率 **76.3%**
3. **Shorts 的業配偵測失效**（召回率僅 **14.6%**），所以商業分析排除 Shorts

第 3 點是重點——**主動說出方法的邊界，比被問到才承認有力得多**。

### 9-4 結論（30 秒）

> 「三件事：
> 第一，這個頻道的產能沒有萎縮，是**結構轉移**——長片減少、短影音大增。
> 第二，廠商的投放沒有減少——長片業配率 2022 年起穩定在 55% 上下。
> 看起來下降是**偵測失效**造成的：Shorts 我只測得到一成五。
> 第三，唯一真實的挑戰是《對決》停播後的規模缺口——但四個新系列**第一集就有業配**，
> 而《開箱去》等到第 91 集，**廠商信賴已經從單一節目轉移到品牌本身**。」

🔴 **2026-08-05 改寫**：舊版第二點寫「業配影片表現較好是選擇效應」，
依 `HANDOFF.md` 第零章那個方向不成立也不需要提；舊版第三點的「新系列業配率已超過既有系列」
早在方案丁（2026-08-02）就因信賴區間跨越零而被「首集就有業配 4/4」取代，此處一併同步。

### 9-5 發表當天的操作提醒

| 事項 | 說明 |
|---|---|
| **前一天先重新整理** | Power BI Desktop **沒有自動刷新**。開檔後按`常用`→`重新整理` |
| **抄一次最新數字** | 資料每天變動，講稿數字要與畫面一致（見 `HANDOFF.md` 十五章） |
| **截圖備份** | 每頁存一張 PNG。若現場網路或檔案出問題，還能用圖片講完 |
| **練熟三個關鍵點擊** | 第 3 頁格式三步、第 4 頁資料範圍切換、第 5 頁系列切換 |
| **第 7 頁當備用** | 不列入 10 分鐘，被問限制時再翻出來 |

### 9-6 可展示的加分項

打開 GitHub 的 commit 紀錄給評審看——**每天一筆 `資料自動更新` 的提交，
提交者是 `github-actions[bot]`**。這比口頭說「有做自動化」有說服力得多。

```
https://github.com/joker8604169-cloud/joeman-youtube-analytics/commits/main
```

---

### 9-7 Q&A 備答：方法論四題（2026-08-06 整理）

這四題只要被問到一題，答得出來就會拉開差距。

#### ① 「精確率和召回率是什麼？」

先立一張 2×2（混淆矩陣）：每支影片同時有「程式怎麼判」與「真相是什麼」兩個身分。

|  | 實際是業配 | 實際不是 |
|---|---|---|
| 判定為業配 | 真陽性 TP | 偽陽性 FP（**誤判**） |
| 判定為非業配 | 偽陰性 FN（**漏抓**） | 真陰性 TN |

$$	ext{精確率}=rac{TP}{TP+FP}\qquad	ext{召回率}=rac{TP}{TP+FN}$$

**分子相同，分母不同**——精確率的分母由**你的判定**決定，召回率的分母由**真相**決定。

> 撈魚版：精確率＝撈上來的東西裡有多少真的是魚；召回率＝池子裡的魚你撈到幾成。

🔴 **兩個一定要一起報**，因為各自都能造假：全部判成業配 → 召回率 100%；
只判最有把握的一支 → 精確率 100%。

**為什麼要用兩份不同的抽樣**：精確率的分母是「判定為業配」，所以從那堆抽；
召回率的分母是「實際是業配」，而你**不知道**哪些是——只能從「判定為非業配」
純隨機抽，量出漏抓率再回推。這就是專案有兩份抽樣檔的原因。
⚠️ 召回率抽樣**必須純隨機**：曾用分層抽樣宣稱 92%，純隨機重測只有 60.8%。

#### ② 「95% 信賴區間 85.1–100% 怎麼讀？」

**後面兩個數字**是上下界——真實值若低於 85.1%，「抽 22 支全對」就會變成太罕見的事。
上界頂到 100% 是因為 22/22 已經是滿分。

**前面的 95%** 是信賴水準，描述的是**方法**不是這一個區間：
用同樣方法反覆抽樣，約 95% 的區間會涵蓋真實值。

❌ 不要說「真實值有 95% 機率落在區間內」——真實值是固定的，會變動的是區間。
✅ 安全講法：「真實值低於 85.1% 的話，我抽 22 支全對就太不尋常了。」

**區間寬度幾乎由樣本數決定**：全對時，n=10 下界約 72%、n=22 是 85.1%、n=100 約 96%。
這正是 KPI 顯示「22 / 22」而不是「100%」的理由。

📌 用的是 **Wilson score 區間**，不是常態近似——後者算 22/22 會得到 [100%, 100%]，
宣稱絕不出錯，荒謬。

#### ③ 「你用什麼模型判斷業配？」

🔴 **沒有模型。判定是規則，不是機器學習。**

```
人工覆寫表  ＞  API 官方付費宣傳旗標  ＞  關鍵字加權計分
（最高優先）      （全頻道只有 3 支）      （絕大多數靠這個）
```

`rules/sponsor_rules.csv` 共 **35 條**規則，權重 3／2／1（另有一條 −4 扣分）：
權重 3 是強訊號（折扣碼、專屬連結、贊助聲明），單獨命中即成立；加總 **≥2 分**判定為業配。
比對前先剔除說明欄罐頭文字。Python 只負責讀表、編譯正則、逐支比對加總——
**判斷邏輯完全在那張 CSV 裡，不在程式裡**。

> 「我沒有用機器學習。判定邏輯是 35 條可讀的規則，存在一張 CSV，任何人都能打開看、也能改。
> 正因為是規則，我才能**逐支追溯為什麼判成業配**（`sponsor_hits` 欄位記著命中哪幾條）。
> 代價是它只看得到文字——所以我才做人工抽樣，量出精確率、召回率，
> **那些數字量的就是這套規則的極限**。」

⚠️ `sponsor_evaluate.py` 的輸出寫「模型偵測」，那是誤稱，建議改為「規則偵測」。

#### ④ 「你有沒有看過誤判？」

**有，一支。**《AI是否會取代Youtuber？》（網紅觀察室）命中「產品導流連結」被判成業配，
人工核對是否。

它不在精確率那 22 支裡——它來自**漏抓率**抽樣（原本判非業配，規則調整後才變業配），
不屬於精確率的抽樣框架所以不計入。若計入是 22/23＝95.7%。

🔴 **正確答案是「有，一支」，不是「沒有」。** 主動說出自己看過的錯誤，
比宣稱零錯誤可信得多。

---

## 十、修訂紀錄

### 2026-08-06｜第 5 頁 KPI2 空白類型修正、兩處規則覆寫

| # | 修改 | 理由 |
|---|---|---|
| 1 | **`業配率最低類型` 的 `FILTER` 加上 `&& [樣本數_長片] > 0`** | `[僅長片]` 刻意不含觀看數門檻，所以高門檻時會有類型「通過 ≥20 閘門但一支不剩」。那種類型的 `[類型業配率]` 是 BLANK，而 `TOPN ( ..., ASC )` 把 BLANK 當成比 0 還小，**它會被選成最低**。症狀：門檻點「100 萬」時 KPI2 顯示**電競賽事**，但電競賽事破百萬的長片是 **0 支**——那格顯示的不是「業配率最低」，是「沒有資料」。用 `[樣本數_長片]` 而非 `[影片數]`，是因為 `[類型業配率]` 只算長片，判斷有無樣本的尺必須同格式 |
| 1b | **同一量值再加第二組排序 `[@樣本], DESC`** | 上一項修好之後，100 萬門檻下變成**三個類型並列 0.0%**，`TOPN` 只有一組排序時取誰是任意的，實測挑中樣本僅 4 支的日常Vlog。三個都是真的 0.0%，KPI 不會錯，但**它會隨每日資料變動**，排練與正式上台可能顯示不同名字。第二組排序設成「樣本數由多到少」後固定取證據最紮實者（100 萬＝玩具/收藏 17 支）。門檻「全部」時遊戲實況唯一最低，不受影響 |
| 2 | `業配率最高類型`、`最低類型業配率` **不需要**同樣的修正 | BLANK 排在最小端，`MAXX`／`TOPN` 取最大時永遠選不到；`MINX` 本來就忽略 BLANK，`類型間差距` 44.9pp 是對的。為寫法對稱而一起加是零風險，但不會改變任何數值 |
| 3 | **`manual_overrides.csv` 新增 `qstdz2GV4SI`（`is_sponsored=False`）** | 《Joeman大麻風暴》紀錄片說明欄寫「**本影片與臺灣新北地方檢察署合作**」，命中權重 3 的 `影片合作聲明` 單條成立。對象是地檢署、屬反毒公益合作，**無商業委託**。同一條規則 2026-08-03 已因《2015年最令我印象深刻的10款遊戲》覆寫過一次（`XxbsfsQp88U`），此支為漏網。**沒有改規則權重**，理由同第八章：改權重會影響全期判定，代價遠大於收益 |
| 4 | **`playlist_map.csv` 的【第九人稱】：`遊戲實況` → `人物/訪談`**，並補上 `series_name=第九人稱` | 該播放清單 3 支全是社會議題紀錄片（詐騙群組臥底、大麻風暴、黃牛產業鏈），與遊戲實況無關。第四支《一個月工作 400 小時的人生》不在清單內、靠標題規則已歸在人物/訪談——改完四支一致 |
| 5 | 受影響的數字 | 長片偵測業配數 **514 → 513**、推估實際 **674 → 672**、2022 年起業配率 **53.4% → 53.2%**、類型間差距 **45.3 → 45.4pp**、遊戲實況（門檻全部）**0.4% → 0.3%**、遊戲實況（門檻 100 萬）**33.3% → 0.0%**。**第 6 頁完全不受影響**（既有系列 2/22、新系列 4/4 不變），主線結論不變 |

⚠️ 第 3、4 點改的是 `rules/`，**要重跑 `transform` 才會生效**。本機 `data/raw` 過期，
不可直接跑（`HANDOFF.md` 第十三章第 10 點）——正確路徑是 commit 後由 GitHub Actions 重跑。

### 2026-08-01｜第 5 頁論證權重、缺漏量值、數字更新

| # | 修改 | 理由 |
|---|---|---|
| 1 | 第 5 頁主圖降為配角、側上圖升為主要證據（第四節） | 跨類型橫向比較中業配率與觀看數無相關（Spearman ρ = −0.07）；人物/訪談 74.5 萬／16.0% vs 挑戰/企劃 72.0 萬／51.4%，流量相同但業配率差三倍。橫向排序反映的是內容適不適合置入，不是流量。縱向的進場時序控制住了內容型態，才是有效的因果證據 |
| 2 | 新增主圖年代效應警語（第四節） | 主圖含全部 14 類型，遊戲實況 0.7%（2015–2016 佔 90.7%）、電競賽事 2.5%（2010–2011 佔 64.6%）會出現為接近零的長條。2010–2016 長片業配率全在 5% 以下，2018 年才破兩位數——是年代效應不是廠商偏好，逐項揭露時必須主動說明 |
| 3 | 新增量值 `業配率最低類型`、`類型間差距`（第三節） | 背景圖 `bg_page5.png` 印死的 KPI 標籤為「業配率最高類型／業配率最低類型／類型間差距／樣本數（長片）」，原文件第四節寫的四張卡與 PNG 不符，且缺這兩個量值。以 PNG 為準補齊 |
| 4 | 樣本門檻說明：財經／投資 7 支 → **6 支** | 實測全期長片 6 支（含 Shorts 才 11 支），原文寫「近三年 7 支」已過期 |
| 5 | 挑戰／企劃 53.6% → **45.7%**；刪除「美食 14.0%」 | 以 8 種口徑（含/不含 Shorts × 全期/2018起/2019起/近三年 × 門檻 20/40）測試，**無任何組合能重現 53.6% 與 14.0%**，且美食在所有口徑下都不是最低類型。研判為業配規則修正後（見 `HANDOFF.md` 十三章第 1 點）數字整體位移，原口徑已無法還原 |
| 6 | 第 4 頁資料範圍切換 27% → **30.4%** | 實測 2023 年起：長片 53.0%、含 Shorts 30.4% |
| 7 | 既有系列業配率 55.6% → **56.1%**；新系列中位觀看 59.2 萬 → **59.9 萬** | 資料每日更新造成的漂移 |
| 8 | 《對決》2023 業配率 81% → **86.3%**；《看房》2022 年 43% → **42.9%** | 同上，並補齊完整年序 |
| 9 | 第 4 頁加入 2023/11/06 事件口白（第四節） | 已決議納入講稿、不加視覺。完整數據見 `HANDOFF.md` 第十五章 |
| 10 | 兩份文件開頭加註「數字為當日快照」 | 資料每日自動更新，避免日後引用過期數字 |
| 16 | **第 6 頁改版（方案丁）**：KPI4「新系列業配率」→「**首集就有業配 4/4**」、側下圖「新系列動能對照」→「**首次業配出現在第幾集**」；背景圖重產 | 原收尾論點「新系列業配率 ＞ 既有系列」的自助抽樣 95% 信賴區間 **[−12.0, +29.9]pp 跨越零**，照第七節第 7 點的標準不能下結論；中位觀看差異同樣不顯著。改用二元類別觀察（新系列 4/4 vs 既有 2/22，機率 0.01%），**不需要信賴區間就成立**，且與第 5 頁「廠商進場時序」前後呼應 |
| 17 | **`系列世代` 界線：曆年 ≥2025 → 《對決》停播日 2025-11-10** | 原規則會把《工作室日常廢片》（首播 2025-08-27，比停播**早 75 天**）算成新系列，不符「招牌停播後的接棒」的宗旨。改用停播日後，定義與論點對齊，新系列從 5 個/26 支收斂為 **4 個/19 支** |
| 18 | **新增 `是首集` 計算欄位與 `首集就有業配`、`首次業配集數`、`首次業配出現在第幾集` 三個量值** | 支撐第 6 頁的新 KPI 與側下圖，見第 2-4 節與第三節第 6 頁區塊 |
| 19 | 側上圖的宣稱：「81% → 38%」→「**2022 年 81.0% → 2025 年 64.1%**」 | 原講法的終點 2026 是**不完整年度**（7 個月、63 支），起點 2022 是**全期最高峰**，用最高點對半年會放大落差。改成兩端都是完整年度，−16.9pp，結論仍成立但站得更穩 |
| 20 | 主圖氣泡圖的視覺層級篩選：集數 ≥10 → **≥3** | ≥10 時只有《我獨自穿越日本》一個新系列能露臉，畫面撐不起「新系列」這一群 |
| 11 | 第 9-1 節時間分配表標記為未定案（**已於 2026-08-06 定案**） | 加入事件口白後總長超出，且原方案的「資料特徵頁壓縮」額度已被用於補原有的 20 秒超時，不能重複支用。詳見該節 |
| 12 | **`流量貢獻度`、`長片系列化比例` 的 `VAR` 名稱改為 ASCII**（2026-08-02，實作時發現） | DAX 的 `VAR` 名稱不能用中文，貼進 Power BI 會回報「權杖無效」。這兩個量值原本寫成 `VAR 篩選後`／`VAR 長片總數`，**表示它們從未真的在 Power BI 裡執行過**。已加入第七節第 6 點 |
| 13 | **`長片系列化比例` 改用 `系列世代 <> "無系列"` 判斷** | 原本用 `NOT ISBLANK('videos'[series])`。`series` 空白值匯入後可能是 null 也可能是空字串，`ISBLANK` 只抓得到前者，抓不到時比例會變 100% 且不報錯。已加入第七節第 7 點 |
| 14 | **新增量值 `最新年度提醒`**（見 `BUILD.md` 3-6）；第 4 頁演出改為**三停點**、年份預設全期 | ① 主圖的柱子逐年下降，視覺上像「投放腰斬」，與本頁論點相反；最後一年又是不完整年度，需要動態註記說明，**且不能寫死年月**（資料每日更新）。② 使用者決定敘事從 2010 年開始逐步推進，年份預設改為全期。實測發現**全期時「資料範圍」切換只差 1.6pp**（Shorts 2021 年才出現），演出順序因此被資料結構鎖定 |
| 15 | **第 4 頁主圖改用 `[業配影片數（依資料範圍）]` 與 `[業配率（依資料範圍）]`** | 原本的 `[業配影片數]`／`[類型業配率]` 寫死「僅長片」，不受資料範圍切片器影響——切換時只有 KPI 變、主圖不動。改用受範圍影響的版本後整條紅線會下沉，投影幕上的說服力差很多 |
