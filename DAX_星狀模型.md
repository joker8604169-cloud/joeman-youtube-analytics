# DAX 量值清單（星狀結構版）

> 本文件取代 `DAX.md` 第二節（前置設定）與第三節（量值清單）。
> `DAX.md` 其餘章節——**每頁要說的話、格式設定、互動設定、簡報執行、Q&A 備答**——
> 完全不受模型改版影響，繼續沿用。
>
> 模型結構、資料表欄位、載入步驟見 `STAR_SCHEMA.md`。
> 最後更新：2026-08-10
>
> ⚠️ 文中數字為撰寫當日快照，資料每日自動更新，引用前請重新查詢。

---

## 目錄

1. [這次改了什麼](#一這次改了什麼)
2. [五個結構性差異（改寫前必讀）](#二五個結構性差異改寫前必讀)
3. [計算欄位：全部移除](#三計算欄位全部移除)
4. [量值清單](#四量值清單)
5. [舊 → 新 對照總表](#五舊--新-對照總表)
6. [量值使用狀態：哪些還在用](#六量值使用狀態哪些還在用)
7. [星狀模型專屬的坑](#七星狀模型專屬的坑)
8. [改寫後的驗收步驟](#八改寫後的驗收步驟)

---

## 一、這次改了什麼

舊模型是**一張扁平寬表** `videos`（24 欄）＋ 兩張中斷連結表。
新模型是**兩張事實表 ＋ 六張維度表**的星狀結構：

```
                    dim_日期
                       │
   dim_影片格式 ── fact_影片 ── dim_類型
                    │    │
   dim_業配判定 ─────┘    └───── dim_系列
                    │
                 dim_影片
                    │
              fact_每日快照
```

**量值改寫只有三種動作**，沒有第四種：

| 動作 | 說明 | 影響量值數 |
|---|---|---|
| A. 換資料表前綴 | `'videos'[view_count]` → `'fact_影片'[觀看數]` | 全部 44 個 |
| B. 篩選條件改指維度 | `'videos'[video_format]="長片"` → `'dim_影片格式'[影片格式]="長片"` | 32 個 |
| C. 硬編碼常數改讀維度 | `SWITCH(...,"長片",0.763,...)` → `SELECTEDVALUE('dim_影片格式'[業配召回率])` | 2 個 |

**唯一一個邏輯真的變了的量值是 `對決佔頻道觀看`**，理由見第二節第 1 點。
其餘 43 個量值的回傳值與舊模型逐一比對相同（驗證方式見第七節）。

---

## 二、五個結構性差異（改寫前必讀）

### 1. 🔴 `ALL('fact_影片')` **不等於** 舊的 `ALL('videos')`

這是整份改寫唯一會**算錯數字而不報錯**的地方。

舊模型裡年份、類型、系列都是 `videos` 自己的欄位，所以 `ALL('videos')` 一次清光。
星狀模型裡它們住在維度表，`ALL('fact_影片')` **只清掉事實表自己欄位上的篩選**，
維度表的篩選仍然會沿著關聯propagate 進來。

```dax
-- ❌ 錯：切了年份或系列，分母還是會被篩，比率會虛高
CALCULATE ( SUM ( 'fact_影片'[觀看數] ), ALL ( 'fact_影片' ) )

-- ✅ 對：清掉整個模型的篩選
CALCULATE ( SUM ( 'fact_影片'[觀看數] ), REMOVEFILTERS () )
```

`REMOVEFILTERS()` 不帶參數＝移除模型中所有資料表的篩選。
若你的 Power BI 版本不接受無參數寫法，改用逐表列舉（等價，只是囉唆）：

```dax
CALCULATE (
    SUM ( 'fact_影片'[觀看數] ),
    REMOVEFILTERS ( 'fact_影片' ),  REMOVEFILTERS ( 'dim_日期' ),
    REMOVEFILTERS ( 'dim_影片' ),   REMOVEFILTERS ( 'dim_影片格式' ),
    REMOVEFILTERS ( 'dim_類型' ),   REMOVEFILTERS ( 'dim_系列' ),
    REMOVEFILTERS ( 'dim_業配判定' )
)
```

📌 只有 `對決佔頻道觀看` 一個量值用到「全模型不受篩選」的分母。
`流量貢獻度` 的分母只要解除**門檻**（觀看數欄位）這一個篩選，寫
`REMOVEFILTERS ( 'fact_影片'[觀看數] )` 就對了——**單一資料行的 REMOVEFILTERS 行為與舊版完全相同**。

### 2. `VALUES ( 維度[欄位] )` 回傳的是**維度全體**，不是「有資料的那些」

舊的 `VALUES('videos'[category])` 會跟著年份篩選縮小（2020 年沒拍的類型不會出現）。
星狀模型的單向關聯**不會把事實表的篩選逆向送回維度表**，
所以 `VALUES('dim_類型'[影片類型])` 永遠是 14 個類型。

**本專案不受影響**：三個用到它的量值（`業配率最高類型`／`業配率最低類型`／
`最高｜最低類型業配率`）外面都包著 `FILTER ( ..., CALCULATE ( [僅長片] ) >= 20 )`，
沒有資料的類型 `[僅長片]` 回傳 BLANK、`BLANK >= 20` 為 FALSE，**在同一步就被濾掉了**。

⚠️ 但這條規則要記住：**日後任何新量值若用 `VALUES(維度)` 且沒有樣本量閘門，
會多出空類別。** 需要「只看有資料的」時寫 `CALCULATETABLE ( VALUES ( 'dim_類型'[影片類型] ), 'fact_影片' )`。

### 3. `MAX ( 'dim_日期'[年] )` ≠ 資料的最後一年

日期維度是**連續日曆**，補到當年 12/31。沒有篩選時 `MAX('dim_日期'[年])` 是 2026，
`MAX('dim_日期'[日期])` 是 **2026-12-31**——不是最後一支影片的發布日。

| 量值意圖 | 寫法 |
|---|---|
| 「畫面上這一格是哪一年」（軸／條件式格式） | `MAX ( 'dim_日期'[年] )` ✅ |
| 「資料實際到哪一天」（動態註記、資料規模） | `MAX ( 'fact_影片'[發布日期] )` ✅ |

`可比性`、`長片中位觀看（可比）` 屬前者；`最新年度提醒`、`動態資料規模` 屬後者。
**寫反了不會報錯，只會讓註記在每年 1 月 1 日就開始說謊。**

### 4. `is_sponsored` 現在住在 `dim_業配判定`

篩選條件從 `'videos'[is_sponsored] = TRUE()` 改成 `'dim_業配判定'[是否業配] = TRUE()`。
**型別仍是 True/False**，所以 `= TRUE()` 的寫法原封不動，只換資料表前綴。

⚠️ `配對中位觀看` 裡的 `REMOVEFILTERS` 對象要改成**整張 `'dim_業配判定'`**，
不能只解除 `[業配標籤]`——業配標籤與是否業配現在是同一張表的兩欄，
只解除其中一欄，另一欄的篩選仍會擋住。

### 5. 「無系列」現在是一個真實的維度成員

`dim_系列` 有一列 `系列鍵 = 0`、`系列名稱 = "（無系列）"`、`有系列 = FALSE`，
2,374 支沒有系列歸屬的影片全部指向它。

**`ISBLANK ( series ) || series = "" 這組判斷從此不需要存在**——
`DAX.md` 第七節第 7 點記載的那個坑（比例從 36.6% 變成 100% 且不報錯）
在結構上已經不可能發生。判斷有無系列一律寫 `'dim_系列'[有系列] = TRUE()`。

---

## 三、計算欄位：全部移除

舊模型有 5 個 DAX 計算欄位（含 1 個 Power Query 條件資料行），**新模型一個都不需要**——
它們的內容全部在 `src/build_star.py` 算好、變成維度資料行。

| 舊計算欄位 | 舊寫法的問題 | 新位置 |
|---|---|---|
| `系列世代` | `ALLEXCEPT` 巢狀迭代，每次重新整理都重算 3,506 列；且靠 `ISBLANK \|\| = ""` 擋空白 | `'dim_系列'[系列世代]`（30 列算一次） |
| `是首集` | 同上，另一份 `ALLEXCEPT` | `'dim_影片'[是首集]` |
| `業配標籤` | `IF('videos'[is_sponsored],"業配","非業配")` | `'dim_業配判定'[業配標籤]` |
| `系列_含無系列` | 專門為了讓「無系列」在軸上現形而建 | `'dim_系列'[系列名稱]`（本來就有「（無系列）」） |
| `格式排序`（Power Query 條件資料行） | 建成 DAX 計算欄位會**循環相依**，只能繞到 Power Query 做 | `'dim_影片格式'[格式排序]` |

⚠️ **`格式排序` 這一項順便解掉了 `BUILD.md` 第四節第 ③ 點記載的循環相依。**
排序欄與被排序欄現在是維度表裡兩個獨立的實體資料行，模型看不到任何計算式。

**仍需在 Power BI 做的「依資料行排序」設定**（三處，都是純設定不是公式）：

| 資料表 | 欄位 | 依 |
|---|---|---|
| `dim_影片格式` | `影片格式` | `格式排序` |
| `dim_類型` | `影片類型` | `類型排序` |
| `門檻` | `門檻標籤` | `排序` |
| `資料範圍` | `範圍` | `排序` |

---

## 四、量值清單

> 中斷連結表 `門檻` 與 `資料範圍` 的 `DATATABLE` 定義**完全不變**，
> 照 `DAX.md` 第 2-1、2-2 節原樣建立。它們仍然**不建任何關聯**。

### ▍基礎層

```dax
選定門檻 = SELECTEDVALUE ( '門檻'[門檻值], 0 )
```
> 未改。讀取門檻切片器的選取值，未選取時回傳 0（等於不過濾）。

```dax
僅長片 =
CALCULATE ( COUNTROWS ( 'fact_影片' ), 'dim_影片格式'[影片格式] = "長片" )
```
> 輔助量值，供樣本量閘門使用。**刻意不含觀看數門檻**——
> 「誰上場」不能跟著門檻變（判準見 `系列長片數_不受門檻`）。

```dax
影片數 =
VAR th = [選定門檻]
RETURN CALCULATE ( COUNTROWS ( 'fact_影片' ), KEEPFILTERS ( 'fact_影片'[觀看數] >= th ) )
```
> **所有計數的基礎。** `KEEPFILTERS` 一樣不能省——它保留視覺物件本身的篩選
> （年份、類型），只再疊加門檻條件。
> 📌 `COUNTROWS('fact_影片')` 就是影片數，因為事實表的粒度是**一支影片一列**。

```dax
總觀看 =
VAR th = [選定門檻]
RETURN CALCULATE ( SUM ( 'fact_影片'[觀看數] ), KEEPFILTERS ( 'fact_影片'[觀看數] >= th ) )
```

```dax
中位觀看 =
VAR th = [選定門檻]
RETURN CALCULATE ( MEDIAN ( 'fact_影片'[觀看數] ), KEEPFILTERS ( 'fact_影片'[觀看數] >= th ) )
```
> 全報表的觀看指標一律用這個，不用平均。

```dax
業配影片數 =
VAR th = [選定門檻]
RETURN
    CALCULATE (
        COUNTROWS ( 'fact_影片' ),
        'dim_業配判定'[是否業配] = TRUE (),
        KEEPFILTERS ( 'fact_影片'[觀看數] >= th )
    )
```

```dax
業配率 = DIVIDE ( [業配影片數] + 0, [影片數] )
```
> 🔴 **`+ 0` 不能省。** 業配數為 0 的年份 `COUNTROWS` 回傳 BLANK 不是 0，
> `DIVIDE ( BLANK, n )` 也是 BLANK，**整個年份會被圖表丟掉**
> （症狀：第 5 頁側上圖 2010／2011／2013／2014 四根柱子消失）。
> 分母仍為 0 時 `DIVIDE` 照樣回傳 BLANK，所以沒有影片的年份不會被硬塞進來。
>
> **通則：任何「計數 ÷ 計數」的比率量值，分子都要加 `+ 0`。**

```dax
長片數 = CALCULATE ( [影片數], 'dim_影片格式'[影片格式] = "長片" )
```

```dax
Shorts 數 = CALCULATE ( [影片數], 'dim_影片格式'[影片格式] = "Shorts" )
```

---

### ▍第 1 頁 封面

```dax
動態資料規模 =
"分析母體 " & FORMAT ( COUNTROWS ( 'fact_影片' ), "#,0" ) & " 支影片｜"
    & YEAR ( MIN ( 'fact_影片'[發布日期] ) ) & "–"
    & YEAR ( MAX ( 'fact_影片'[發布日期] ) )
    & "｜每日自動更新"
```
> 🔴 **年份取自事實表不是日期維度**（第二節第 3 點）。
> 寫成 `MIN('dim_日期'[年])` 會固定回傳 2010–2026，因為日曆補到年底，
> 副標題會在資料還沒到 2026 年底時就宣稱涵蓋整年。

---

### ▍第 3 頁 產能結構

```dax
長片中位觀看 = CALCULATE ( [中位觀看], 'dim_影片格式'[影片格式] = "長片" )
```

```dax
可比性 = IF ( MAX ( 'dim_日期'[年] ) <= 2023, "可比", "仍在累積" )
```
> 供條件式格式與工具提示使用。這裡**要用日期維度**——它問的是「畫面上這格是哪一年」。
> 📌 `'dim_日期'[觀看可比性]` 這個資料行存的是同一套判準，可直接當工具提示欄位；
> 量值保留是因為它要在**沒有年份軸**的情境（KPI 卡）也回答得出來。

```dax
長片中位觀看（可比） =
IF ( MAX ( 'dim_日期'[年] ) <= 2023, [長片中位觀看] )
```

```dax
長片中位觀看（仍在累積） =
IF ( MAX ( 'dim_日期'[年] ) >= 2023, [長片中位觀看] )
```
> ⚠️ **2023 兩邊都給值是刻意的**，否則兩條線會在 2023／2024 之間斷開一格。
> ⚠️ 這兩個回傳 `BLANK()` 是刻意的，折線圖靠它判斷「這年沒有資料點」而斷線。
> **不要改成回傳 `""`**（見 `BUILD.md` 3-6 的判準）。

```dax
流量貢獻度 =
VAR th = [選定門檻]
VAR Filtered =
    CALCULATE ( SUM ( 'fact_影片'[觀看數] ), KEEPFILTERS ( 'fact_影片'[觀看數] >= th ) )
VAR Total =
    CALCULATE ( SUM ( 'fact_影片'[觀看數] ), REMOVEFILTERS ( 'fact_影片'[觀看數] ) )
RETURN DIVIDE ( Filtered, Total )
```
> 🔴 分母**只解除觀看數這一欄**的篩選，保留年份等其他篩選——這正是舊版
> `ALL('videos'[view_count])` 的行為，**單一資料行的 `REMOVEFILTERS` 在星狀模型下行為不變**。
> 不要順手改成 `REMOVEFILTERS('fact_影片')`，那會連年份一起清掉。
>
> 🔴 `VAR` 的名稱只能用 ASCII。寫成 `VAR 篩選後` 會回報「權杖無效」。
> 點「100 萬↑」時顯示 46.0%，代表**一成影片撐起近半流量**。

---

### ▍第 4 頁 投放規模

```dax
業配率（依資料範圍） =
VAR sel = SELECTEDVALUE ( '資料範圍'[範圍], "僅長片（有效範圍）" )
RETURN
    IF (
        sel = "僅長片（有效範圍）",
        CALCULATE ( [業配率], 'dim_影片格式'[影片格式] = "長片" ),
        [業配率]
    )
```
> **全報表最重要的一個互動。** 切到「全部」時業配率從 53.0% 掉到 30.4%。
> 預設值設為「僅長片」，確保未選取時顯示的是有效數字。

```dax
業配影片數（依資料範圍） =
VAR sel = SELECTEDVALUE ( '資料範圍'[範圍], "僅長片（有效範圍）" )
RETURN
    IF (
        sel = "僅長片（有效範圍）",
        CALCULATE ( [業配影片數], 'dim_影片格式'[影片格式] = "長片" ),
        [業配影片數]
    )
```

```dax
偵測業配數_長片 = CALCULATE ( [業配影片數], 'dim_影片格式'[影片格式] = "長片" )
```
> KPI2 用這個，不是 `[業配影片數（依資料範圍）]`。
> **KPI2 與 KPI3 是一對（下限 vs 推估），必須同母體**，而 KPI3 只能是長片。

```dax
格式召回率 = SELECTEDVALUE ( 'dim_影片格式'[業配召回率] )
```
> ⭐ **本次改版最有價值的一個改寫。** 舊版是
> `SWITCH ( SELECTEDVALUE('videos'[video_format]), "長片", 0.763, "Shorts", 0.146, BLANK() )`
> ——把人工抽樣得到的常數寫死在量值裡。
>
> 現在召回率是 `dim_影片格式` 的一個資料行，來源是 `rules/format_audit.csv`。
> **重做抽樣只要改那張 CSV 一個檔**，不必翻量值。
>
> 行為完全相同：格式不只一個時 `SELECTEDVALUE` 回傳 BLANK，與舊版 `SWITCH` 落到
> `BLANK()` 分支一致。

```dax
推估實際業配_依格式 =
VAR r = [格式召回率]
RETURN IF ( NOT ISBLANK ( r ), ROUND ( DIVIDE ( [業配影片數], r ), 0 ) )
```

```dax
推估實際業配 =
CALCULATE ( [推估實際業配_依格式], 'dim_影片格式'[影片格式] = "長片" )
```
> 🔴 **引用 `[推估實際業配_依格式]`，不要在這裡再寫一次常數。**
> 2026-08-05 踩過：兩處各寫一次召回率，改了一處另一處沒改，
> 側上圖總長 674、KPI3 卻顯示 603，同一頁兩個數字對不上。
> 現在召回率只存在 `rules/format_audit.csv` 一個地方，結構上不可能再漂開。

```dax
沒測到的業配數 = [推估實際業配_依格式] - [業配影片數]
```
> 堆疊橫條圖的第二段。兩段加起來就是推估實際總數。

```dax
判定精確率_長片 =
CALCULATE (
    SELECTEDVALUE ( 'dim_影片格式'[業配精確率] ),
    'dim_影片格式'[影片格式] = "長片"
)
```
> ⭐ 同樣從硬編碼改成讀維度。回傳文字 **「22 / 22」**。
>
> **回傳文字而不是 100%**：分母只有 22 支，寫「100%」會被讀成「這套判定不會出錯」，
> 而區間下界其實只有 85.1%。把分母攤在檯面上，與第 6 頁 KPI4 顯示「4 / 4」同一條原則。
>
> 📌 想在工具提示補上信賴區間，直接拉 `'dim_影片格式'[業配精確率信賴區間]`，
> 不需要新量值。

---

### ▍第 5 頁 投放偏好

```dax
類型業配率 = CALCULATE ( [業配率], 'dim_影片格式'[影片格式] = "長片" )
```
> ⚠️ 名字只反映第一個用途，內容其實是「限長片的業配率」。
> **第 5 頁主圖、側上圖、側下圖三張都用它**（分別按類型、年份、系列切）。

```dax
業配率最高類型 =
VAR t =
    ADDCOLUMNS (
        FILTER ( VALUES ( 'dim_類型'[影片類型] ), CALCULATE ( [僅長片] ) >= 20 ),
        "@率", [類型業配率]
    )
RETURN MAXX ( TOPN ( 1, t, [@率] ), 'dim_類型'[影片類型] )
```
> **`>= 20` 是刻意設的樣本門檻。** 不設的話財經／投資（僅 6 支長片）
> 可能因比例偶然偏高而登上 KPI。目前結果是「挑戰／企劃 45.7%」。
>
> 📌 `VALUES('dim_類型'[影片類型])` 現在回傳全部 14 個類型（第二節第 2 點），
> 但 `>= 20` 這道閘門會把沒有樣本的類型連同 BLANK 一起濾掉，**結果不變**。

```dax
業配率最低類型 =
VAR t =
    ADDCOLUMNS (
        FILTER (
            VALUES ( 'dim_類型'[影片類型] ),
            CALCULATE ( [僅長片] ) >= 20 && [樣本數_長片] > 0
        ),
        "@率", [類型業配率],
        "@樣本", [樣本數_長片]
    )
RETURN MINX ( TOPN ( 1, t, [@率], ASC, [@樣本], DESC ), 'dim_類型'[影片類型] )
```
> 🔴 **`&& [樣本數_長片] > 0` 不能省**：`[僅長片]` 不含觀看數門檻（刻意的），
> 所以門檻拉高時會有類型**通過 `>= 20` 閘門、卻一支影片都不剩**。
> 那種類型的 `[類型業配率]` 是 BLANK，而 `TOPN ( ..., ASC )` 把 BLANK 當成比 0 還小，
> **它會被選成「最低」**。症狀：門檻點「100 萬」時 KPI2 顯示電競賽事，
> 但電競賽事破百萬的長片是 0 支。
>
> 🔴 **`[@樣本], DESC` 這組並列規則不能省**：100 萬門檻下有三個類型並列 0.0%，
> `TOPN` 只給一組排序時取誰是任意的。第二組排序設成「樣本數由多到少」之後，
> 並列時固定取證據最紮實的那一個（100 萬＝玩具/收藏 17 支）。

```dax
最高類型業配率 =
VAR t =
    ADDCOLUMNS (
        FILTER ( VALUES ( 'dim_類型'[影片類型] ), CALCULATE ( [僅長片] ) >= 20 ),
        "@率", [類型業配率]
    )
RETURN MAXX ( t, [@率] )
```

```dax
最低類型業配率 =
VAR t =
    ADDCOLUMNS (
        FILTER ( VALUES ( 'dim_類型'[影片類型] ), CALCULATE ( [僅長片] ) >= 20 ),
        "@率", [類型業配率]
    )
RETURN MINX ( t, [@率] )
```
> 這兩個回傳的是**比率**，KPI 卡不直接用，是 `類型間差距` 的中間量。

```dax
類型間差距 =
FORMAT ( ( [最高類型業配率] - [最低類型業配率] ) * 100, "0.0" ) & "pp"
```
> 🔴 **必須顯示 `pp` 不是 `%`**：兩個百分比相減得到的是**百分點差**。
> 回傳文字是因為這個版本的 Power BI「格式」下拉沒有「自訂」選項。

```dax
樣本數_長片 = CALCULATE ( [影片數], 'dim_影片格式'[影片格式] = "長片" )
```

```dax
系列長片數_不受門檻 =
CALCULATE ( COUNTROWS ( 'fact_影片' ), 'dim_影片格式'[影片格式] = "長片" )
```
> 🔴 **與 `[系列正片數]` 只差一件事：沒有內建觀看數門檻。**
> 凡是用來決定「**哪些系列上場**」的篩選條件，都必須用這個版本。
>
> **判準：X 軸／Y 軸要跟著門檻變（那是演出的重點），但「誰上場」不能跟著變。**
>
> 📌 `'dim_系列'[系列長片集數_全期]` 存的是同一個數字的**全期靜態值**，
> 只供切片器排序與查核，**不受任何篩選影響**——需要跟著年份／類型變動時
> 一律用這個量值，不要拉那個資料行。

```dax
系列業配密度 = IF ( [系列長片數_不受門檻] >= 10, [類型業配率] )
```
> ⭐ 第 5 頁側下圖的 X 軸。格式：**百分比，1 位小數**。
>
> **為什麼門檻要包進量值**：Power BI 的「前 N 項」**先算**，
> 它在量值篩選生效前就挑出前 6 名，門檻才把前五個砍掉，實測只剩 1 列。
> 不合格者回傳 BLANK 之後 **Top N 不會挑到 BLANK**，先後順序的問題就消失了。

```dax
主圖色_最高標紅 =
VAR topCat =
    CALCULATE ( [業配率最高類型], ALLSELECTED ( 'dim_類型'[影片類型] ) )
RETURN
    IF ( SELECTEDVALUE ( 'dim_類型'[影片類型] ) = topCat, "#FF0000", "#A0A0A0" )
```
> 🔴 **`ALLSELECTED` 不能省**：不加的話**每一根長條都會變紅**，
> 因為 `[業配率最高類型]` 內部的 `VALUES` 遵守當下的篩選內容，
> 在「遊戲實況」那一列上只回傳「遊戲實況」，條件永遠成立。
>
> **用 `ALLSELECTED` 不用 `ALL`**：逐項加入類型的演出中，`ALL` 會把紅色標在
> 一個沒顯示的類型上，畫面上一根紅的都沒有。

---

### ▍第 6 頁 接棒動能

```dax
系列正片數 = CALCULATE ( [影片數], 'dim_影片格式'[影片格式] = "長片" )
```
> **系列集數一律用正片計算。** Shorts 多為同標題的宣傳短片，計入會虛增集數。

```dax
系列總觀看 = CALCULATE ( [總觀看], 'dim_影片格式'[影片格式] = "長片" )
```
> 第 6 頁氣泡圖的「大小」欄位。直接用 `[總觀看]` 會混入 Shorts，
> 與 X 軸的 `[系列正片數]`（已限長片）口徑不一致。

```dax
對決佔頻道觀看 =
DIVIDE (
    CALCULATE ( SUM ( 'fact_影片'[觀看數] ), 'dim_系列'[系列名稱] = "Joe是要對決" ),
    CALCULATE ( SUM ( 'fact_影片'[觀看數] ), REMOVEFILTERS () )
)
```
> 🔴 **這是全部量值裡唯一一個「照抄舊寫法會算錯」的**（第二節第 1 點）。
> 舊版分母是 `ALL('videos')`，在扁平表裡一次清光所有篩選；
> 星狀模型裡 `ALL('fact_影片')` 清不掉維度表送進來的篩選，
> 切了年份或系列分母就會縮小，比率虛高且**不會報錯**。
>
> 分母改用 `REMOVEFILTERS()` 之後，即使切了年份這張卡仍顯示
> 「對決佔全頻道 21.1%」的固定事實。
>
> ⚠️ 第 6 頁的「年份區間」對這張卡仍要**關閉編輯互動**——
> 分母鎖住了，**分子還是會被篩**，拉到 2025 起會變 2.3%。

```dax
長片系列化比例 =
VAR LongTotal =
    CALCULATE ( COUNTROWS ( 'fact_影片' ), 'dim_影片格式'[影片格式] = "長片" )
VAR WithSeries =
    CALCULATE (
        COUNTROWS ( 'fact_影片' ),
        'dim_影片格式'[影片格式] = "長片",
        'dim_系列'[有系列] = TRUE ()
    )
RETURN DIVIDE ( WithSeries + 0, LongTotal )
```
> 🔴 **`WithSeries + 0` 不能省**：2010 年有 200 支長片、有系列歸屬 0 支，
> `CALCULATE` 篩不到任何列時回傳 BLANK，`DIVIDE ( BLANK, 200 )` 也是 BLANK，
> **第 6 頁側上圖的 2010 整年會消失**，X 軸從 2011 開始。
>
> ⭐ **判斷「有沒有系列」改用 `'dim_系列'[有系列]`。**
> 舊版先寫 `NOT ISBLANK('videos'[series])`（會踩空字串的坑），
> 後來改用 `系列世代 <> "無系列"` 繞過去。現在維度表有一個真實的布林欄位，
> **兩種寫法的歷史包袱一次清掉**。
>
> **刻意只算長片。** 含 Shorts 的比例會混入格式效應，把 81%→38% 誇大成 80%→19%。

```dax
新系列中位觀看 =
CALCULATE (
    [中位觀看],
    'dim_系列'[系列世代] = "新系列",
    'dim_影片格式'[影片格式] = "長片"
)
```

```dax
新系列業配率 =
CALCULATE (
    [業配率],
    'dim_系列'[系列世代] = "新系列",
    'dim_影片格式'[影片格式] = "長片"
)
```
> ⚠️ **這個量值已不再擔任 KPI**（自助抽樣 95% 信賴區間跨越零，不能下結論）。
> 保留供工具提示或備答用。

```dax
首集就有業配 =
VAR tot =
    CALCULATE (
        COUNTROWS ( 'fact_影片' ),
        'dim_影片'[是首集] = TRUE (),
        'dim_系列'[系列世代] = "新系列"
    )
VAR hit =
    CALCULATE (
        COUNTROWS ( 'fact_影片' ),
        'dim_影片'[是首集] = TRUE (),
        'dim_系列'[系列世代] = "新系列",
        'dim_業配判定'[是否業配] = TRUE ()
    )
RETURN hit & " / " & tot
```
> ⭐ 第 6 頁 KPI4。回傳文字「**4 / 4**」。
>
> 這是**二元類別觀察**不是比率比較，不需要信賴區間：
> 既有系列 22 個裡只有 2 個第一集就有業配，新系列 4 個全部都有。
> 若機率相同，連續四次命中的機率是 **0.01%**。

```dax
首次業配集數 =
VAR firstSponsored =
    CALCULATE (
        MIN ( 'fact_影片'[發布日期] ),
        'dim_業配判定'[是否業配] = TRUE (),
        'dim_影片格式'[影片格式] = "長片"
    )
RETURN
    IF (
        NOT ISBLANK ( firstSponsored ),
        CALCULATE (
            COUNTROWS ( 'fact_影片' ),
            'dim_影片格式'[影片格式] = "長片",
            'fact_影片'[發布日期] <= firstSponsored
        )
    )
```
> 在以 `'dim_系列'[系列名稱]` 為軸的圖表裡算出「到第一支業配為止累計第幾集」。
> 從無業配的系列回傳空白。
>
> 📌 日期比較用的是**事實表的 `[發布日期]`**，不是 `'dim_日期'[日期]`。
> 對事實表欄位加篩選不會動到日期維度上既有的年份篩選，行為與舊模型一致。

```dax
首次業配出現在第幾集 =
VAR eps = [系列長片數_不受門檻]
VAR gen = SELECTEDVALUE ( 'dim_系列'[系列世代] )
RETURN IF ( gen = "新系列" || eps >= 50, [首次業配集數] )
```
> 控制第 6 頁側下圖顯示哪些系列：**全部新系列 ＋ 集數 ≥50 的既有系列**，共 8 列。
> `≥40` 時是 9 列，205px 的卡片塞不下，**被推到摺線下的正好是《Joeman開箱去》第 91 集**
> ——這頁收尾台詞最重要的那個數字。
>
> 🔴 **`eps` 用 `[系列長片數_不受門檻]` 不是 `[系列正片數]`（2026-08-10 修正）。**
> 後者內建觀看數門檻，會讓「誰上場」跟著門檻變——實測第 6 頁門檻點「100 萬」時
> **8 列只剩 5 列**。`[首次業配集數]` 本來就不含門檻（「第幾集」是歷史事實），
> 換掉 `eps` 之後這張圖在任何門檻下都是同樣 8 列、同樣的集序。
>
> ⚠️ 同一個修正也要套到**第 6 頁主圖的視覺層級篩選**（`>= 3` 那個條件），
> 詳見 `BUILD.md` 第 6 頁主圖那一節的實測對照表。
>
> ⭐ `SELECTEDVALUE('dim_系列'[系列世代])` 在星狀模型下比舊版更穩：
> 系列世代現在是**系列的屬性**，一個系列必然只有一個世代，
> 舊模型裡它是影片的屬性、靠同系列每列算出同值才碰巧成立。

---

### ▍孤兒與備用量值

```dax
最新年度提醒 =
VAR maxPub = MAX ( 'fact_影片'[發布日期] )
RETURN
    IF (
        YEAR ( maxPub ) = YEAR ( TODAY () ),
        FORMAT ( maxPub, "yyyy" ) & " 年僅 1–" & FORMAT ( MONTH ( maxPub ), "0" ) & " 月",
        ""
    )
```
> 🔴 **`MAX` 一定要取 `'fact_影片'[發布日期]`，不能取 `'dim_日期'[日期]`**（第二節第 3 點）。
> 日期維度補到 12/31，取到那裡會永遠顯示「1–12 月」。
>
> **回傳 `""` 而不是 `BLANK()`**：卡片視覺遇到 `BLANK()` 會顯示「(空白)」四個字。
> ⚠️ `""` 只能用在純文字顯示的量值，數值型量值回傳空字串會讓整欄型別變文字。

```dax
配對中位觀看 =
VAR sN =
    CALCULATE (
        COUNTROWS ( 'fact_影片' ),
        REMOVEFILTERS ( 'dim_業配判定' ),
        'dim_業配判定'[是否業配] = TRUE (),
        'dim_影片格式'[影片格式] = "長片"
    )
VAR nN =
    CALCULATE (
        COUNTROWS ( 'fact_影片' ),
        REMOVEFILTERS ( 'dim_業配判定' ),
        'dim_業配判定'[是否業配] = FALSE (),
        'dim_影片格式'[影片格式] = "長片"
    )
RETURN IF ( sN >= 30 && nN >= 30, [中位觀看] )
```
> 🔴 **`REMOVEFILTERS` 的對象改成整張 `'dim_業配判定'`**（第二節第 4 點）。
> 舊版寫 `REMOVEFILTERS('videos'[業配標籤])` 是因為那時業配標籤是 `videos` 的計算欄位；
> 現在業配標籤與是否業配同表，只解除一欄另一欄仍會擋住，兩個 `VAR` 會永遠只數到自己那組。
>
> ⚠️ 本量值服務的是 2026-08-05 已換題的第 4 頁側上圖（方案 B），
> 目前**沒有視覺引用**。保留供備案使用。

🔴 **`判定精確率`（無後綴的那個）不要建。**
舊模型的 `判定精確率 = 0.968` 是**混合格式**樣本算出來的（27 長片 ＋ 8 Shorts），
拆出長片專屬是 22/22。它已被 `[判定精確率_長片]` 完全取代、沒有任何視覺引用。
**若舊 .pbix 裡還留著，直接刪除**——兩個名字只差三個字卻給不同答案，是最容易誤用的一種殘留。

---

### ▍每日快照（選用，目前未載入）

`fact_每日快照` 已由 `src/build_star.py` 產出，但**報表目前沒有任何視覺用到它**。
若日後要做 `BUILD.md` 8-5 節的成長率工具提示，載入後可用：

```dax
最新快照觀看 =
VAR maxSnap = MAX ( 'fact_每日快照'[快照日期] )
RETURN CALCULATE ( SUM ( 'fact_每日快照'[觀看數] ), 'fact_每日快照'[快照日期] = maxSnap )
```

```dax
區間觀看成長 =
VAR maxSnap = CALCULATE ( MAX ( 'fact_每日快照'[快照日期] ), REMOVEFILTERS ( 'dim_日期' ) )
VAR minSnap = CALCULATE ( MIN ( 'fact_每日快照'[快照日期] ), REMOVEFILTERS ( 'dim_日期' ) )
VAR vEnd = CALCULATE ( SUM ( 'fact_每日快照'[觀看數] ), 'fact_每日快照'[快照日期] = maxSnap )
VAR vStart = CALCULATE ( SUM ( 'fact_每日快照'[觀看數] ), 'fact_每日快照'[快照日期] = minSnap )
RETURN vEnd - vStart
```

⚠️ 載入 `fact_每日快照` 之前先看第七節第 2 點——它與 `dim_日期`、`dim_影片`
各有一條關聯，設錯基數會讓模型出現模稜兩可的路徑。

---

## 五、舊 → 新 對照總表

**44 個量值全部有對應，沒有任何一個被刪除。**

| # | 量值 | 改動 | 舊寫法的關鍵片段 → 新寫法 |
|---|---|---|---|
| 1 | `選定門檻` | 無 | — |
| 2 | `僅長片` | A+B | `'videos'` → `'fact_影片'`；格式條件改指 `'dim_影片格式'` |
| 3 | `影片數` | A | `'videos'[view_count]` → `'fact_影片'[觀看數]` |
| 4 | `總觀看` | A | 同上 |
| 5 | `中位觀看` | A | 同上 |
| 6 | `業配影片數` | A+B | `'videos'[is_sponsored]` → `'dim_業配判定'[是否業配]` |
| 7 | `業配率` | 無 | 只引用量值 |
| 8 | `長片數` | B | 格式條件改指維度 |
| 9 | `Shorts 數` | B | 同上 |
| 10 | `動態資料規模` | A🔴 | `MIN('videos'[published_year])` → `YEAR(MIN('fact_影片'[發布日期]))` |
| 11 | `長片中位觀看` | B | — |
| 12 | `可比性` | A | `MAX('videos'[published_year])` → `MAX('dim_日期'[年])` |
| 13 | `長片中位觀看（可比）` | A | 同上 |
| 14 | `長片中位觀看（仍在累積）` | A | 同上 |
| 15 | `流量貢獻度` | A | `ALL('videos'[view_count])` → `REMOVEFILTERS('fact_影片'[觀看數])` |
| 16 | `業配率（依資料範圍）` | B | — |
| 17 | `業配影片數（依資料範圍）` | B | — |
| 18 | `偵測業配數_長片` | B | — |
| 19 | `格式召回率` | **C** ⭐ | `SWITCH(...,0.763,...)` → `SELECTEDVALUE('dim_影片格式'[業配召回率])` |
| 20 | `推估實際業配_依格式` | 無 | 只引用量值 |
| 21 | `推估實際業配` | B | — |
| 22 | `沒測到的業配數` | 無 | 只引用量值 |
| 23 | `判定精確率_長片` | **C** ⭐ | 常數 `"22 / 22"` → `'dim_影片格式'[業配精確率]` |
| 24 | `類型業配率` | B | — |
| 25 | `業配率最高類型` | A | `VALUES('videos'[category])` → `VALUES('dim_類型'[影片類型])` |
| 26 | `業配率最低類型` | A | 同上 |
| 27 | `最高類型業配率` | A | 同上 |
| 28 | `最低類型業配率` | A | 同上 |
| 29 | `類型間差距` | 無 | 只引用量值 |
| 30 | `樣本數_長片` | B | — |
| 31 | `系列長片數_不受門檻` | A+B | — |
| 32 | `系列業配密度` | 無 | 只引用量值 |
| 33 | `主圖色_最高標紅` | A | `'videos'[category]` → `'dim_類型'[影片類型]` |
| 34 | `系列正片數` | B | — |
| 35 | `系列總觀看` | B | — |
| 36 | `對決佔頻道觀看` | **A🔴** | `ALL('videos')` → `REMOVEFILTERS()`；系列條件改指 `'dim_系列'` |
| 37 | `長片系列化比例` | A+B ⭐ | `'videos'[系列世代] <> "無系列"` → `'dim_系列'[有系列] = TRUE()` |
| 38 | `新系列中位觀看` | B | 世代與格式條件都改指維度 |
| 39 | `新系列業配率` | B | 同上 |
| 40 | `首集就有業配` | B | `'videos'[是首集]` → `'dim_影片'[是首集]` |
| 41 | `首次業配集數` | A+B | `'videos'[published_date]` → `'fact_影片'[發布日期]` |
| 42 | `首次業配出現在第幾集` | B🔴 | `SELECTEDVALUE('videos'[系列世代])` → `'dim_系列'[系列世代]`；**`eps` 改用 `[系列長片數_不受門檻]`**（2026-08-10，見第四節） |
| 43 | `最新年度提醒` | A🔴 | `MAX('videos'[published_date])` → `MAX('fact_影片'[發布日期])` |
| 44 | `配對中位觀看` | B🔴 | `REMOVEFILTERS('videos'[業配標籤])` → `REMOVEFILTERS('dim_業配判定')` |

🔴＝改法不只是換前綴，寫錯會算錯數字而不報錯。共 **5 個**：#10、#36、#43、#44，
以及 #15（`流量貢獻度` 若把單欄 `REMOVEFILTERS` 誤寫成整表）。

**已於 2026-08-05 移除、不需改寫的三個**：`業配中位觀看`、`非業配中位觀看`、`觀看差距`。
它們沒有任何視覺引用，新模型直接不建。

**`DAX.md` 第五節的格式設定表對照**：量值名稱一個都沒改，格式設定原樣沿用。

---

## 六、量值使用狀態：哪些還在用

> 盤點依據：`BUILD.md` 第五節逐頁施工表格裡的**欄位指派**（KPI 的「值」、圖表的軸／圖例／
> 大小／工具提示／資料色彩／視覺層級篩選），加上量值彼此的引用關係。
> 盤點日 2026-08-10。

### 6-1 直接放在視覺上（26 個）

| 頁 | 位置 | 量值 |
|---|---|---|
| 1 | 卡片 | `動態資料規模` |
| 3 | KPI ×4 | `影片數`、`長片數`、`Shorts 數`、`長片中位觀看` |
| 3 | 主圖／側下 Y 軸 | `影片數` |
| 3 | 側上 Y 軸 | `長片中位觀看（可比）`、`長片中位觀看（仍在累積）` |
| 3 | 側上 工具提示 | `可比性` |
| 4 | KPI ×4 | `業配率（依資料範圍）`、`偵測業配數_長片`、`推估實際業配`、`判定精確率_長片` |
| 4 | 主圖 柱／線 | `業配影片數（依資料範圍）`、`業配率（依資料範圍）` |
| 4 | 主圖 註記卡 | `最新年度提醒` |
| 4 | 側上 X 軸 | `業配影片數`、`沒測到的業配數` |
| 4 | 側下 X 軸 | `業配影片數` |
| 5 | KPI ×4 | `業配率最高類型`、`業配率最低類型`、`類型間差距`、`樣本數_長片` |
| 5 | 主圖 X 軸／資料色彩 | `類型業配率`、`主圖色_最高標紅` |
| 5 | 側上 Y 軸 | `類型業配率` |
| 5 | 側下 X 軸 | `系列業配密度` |
| 6 | KPI ×4 | `對決佔頻道觀看`、`長片系列化比例`、`新系列中位觀看`、`首集就有業配` |
| 6 | 主圖 X／Y／大小 | `系列正片數`、`長片中位觀看`、`系列總觀看` |
| 6 | 主圖 視覺層級篩選 | `系列長片數_不受門檻`（🔴 2026-08-10 由 `系列正片數` 改） |
| 6 | 側上 Y 軸／註記卡 | `長片系列化比例`、`最新年度提醒` |
| 6 | 側下 X 軸／排序 | `首次業配出現在第幾集` |

### 6-2 沒上視覺但**不能刪**：11 個中間量

刪掉會連鎖壞掉——它們是別的量值的零件。

| 量值 | 被誰引用 |
|---|---|
| `選定門檻` | `影片數`／`總觀看`／`中位觀看`／`業配影片數`／`流量貢獻度` |
| `僅長片` | 四個「最高／最低類型」量值的樣本量閘門 |
| `中位觀看` | `長片中位觀看`／`新系列中位觀看`／`配對中位觀看` |
| `總觀看` | `系列總觀看` |
| `業配率` | `業配率（依資料範圍）`／`類型業配率`／`新系列業配率` |
| `格式召回率` | `推估實際業配_依格式` |
| `推估實際業配_依格式` | `推估實際業配`（KPI3）／`沒測到的業配數`（側上圖） |
| `最高類型業配率` | `類型間差距` |
| `最低類型業配率` | `類型間差距` |
| `系列長片數_不受門檻` | `系列業配密度`／`首次業配出現在第幾集`／第 6 頁主圖篩選 |
| `首次業配集數` | `首次業配出現在第幾集` |

📌 `影片數`、`樣本數_長片`、`系列正片數`、`業配率最高類型`、`長片中位觀看` 這幾個
**既在視覺上、也被別的量值引用**，已列在 6-1。

### 6-3 孤兒：沒有視覺、也沒有被任何量值引用（3 個）

| 量值 | 原本服務什麼 | 為什麼變孤兒 | 處置 |
|---|---|---|---|
| `流量貢獻度` | 第 3 頁「一成影片撐起近半流量」 | **從來沒實作**。`BUILD.md` 8-2b 已標為孤兒並算好座標（高 40、寬 220、橫 780、縱 350、15pt、`#717171`） | ✅ **保留，建議補上視覺**。100 萬門檻時 46.2%，是門檻切片器唯一講得出金句的地方 |
| `新系列業配率` | 第 6 頁 KPI4 | 2026-08-02 方案丁改用 `首集就有業配`——原論點的自助抽樣 95% 信賴區間 [−4.7, +40.0]pp **跨越零**，照第七節的標準不能下結論 | ✅ **保留當備答**。被問「新系列業配率多少」時能當場答 73.7%，但**不可拿它下結論** |
| `配對中位觀看` | 第 4 頁側上圖（方案 B） | 2026-08-05 側上圖換題成「業配偵測缺口」，方案 B 整組沒用上 | ⚠️ **保留但不要拿它設計新圖表**——它比較的是業配 vs 非業配的觀看，撞 `HANDOFF.md` 第零章 |

### 6-4 🔴 舊 .pbix 裡要**主動刪掉**的殘留（4 個）

新模型從頭重建的話這些根本不會出現；若你是在舊檔上改，記得清掉。

| 量值 | 為什麼要刪 |
|---|---|
| `判定精確率`（無後綴） | 舊值 0.968 是**混合格式**樣本（27 長片 ＋ 8 Shorts）。與 `判定精確率_長片` 只差三個字卻給不同答案 |
| `業配中位觀看` | 2026-08-05 整組移除，依 `HANDOFF.md` 第零章「業配是否帶來流量」不成立也不需要討論 |
| `非業配中位觀看` | 同上 |
| `觀看差距` | 同上 |

同時要刪的還有 **5 個計算欄位**（`系列世代`、`是首集`、`業配標籤`、`系列_含無系列`、
`格式排序`）——它們的內容已經在維度表裡，理由見第三節。

### 6-5 選用、目前刻意未載入（2 個）

`最新快照觀看`、`區間觀看成長` 服務 `fact_每日快照`，報表目前沒載入那張表。
要做 `BUILD.md` 8-5 節的成長率工具提示時再建，**建之前先看第七節第 2 點的關聯基數設定**。

---

## 七、星狀模型專屬的坑

### 1. 🔴 關聯方向一律「單一」，不要開雙向

六條關聯全部設成**多對一、交叉篩選方向「單一」**（維度篩事實，事實不篩維度）。

開雙向會發生兩件事：① `VALUES(維度)` 的行為變得依賴篩選路徑、難以推理；
② 載入 `fact_每日快照` 之後模型會出現**模稜兩可的關聯路徑**而拒絕啟用關聯。

### 2. 🔴 `fact_影片` ↔ `dim_影片` 是 1 對 1，基數要手動改成「多對一」

事實表一支影片一列、影片維度也是一支影片一列，Power BI 會自動偵測成 **一對一**，
而一對一關聯的交叉篩選方向**預設是雙向且不能關閉**。

那條雙向關聯會造出一條路徑：
`dim_日期 → fact_影片 → dim_影片 → fact_每日快照`，
與 `dim_日期 → fact_每日快照` 這條直達路徑打架，Power BI 會停用其中一條。

👉 **在「管理關聯」裡把基數手動改成 `多對一 (*:1)`、方向改「單一」。**
Power BI 允許這樣設定（唯一性只是資料現況，不是宣告的限制）。

⚠️ 若你決定**不載入** `fact_每日快照`，這個問題不會發生，但還是建議照設——
日後要加成長率工具提示時就不必回頭改模型。

### 3. `dim_日期` 要標記為日期資料表

`模型` → 選 `dim_日期` → `表格工具` → **`標記為日期資料表`** → 日期資料行選 `日期`。

不標記的話時間智慧函式（`DATEADD`、`SAMEPERIODLASTYEAR` 等）行為不保證。
目前的量值一個都沒用到時間智慧，但標記是零成本的保險。

⚠️ 標記前要確認 `日期` 欄**沒有重複值、沒有空值、日期連續**——
`build_star.py` 產生的是連續日曆，這三點必然成立。

### 4. 年份切片器改用 `'dim_日期'[年]`

第 3、4、6 頁的「年份區間」（介於）切片器欄位從 `videos[published_year]`
改成 `'dim_日期'[年]`。**樣式、座標、預設範圍 2010–2026 全部不變。**

⚠️ 日期維度補到 2026-12-31，但**切片器的「介於」上限仍是 2026**（它列的是年份不是日期），
所以畫面完全一樣。

### 5. 切片器與軸的欄位對照

| 用途 | 舊欄位 | 新欄位 |
|---|---|---|
| 年份軸／年份區間切片器 | `videos[published_year]` | `'dim_日期'[年]` |
| 影片格式切片器／堆疊圖圖例 | `videos[video_format]` | `'dim_影片格式'[影片格式]` |
| 類型軸／類型切片器 | `videos[category]` | `'dim_類型'[影片類型]` |
| 系列軸／系列切片器 | `videos[series]`、`videos[系列_含無系列]` | `'dim_系列'[系列名稱]` |
| 業配／非業配軸 | `videos[業配標籤]` | `'dim_業配判定'[業配標籤]` |
| 觀看數門檻切片器 | `門檻[門檻標籤]` | 不變 |
| 資料範圍切片器 | `資料範圍[範圍]` | 不變 |

📌 **`'dim_系列'[系列名稱]` 已內含「（無系列）」這個成員**，
所以第 4 頁側上圖不再需要 `系列_含無系列` 那個計算欄位。

### 6. 編輯互動的設定完全不變

`DAX.md` 第六節那張表列的五處封鎖照舊——星狀化不改變任何視覺的篩選來源，
只改變欄位住在哪張表。

⚠️ 唯一要重設的是**封鎖對象的欄位換了資料表之後，編輯互動設定會保留**，
但若你是重新拉一次視覺物件，記得回頭把互動再關一次。

---

## 八、改寫後的驗收步驟

### 8-1 先跑程式端的驗證（不必開 Power BI）

```bash
python -m src.verify_star
```

它做兩件事：

1. **數值等價驗證**——用 pandas 同時以「舊扁平表」與「新星狀表」復算 30+ 個關鍵指標
   （含門檻 0／10 萬／100 萬三種情境），逐項比對。任一項不符即以非零狀態碼結束。
2. **DAX 靜態引用檢查**——把本文件所有 ```dax 區塊裡的 `'資料表'[資料行]` 全部抓出來，
   對照實際 CSV 的欄位名。打錯一個字就會列出來。

**兩項都通過，代表量值貼進 Power BI 之後不會因為欄位不存在而報錯，
且算出來的數字與舊報表一致。**

### 8-2 貼進 Power BI 後的四個抽查點

按 `DAX.md` 第八節的順序做（先第 4 頁），做完先看這四格：

| 檢查 | 期望值（2026-08-10 快照，會每日漂移） |
|---|---|
| 第 4 頁 KPI1 `業配率（依資料範圍）`，年份 2022 起 | 約 53% |
| 第 4 頁 KPI4 `判定精確率_長片` | **22 / 22**（文字，不是 100%） |
| 第 6 頁 KPI2 `長片系列化比例` | 約 36.6%——**若顯示 100%，是關聯設錯了** |
| 第 6 頁 KPI4 `首集就有業配` | **4 / 4** |

🔴 **第三格是最靈敏的探針。** 舊模型顯示 100% 代表 `ISBLANK` 沒抓到空字串；
新模型若顯示 100%，代表 `fact_影片` → `dim_系列` 的關聯沒建或方向錯了
（所有列都對到同一個成員）。

### 8-3 兩個必須親眼確認的圖表行為

| 檢查 | 為什麼 |
|---|---|
| 第 5 頁側上圖 2010／2011／2013／2014 **四根柱子都在** | 驗證 `業配率` 的 `+ 0` 有生效 |
| 第 6 頁側上圖 X 軸**從 2010 開始** | 驗證 `長片系列化比例` 的 `WithSeries + 0` 有生效 |

這兩個是 BLANK 陷阱，數字都對但圖會少東西，只能用看的。
