# Power BI 逐頁施工單

> 搭配 `DAX.md`（量值與設計理由）與 `HANDOFF.md`（專案全貌）使用。
> 本文件只回答一件事：**每一個視覺物件要怎麼拉出來。**
> 最後更新：2026-08-01
>
> ⚠️ 文中數字為撰寫當日快照，資料每日自動更新，引用前請重新查詢。

---

## 目錄

1. [怎麼用這份文件](#一怎麼用這份文件)
2. [施工前置檢查表](#二施工前置檢查表)
3. [必須補建的欄位與量值](#三必須補建的欄位與量值必看)
4. [通用設定（每個物件都要做）](#四通用設定每個物件都要做)
5. [逐頁施工](#五逐頁施工) — 第 1～7 頁
6. [互動設定](#六互動設定)
7. [完工檢查表](#七完工檢查表)
8. [待決事項](#八待決事項)

---

## 一、怎麼用這份文件

### 座標的意思

`assets/layout_page{N}.csv` 給的座標**是卡片的「內容區」，不是卡片本身**。
卡片的外框、標題列、標題文字**都已經印在背景 PNG 裡了**。

```
背景 PNG 畫的：  ┌─────────────────┐
                 │ ▌各類型的業配率  │  ← 卡片標題列（PNG）
                 ├─────────────────┤
                 │                 │  ← 視覺物件放這裡（layout CSV 的座標）
                 │                 │
                 └─────────────────┘
```

**所以每個視覺物件都必須關掉自己的標題、背景與邊框**，否則會蓋住背景圖的卡片。
這件事在第四節統一交代，逐頁不再重複。

### 施工順序

依 `DAX.md` 第八節：**先做第 4 頁**（它涵蓋門檻表、資料範圍表、雙軸圖、KPI 卡，
一次驗證所有機制），確認背景對齊、數字正確、篩選器連動之後，再做第 3、5、6 頁，
最後做第 1、2、7 頁（最單純）。

本文件的頁序是 1→7 方便查閱，**不是施工順序**。

### 進度（2026-08-02）

| 頁 | 狀態 |
|---|---|
| **1 封面** | ✅ **完工**（只放動態資料規模一個物件，姓名與課程已取消） |
| **2 資料特徵與判準** | ✅ **完工並驗證** |
| **3 產能結構轉移** | ✅ **完工並驗證** |
| **4 投放規模** | ✅ **完工並驗證**（2026-08-03 重驗：三個演出停點未變，開場數字微調，側上圖年份窗口已釐清） |
| **5 投放偏好** | ✅ **完工並驗證**（含編輯互動） |
| 6 接棒動能 | 🔧 **七個物件全部完工並驗證；剩編輯互動兩處** |
| **7 本報告的限制** | ✅ **完工並驗證** |

前置作業（三張表載入、兩張中斷連結表、兩個計算欄位、35 個量值、七頁畫布與背景）
**全部完成**。第 4 頁那一節是實作驗證過的版本，其他頁遇到相同狀況可直接照抄。

---

## 二、施工前置檢查表

以下全部完成才開始拉視覺物件。詳細步驟見 `DAX.md` 第二節。

| # | 項目 | 確認方式 |
|---|---|---|
| 1 | 建立 .pbix 並存到專案資料夾 | 從頭新建，**不要沿用任何舊檔** |
| 2 | 載入 `videos.csv` | 載入後約 3,500 列（每天增加） |
| 3 | 型別：`is_sponsored`／`ppd_api_flag`／`is_self_promo` → True/False | Power Query 欄位左側圖示 |
| 4 | 型別：`published_date` → 日期 | 同上 |
| 5 | **空值不要取代為 0** | `like_count`(4 支)、`comment_count`(1 支) 是「關閉顯示」不是「零」 |
| 6 | 載入 `data_notes.csv`、`limitations.csv` | 不需改型別 |
| 7 | 建立計算資料表 `門檻`（**不建關聯**） | `DAX.md` 2-1 |
| 8 | 建立計算資料表 `資料範圍`（**不建關聯**） | `DAX.md` 2-2 |
| 9 | 建立計算欄位 `系列世代` | `DAX.md` 2-3 |
| 10 | 貼入全部量值 | `DAX.md` 第三節 ＋ **本文件第三節** |
| 11 | 每頁畫布設為 1920×1080、背景 PNG「符合」、透明度 **0%** | 透明度預設 90%，一定要改 |

⚠️ 第 7、8 項的兩張表**絕對不能與 `videos` 建立關聯**。建了關聯，累加門檻的邏輯就失效。

---

## 三、必須補建的欄位與量值（必看）

`DAX.md` 第三節的量值清單**不足以拉出所有視覺物件**。以下是實作時才會發現的缺口，
補齊之後再開始施工。

### 3-1 計算欄位：業配標籤（在 `videos` 表）

```dax
業配標籤 = IF ( 'videos'[is_sponsored], "業配", "非業配" )
```

**為什麼要**：第 4 頁側上圖要用「業配／非業配」當軸。直接用 `is_sponsored` 當軸，
軸標籤會顯示 `True`／`False`，簡報上很難看，且觀眾要自己翻譯。

### 3-2 折線圖的「可比／不可比」拆線（第 3 頁側上圖）🔴

**這是最容易卡住的一處。** `DAX.md` 的 `可比性` 量值原本預期用條件式格式
把 2024 年後的線改成灰色虛線——**但 Power BI 的折線樣式（實線／虛線）不支援條件式格式**，
只能整條線設定。條件式格式只能改「標記」顏色，改不了線條。

解法是**拆成兩個量值、畫成兩條線**：

```dax
長片中位觀看_可比 =
IF ( MAX ( 'videos'[published_year] ) <= 2023, [長片中位觀看] )
```

```dax
長片中位觀看_累積中 =
IF ( MAX ( 'videos'[published_year] ) >= 2023, [長片中位觀看] )
```

⚠️ **2023 兩邊都給值是刻意的**——否則兩條線會在 2023／2024 之間斷開一格。
2023 這個點兩條線重疊，視覺上剛好是虛線的起點。

`可比性` 量值仍然保留，改用途為**工具提示**（滑鼠移上去顯示「可比／仍在累積」）。

### 3-3 第 5 頁主圖的「最高一條標紅」

```dax
主圖色_最高標紅 =
VAR topCat =
    CALCULATE ( [業配率最高類型], ALLSELECTED ( 'videos'[category] ) )
RETURN
    IF ( SELECTEDVALUE ( 'videos'[category] ) = topCat, "#FF0000", "#A0A0A0" )
```

用法：橫條圖 → `資料色彩` → 條件式格式（fx）→ 格式樣式選「**欄位值**」→ 選這個量值。

🔴 **`ALLSELECTED` 不能省（2026-08-03 實測）**：原版直接寫
`SELECTEDVALUE(...) = [業配率最高類型]`，結果**每一根長條都變紅**。
因為 `[業配率最高類型]` 內部是 `FILTER ( VALUES ( 'videos'[category] ), ... )`，
而 `VALUES` **遵守當下的篩選內容**——橫條圖每一列只有一個類型，在「遊戲實況」那一列上
`VALUES` 只回傳「遊戲實況」，量值就回答「遊戲實況」，條件永遠成立。

**為什麼用 `ALLSELECTED` 不用 `ALL`**：逐項加入類型的演出中，若還沒加到挑戰／企劃，
`ALL` 會把紅色標在一個沒顯示的類型上，**畫面上一根紅的都沒有**。
`ALLSELECTED` 給的是「目前顯示的類型裡最高的那一根」，演出全程都有紅色。

📌 `≥20` 樣本門檻不必再寫一次——`[業配率最高類型]` 內部已經有了。

符合 `HANDOFF.md` 第十章的配色規則：`#FF0000` 每頁最多一處。

### 3-4 第 6 頁氣泡圖的大小欄位

```dax
系列總觀看 = CALCULATE ( [總觀看], 'videos'[video_format] = "長片" )
```

**為什麼要**：氣泡大小要用「該系列的長片總觀看」。直接用 `[總觀看]` 會混入 Shorts，
與 X 軸的 `[系列正片數]`（已限長片）口徑不一致。

### 3-5 一個命名上的提醒

`類型業配率` 這個量值的內容其實只是「限長片的業配率」：

```dax
類型業配率 = CALCULATE ( [業配率], 'videos'[video_format] = "長片" )
```

名字只反映它的第一個用途。**第 5 頁的主圖、側上圖、側下圖三張都用它**
（分別按類型、年份、系列切），不要因為名字裡有「類型」就以為側上圖要另建量值。

### 3-7 第 6 頁方案丁的三個新增（2026-08-02）

#### ① 修改既有計算欄位 `系列世代`：界線改用《對決》停播日

```dax
系列世代 =
VAR s = 'videos'[series]
VAR duelEnd = DATE ( 2025, 11, 10 )
VAR firstPub =
    CALCULATE (
        MIN ( 'videos'[published_date] ),
        ALLEXCEPT ( 'videos', 'videos'[series] ),
        'videos'[video_format] = "長片"
    )
RETURN
    IF (
        ISBLANK ( s ) || s = "",
        "無系列",
        IF ( firstPub > duelEnd, "新系列", "既有系列" )
    )
```

**`2025-11-10` 是《Joe是要對決》最後一集**，標題就是「是時候該放手了⋯停播原因跟心路歷程」。

**為什麼從「曆年 ≥2025」改成停播日**：原規則會把《工作室日常廢片》
（首播 2025-08-27，比停播**早 75 天**）算成新系列，不符合這頁「招牌停播後的接棒」的宗旨。
改用停播日之後，定義與論點完全對齊，被問「你怎麼定義新系列」可以直接回答
「**《對決》最後一集之後開播的**」。

⚠️ **刻意寫死日期，不用 `MAX(published_date)` 動態抓。** 動態寫法在《對決》若復播時
界線會往後跳，**現有的新系列會全部被重新歸類且不報錯**。代價是復播後要手動改這一行。

**改完的驗收值**（全表 3,502）：無系列 **2,372**、既有系列 **1,109**、新系列 **21**
（長片 19 支 ＋ Shorts 2 支）。

#### ② 新增計算欄位 `是首集`

```dax
是首集 =
VAR s = 'videos'[series]
VAR firstPub =
    CALCULATE (
        MIN ( 'videos'[published_date] ),
        ALLEXCEPT ( 'videos', 'videos'[series] ),
        'videos'[video_format] = "長片"
    )
RETURN
    NOT ISBLANK ( s )
        && s <> ""
        && 'videos'[video_format] = "長片"
        && 'videos'[published_date] = firstPub
```

標記每個系列的第一支長片。有了它，KPI4 的量值只是簡單計數，不用寫巢狀迭代。

⚠️ 若某系列首集同日發多支會標記多列——實測只有《Joeman Vlog》（3 支同天），
它從無業配、不會出現在任何視覺上，不影響。

#### ③ 新增量值

```dax
首集就有業配 =
VAR tot =
    CALCULATE ( COUNTROWS ( 'videos' ), 'videos'[是首集] = TRUE (), 'videos'[系列世代] = "新系列" )
VAR hit =
    CALCULATE ( COUNTROWS ( 'videos' ), 'videos'[是首集] = TRUE (), 'videos'[系列世代] = "新系列",
                'videos'[is_sponsored] = TRUE () )
RETURN hit & " / " & tot
```
> 回傳**文字**「4 / 4」，不用設格式。顯示分數而不是 100%，是為了把分母攤在檯面上。

```dax
首次業配集數 =
VAR firstSponsored =
    CALCULATE ( MIN ( 'videos'[published_date] ), 'videos'[is_sponsored] = TRUE (),
                'videos'[video_format] = "長片" )
RETURN
    IF (
        NOT ISBLANK ( firstSponsored ),
        CALCULATE ( COUNTROWS ( 'videos' ), 'videos'[video_format] = "長片",
                    'videos'[published_date] <= firstSponsored )
    )
```
> 在以 `series` 為軸的圖表裡算出「到第一支業配為止累計第幾集」。從無業配的系列回傳空白。

```dax
首次業配集數_顯示 =
VAR eps = [系列正片數]
VAR gen = SELECTEDVALUE ( 'videos'[系列世代] )
RETURN IF ( gen = "新系列" || eps >= 40, [首次業配集數] )
```
> 控制側下圖顯示哪些系列：**全部新系列 ＋ 集數 ≥40 的既有系列**，剛好 9 列
> （205px ÷ 9 = 22.8px／列）。`≥40` 純粹是控制列數，放寬到 20 會變 11 列太擠、
> 收到 50 會漏掉《網紅觀察室》。**這個數字可以調，調完看列數就好。**

### 3-6 動態註記：最新年度提醒（第 4 頁主圖用）

```dax
最新年度提醒 =
VAR maxPub = MAX ( 'videos'[published_date] )
RETURN
    IF (
        YEAR ( maxPub ) = YEAR ( TODAY () ),
        FORMAT ( maxPub, "yyyy" ) & " 年僅 1–" & FORMAT ( MONTH ( maxPub ), "0" ) & " 月",
        ""
    )
```

目前輸出：**`2026 年僅 1–7 月`**

**三個設計重點**

1. **月份取自資料本身**（`MAX(published_date)`），不是系統時間——八月有新片上架就自動變「1–8 月」
2. **`YEAR(TODAY())` 的條件式**：只有當畫面上的最新年度就是今年時才顯示。
   年份切片器若拉到 2020–2024，提醒會自動消失（2024 是完整年度，不需要提醒）
3. **回傳 `""` 而不是 `BLANK()`**：卡片視覺遇到 `BLANK()` 會顯示 **`(空白)`** 四個字，
   空字串才會整張卡片隱形

> 🔴 **`""` 只能用在純文字顯示的量值。** 數值型量值回傳空字串會讓整欄型別從數字變文字，
> 圖表軸直接失效。**特別不要動 `長片中位觀看_可比` / `_累積中`**——
> 那兩個回傳 `BLANK()` 是刻意的，折線圖靠它判斷「這個年份沒有資料點」而讓線斷開，
> 第 3 頁的實線／虛線切換完全依賴這個行為。
>
> **判準**：量值給人看的文字 → `""`；量值要進圖表當數值 → `BLANK()`。

⚠️ **凡是含日期或期間的註記，一律用量值不用文字方塊。** 資料每日自動更新，
寫死的「2026 年僅 1–7 月」幾週後就過期，而且會讓「本報表每日自動更新」這個賣點自相矛盾。

---

## 四、通用設定（每個物件都要做）

拉出**任何**視覺物件之後，立刻做這四件事：

| # | 位置 | 設定 |
|---|---|---|
| 1 | 一般 → 標題 | **關閉**（標題已印在背景 PNG） |
| 2 | 一般 → 效果 → 背景 | **關閉**（卡片已印在背景 PNG） |
| 3 | 一般 → 效果 → 視覺效果邊框 | **關閉** |
| 4 | 一般 → 屬性 → 大小與位置 | 依本文件給的**高度／寬度／橫向／縱向**由上而下輸入 |

**第 4 項一定要用打字輸入，不要用滑鼠拖。** 拖曳對不準，四張 KPI 卡差 2px 就看得出來。

### 🔴 單位陷阱：畫布是 px，字級是 pt

**Power BI 的畫布座標單位是 px，但字級欄位的單位是 pt**，兩者不同：

```
pt × 1.333 = px        例：33pt = 44px
```

填錯的後果：數值區高 64px，你填「44」以為是 44px，Power BI 會當成 44pt＝59px，
**行高 80px，直接被裁掉一半**。

本文件凡是講「字級」一律標 **pt**，講「座標／尺寸」一律標 **px**。

### KPI 卡的共同設定　⚠️ 用「卡片（舊版）」

🔴 **視覺類型必須選「卡片」的舊版**，不是「卡片(新)」。

在「量測計、卡片和 KPI」這一組裡，圖示是**方框中有「123」**的那個。放上畫面後點選它，
用格式窗格的區段清單確認版本：

| 看到的區段 | 版本 |
|---|---|
| 只有 `圖說文字值`、`類別標籤`（＋`大小和樣式`、`標題`） | ✅ **舊版**，正確 |
| 有 `多卡片版面配置`、`卡片`、`參考標籤` | ❌ 新版，換掉 |

**新版有三層背景（大小和樣式／多卡片版面配置／卡片）與兩層內距**，
關掉一層還是白底，逐張設定成本高很多。舊版只有兩個區段。

#### 設定

| 區段 | 設定 |
|---|---|
| 大小和樣式 → 背景 | **關閉** |
| 大小和樣式 → 視覺效果框線 | **關閉** |
| 大小和樣式 → 陰影 | **關閉** |
| 標題 | **關閉** |
| **類別標籤** | **關閉**（標籤已印在背景 PNG 的卡片左上） |
| 圖說文字值 → 字型 | **`Segoe UI` 粗體** |
| 圖說文字值 → 大小 | **33** |
| 圖說文字值 → 色彩 | `#F1F1F1` |
| 圖說文字值 → **顯示單位** | 🔴 **`無`** |

**第一張設定好之後 `Ctrl+C` / `Ctrl+V` 複製**，其餘 15 張只要改量值與橫向座標，
格式全部繼承。不要每張重設。

#### 🔴 字型清單裡沒有微軟正黑體

Power BI 的字型下拉是**固定清單**，不是系統字型清單，找不到 `Microsoft JhengHei`。
用 **`Segoe UI` 粗體**即可：

- KPI 值幾乎都是數字與百分比，背景圖裡沒有數字，不存在「與 PNG 對不上」的問題
- 唯二的中文 KPI（第 5 頁「挑戰/企劃」「遊戲實況」）會由 Windows 自動回退到
  系統中文字型＝微軟正黑體，跟背景圖一致
- **粗體是刻意的**：標籤是 21px 細體淺灰、數值是 33pt 粗體亮白，層次才拉得開

#### 🔴 顯示單位一定要設「無」

預設是「自動」，`[影片數]` 3,502 會顯示成 **`3.5K`**、
`[系列總觀看]` 1,270,789,345 會顯示成 **`1.27bn`**。

#### 數值是置中的，這是舊版卡片的限制

舊版卡片**沒有水平對齊選項**，數值固定置中，而背景圖的標籤是靠左（x=226 等）。

**已決定接受**（2026-08-02）：數值區寬 339px，靠左會讓右側空掉一大片；
置中反而視覺平衡，且四張卡口徑一致。標籤小、數字大，視線會直接落在數字上，
不會去比對兩者左緣。

> 📐 **數值區尺寸與位置的由來**
> - **高 64px**（2026-08-01）：原本標籤 27px、數值區僅 48px，數值最大只能設到 26pt，
>   只比標籤大 1.26 倍，KPI 弱得像內文。標籤縮回 `HANDOFF.md` 記載的 21px 後，
>   數值區加大到 64px，可放 33pt（標籤的 2.1 倍）。圖表區完全未動。
> - **縱向 145**（2026-08-02）：**在 Power BI 裡目視微調出來的，不是幾何計算值。**
>   舊版卡片會把數值垂直置中於自己的框內，而背景圖卡片的內距上下不對稱
>   （上 16、下 7），照幾何置中（縱向 155）會看起來偏下。收到 145 才平衡。
>   改這個值要重新目視確認，不能只靠算的。
> - **寬度從來不是問題**：最寬的值是第 5 頁「挑戰/企劃」，44px 時只佔 194px，可用 339px。

### 🔴 沒有「顯示正負號」這個勾選框

Power BI 的量值格式區只有**格式／小數位數／千分位**三項，**沒有**顯示正負號的選項。
要讓正數帶 `+`，只能用**自訂格式字串**：

理論上是 `量值工具` → 「格式化」群組 → `格式` 下拉 → **`自訂`** → 輸入
`+0.0%;-0.0%;0.0%`（三段以分號分隔，對應正數；負數；零）。

### 🔴 但這個版本的「格式」下拉**沒有「自訂」選項**（2026-08-03 實測）

沒有自訂格式字串就無法附加單位或正負號，**改用 `FORMAT()` 自己組字串**：

```dax
類型間差距 = FORMAT ( ( [最高類型業配率] - [最低類型業配率] ) * 100, "0.0" ) & "pp"
```

代價是量值變成**文字**，不能進圖表軸——只適合 KPI 卡。

**兩個受影響的量值**：

| 量值 | 情況 |
|---|---|
| `類型間差距` | 🔴 **必須顯示 `pp` 不是 `%`**。兩個百分比相減得到的是**百分點差**，顯示成「45.0%」會被讀成「差了 45%」。已改為上面的文字版本 |
| `觀看差距` | 想顯示 `+27.2%` 的正號同樣做不到。但它是「孤兒量值」（第 8-2b 節，**不放視覺、只口頭講**），目前沒有實際影響 |

### 🔴 五個實作時才會踩到的坑（2026-08-02 實測）

#### ① 字級調不上去 → 關閉「回應式」

視覺物件太小時，Power BI 的**回應式**會自動壓縮字級並**鎖住上限**（通常卡在 9pt），
手動填 14 會被彈回去。

👉 `一般` → `屬性` → `進階選項` → **`回應式`** → **關閉**

畫布是固定的 1920×1080，不需要這個功能，它只會綁手綁腳。**每張側圖都要關。**

關掉後仍上不去，代表視覺高度真的不夠：減少列數，或退而求其次——
**把字色從 `#AAAAAA` 改成 `#F1F1F1`**。在小尺寸下，亮度比字級更能救可讀性。

#### ② 「前 N 項」遇到並列會多給

Power BI 的前 N 項篩選，**第 N 名有並列時會全部保留**。

實測：第 4 頁側下設「前 6 項」卻出現 7 列（未分類 12、汽車 12 並列）；
改「前 5 項」在另一個年份範圍下又變 6 列（人物/訪談 19、未分類 19 並列）。

**並列與否隨篩選範圍變動，沒辦法一勞永逸。** 做法是：設定後**目視確認列數**，
發表前重新整理資料後再確認一次。多一列通常無害，只要版面塞得下。

#### ③ 排序欄位不能用 DAX 計算欄位 → 會循環相依

想讓 `video_format` 的按鈕照「長片 / Shorts」排，直覺是建一個 DAX 計算欄位：

```dax
格式排序 = IF ( 'videos'[video_format] = "長片", 1, 2 )   -- ❌
```

再設 `video_format` 依 `格式排序` 排序，會跳出：

> 偵測到循環相依性：videos[video_format], videos[格式排序], videos[video_format]

**原因**：`格式排序` 由 `video_format` 算出來，`video_format` 又要依 `格式排序` 排序，互相依賴。

**解法：改在 Power Query 建**（產生的是實體資料欄，模型看不到計算式，沒有相依關係）

`轉換資料` → 選 `videos` → `新增資料行` → **`條件資料行`**：
名稱 `格式排序`、如果 `video_format` 等於 `長片` 則 `1`、其他 `2` →
**型別改成整數** → `關閉並套用` → 再設「依資料行排序」。

⚠️ **`門檻` 與 `資料範圍` 兩張 `DATATABLE` 表沒有這個問題**——它們的排序欄是
資料表定義裡就寫好的獨立欄位，不是從標籤欄算出來的。

⚠️ 這只是操作順手度，**不影響任何數字**。嫌麻煩可以不做。

#### ④ 資料標籤被裁掉 → 手動設 X 軸範圍留白

最長的長條頂到繪圖區右緣時，它的資料標籤會被切掉。

👉 `X 軸` → `範圍` → **`結束`** 填入「最大值 × 1.25」左右 → **再把 X 軸關閉**
（範圍設定會保留，軸線不顯示）

實測值：第 4 頁側下最大 215 → 結束設 **270**；側上最大 640,583 → 結束設 **800000**。

**不要改用「內部結尾」**：長條夠長的放得進去，短的 Power BI 會自動丟到外面，
變成一半深色字一半淺色字，更亂。

⚠️ 資料每日增加，**發表前一天重新整理後要看一眼標籤有沒有再被裁到**。

#### ⑥ 「前 N 項」先算，量值篩選後算 🔴

同一個視覺物件上若**同時**有「前 N 項」與「量值的進階篩選」，**Top N 會先算**——
它在量值篩選生效之前就從**全部**類別挑出前 N 名，量值篩選才把其中不合格的砍掉，
**結果遠少於 N 列**。

實測（第 5 頁側下）：`series` 前 6 項依業配率遞減 ＋ `[系列長片數_不受門檻] >= 10`
→ **只剩 1 列**。因為 Top 6 先挑出五個 2–3 集的 100% 小系列，門檻再把它們全砍掉。

**解法：把條件包進同一個量值，讓不合格者回傳 `BLANK()`。Top N 不會挑到 BLANK。**

```dax
系列業配密度 = IF ( [系列長片數_不受門檻] >= 10, [類型業配率] )
```

⚠️ 第 4 頁側下也用了「前 N 項」，但那裡沒有搭配量值篩選，所以沒踩到。
**日後任何視覺要同時用這兩者，一律改寫成單一量值。**

#### ⑤ 按鈕膠囊被裁 → 調「邊框間距」，不是改寬度或圓角

高 **56px** 的按鈕交叉分析篩選器，預設的上下邊框間距會把可用高度吃到膠囊放不下，
症狀是**最左邊那顆看起來被削平**（第 6 頁觀看數門檻實測）。

👉 `一般` → `屬性` → `大小及位置` → `邊框間距` → **下** 減 1

🔴 **裁切是相對於視覺物件自己的邊界，移動位置或加寬完全無效**——第一顆按鈕永遠貼在
新的左緣上，切法一模一樣。也不是邊界／陰影／圓角半徑造成的，那三項都排查過。

⚠️ 第 3 頁（480px）與第 5 頁（534px）的門檻膠囊**高度同樣是 56**，很可能複製同一個症狀。

⚠️ 另有一種「假裁切」：編輯模式下物件被選取時，Power BI 會沿邊界畫選取框線，
白色的「已選取」膠囊與白色框線疊在一起也像被削掉。**先點空白處取消選取確認一次**，
再動任何設定。

### 圖表的共同配色（`HANDOFF.md` 第十章）

| 顏色 | 用途 |
|---|---|
| `#FF0000` | **每頁最多一處**的重點 |
| `#A0A0A0` | 主要資料條／線 |
| `#8C8C8C` | 次要資料條／線 |
| `#5C5C5C` 虛線 | **不可比／不可信的資料** |

### 🔴 文字色彩的統一規則（2026-08-03 定案）

| 用途 | 色彩 |
|---|---|
| **X／Y 軸文字**（軸標籤、類別名稱） | **`#AAAAAA`** |
| **資料標籤**（長條上的數值） | **`#F1F1F1`** |
| 格線 | `#272727` |
| 腳註／動態註記 | `#717171` |

軸文字是**座標系統**、資料標籤是**主體訊息**，兩者亮度分層，跟 KPI 卡「標籤淺灰、數值亮白」
是同一個作風，**這不是不一致**。

⚠️ 本節第 ① 點提到「小尺寸下把字色從 `#AAAAAA` 改成 `#F1F1F1`」是**例外條款**，
只在字級真的調不上去、可讀性告急時才動用。實測 9pt 的 `#AAAAAA` 在 `#1F1F1F` 底上
對比約 **7:1**，遠高於門檻，所以第 6 頁側下並未動用這個例外。
第 3、4 頁原本寫成 `#F1F1F1` 的軸值已於 2026-08-03 一併改回 `#AAAAAA`。

---

## 五、逐頁施工

---

## 第 1 頁｜封面

座標檔：`assets/layout_page1.csv`　背景：`bg_page1.png`　無篩選器

### ✅ 已完工（2026-08-03）——**只放一個物件**

🔴 **使用者決定不放報告人姓名與日期／課程兩個文字方塊**，封面只留動態資料規模那一行。
座標表裡 610／680 那兩格保留不用。

| 物件 | 視覺類型 | 高 | 寬 | 橫 | 縱 |
|---|---|---|---|---|---|
| 動態資料規模 | **卡片（舊版）** | 46 | **620** | 140 | **610** |

| 設定 | 值 |
|---|---|
| 欄位 | `[動態資料規模]` |
| 圖說文字值 → 字型 | Segoe UI、**20pt** |
| 圖說文字值 → 色彩 | **`#AAAAAA`** |
| 圖說文字值 → 顯示單位 | 無 |
| 類別標籤 | 關閉 |
| 標題／背景／視覺效果框線／陰影／回應式 | 全關 |

⚠️ **必須用「卡片」不能用文字方塊**——它要顯示 DAX 量值，文字方塊放不了量值。

**三個數字的由來**

- **縱向 610**：背景圖的紅色橫線畫在 y=552–558，610 是原設計的下一個元素起點，保留 52px 呼吸。留在原本的 740 會離橫線 182px，孤零零浮在中間。
- **20pt ＋ `#AAAAAA`**：封面字級階梯原本是 `84 → 38 → 34 → 26 → 24`（px）。拿掉 34（姓名）與 26（日期）後，直接從 38 跳到 24 會斷太重，而 `#717171` 是腳註灰，用在唯一一行活資訊上像沒設定完。20pt ＝ 26px 正好接上被拿掉的那一階。
- **寬度 620**：🔴 舊版卡片**沒有水平對齊選項、數值固定置中**（第四節），而標題與副標靠左在 x=140。原本寬 780 時文字會從約 x=232 開始，**比標題右縮 92px，一眼看得出沒對齊**。把寬度收到與文字長度相近，置中就等於靠左（20pt 下該字串約 613px，左緣落在 143）。

⚠️ 若結尾出現「⋯」代表框不夠寬，加到 **700**（左緣右移約 43px，仍可接受）。
對齊要求極高的話改用**「卡片（新）」**，它有 `水平對齊 → 靠左`，代價是要多關三層背景。

⚠️ `layout_page1.csv` 的區塊名稱裡寫的是 `34px`／`26px`／`24px`，那是**背景圖的設計值（px）**。
Power BI 的字級欄位要填 **pt** 值。

顯示結果：`分析母體 3,503 支影片｜2010–2026｜每日自動更新`

**設計理由**：副標「解構一個頻道的產能結構、廠商投放與 IP 接棒」印死在 PNG 裡，
刻意不含任何數字；規模數字由量值即時算，資料一更新就跟著變。

---

## 第 2 頁｜資料特徵與判準

座標檔：`assets/layout_page2.csv`　背景：`bg_page2.png`　無篩選器

| # | 物件 | 視覺類型 | 座標 | 資料 |
|---|---|---|---|---|
| 1 | 資料特徵與判準 | **表格** | 高 684、寬 1654、橫 212、縱 338 | `data_notes` 表 |

**欄位（依序拖入）**：`項目`、`說明`

### 🔴 施工順序不能顛倒（2026-08-03 實測）

**欄寬必須趁資料行標題還看得見時拖曳**——標題隱藏之後就沒有東西可以拖了。

**1　排序（先做，要進 Power Query）**

`data_notes.csv` 沒有排序欄，照文字排序會變成「業配判定方式」排第一、「資料來源」排到第八，
而原始順序是有邏輯的：資料來源 → 抓取範圍 → 分析母體 → 時區 → 判定方法 → 準確度 → 限制 → 更新時間。

1. `常用` → `轉換資料` → 左邊點 `data_notes`
2. `新增資料行` → **`索引資料行`** → `從 1`
3. `關閉並套用`
4. 資料窗格點 `data_notes[項目]` → `資料行工具` → **`依資料行排序`** → 選 `索引`
5. 回報表，點表格的「項目」標頭一次

⚠️ **不要把 `索引` 欄拖進表格**，它只負責排序。
📌 Power Query 的實體資料欄**不會觸發第四節第 ③ 點的循環相依**，那個坑只在用 DAX 計算欄位當排序依據時才會踩到。

**2　值的四個顏色（一個都不能漏）**

| 設定 | 值 |
|---|---|
| 文字色彩 | `#F1F1F1` |
| **替代文字色彩** | `#F1F1F1` |
| 背景色彩 | `#1F1F1F` |
| **變更背景色彩** | `#1F1F1F` |
| 字型大小 | **15pt** |
| 自動換行 | 開啟 |

🔴 **「替代」那兩個是斑馬紋的偶數列。**只改前兩個的話，偶數列的字是黑色的——
在深色底上**整列消失**，實測時以為資料掉了。`#1F1F1F` 是背景 PNG 卡片的底色，設同色等於透明。

**3　欄寬**

拖「項目」與「說明」的分隔線，把 `項目` 收到約 **220px**（剛好放得下「Shorts 偵測限制」一行就停手），
再拖 `說明` 右緣，**直到底部橫向捲軸消失**。

🔴 **捲軸沒消失之前，自動換行不會生效**——欄位比視覺物件還寬時 Power BI 直接給捲軸，
不會折行。捲軸消失的瞬間長字串才開始換行、列高變高，那是正確狀態。

**4　格線與行距**

| 設定 | 值 |
|---|---|
| 水平格線 | 開啟、`#272727` |
| 垂直格線 | 關閉 |
| **框線** | **關閉**（預設是**藍色**，背景 PNG 已經畫了卡片外框） |
| **列填補** | 預設 3 → **8** |

**5　最後才隱藏資料行標題**

🔴 **表格視覺沒有關閉資料行標題的開關。** 第四節以前寫的「資料行標頭關閉」做不到。
改用同色隱藏：

`格式` → `視覺效果` → `資料行標題` → `文字` → 文字色彩 `#1F1F1F`、背景色彩 `#1F1F1F`

標題列仍會佔約 30px 的高度，屬預期。

⚠️ 表格共 10 列，684px 塞得下，**不應該出現捲軸**。若出現，字級 15 → **14**。

---

## 第 3 頁｜產能結構轉移

座標檔：`assets/layout_page3.csv`　背景：`bg_page3.png`（**2026-08-02 重產**，篩選器寬度已改）
**這頁的一句話**：總產能提高 78%（2019→2025），其中長片產能部分轉移到短影音。

> 🔴 **背景圖已更新**，Power BI 裡要**重新套用一次第 3 頁的畫布背景**
> （`格式化頁面` → `畫布背景` → 重新瀏覽選 `bg_page3.png` → 影像調整「符合」、透明度 0%）。
> 原因見下方〈篩選器 ×3〉。

### KPI ×4

| # | 標籤（PNG 已印） | 視覺 | 座標 | 值 | 格式 |
|---|---|---|---|---|---|
| 1 | 影片總數 | 卡片 | 高 64、寬 339、橫 226、縱 145 | `[影片數]` | 整數，千分位 |
| 2 | 長片數 | 卡片 | 高 64、寬 339、橫 655、縱 145 | `[長片數]` | 整數，千分位 |
| 3 | Shorts 數 | 卡片 | 高 64、寬 339、橫 1084、縱 145 | `[Shorts 數]` | 整數，千分位 |
| 4 | 長片中位觀看 | 卡片 | 高 64、寬 339、橫 1513、縱 145 | `[長片中位觀看]` | 整數，千分位 |

### 主圖｜產能結構：長片與 Shorts 的消長

- **視覺類型**：堆疊直條圖
- **座標**：高 528、寬 796、橫 212、縱 338

| 欄位區 | 放什麼 |
|---|---|
| X 軸 | `published_year` |
| Y 軸 | `[影片數]` |
| 圖例 | `video_format` |

**資料色彩**：`長片` → `#A0A0A0`　／　`Shorts` → `#FF0000`

**格式**

| 項目 | 設定 |
|---|---|
| X 軸 → 類型 | **`類別`**（用連續會出現小數刻度） |
| X／Y 軸 → 值 | 字級 **12**、色彩 `#AAAAAA` |
| X／Y 軸 → 標題 | 關閉 |
| 圖例 | 置頂靠右、`#AAAAAA`、**12pt**（這裡的序列名是「長片」「Shorts」，是給觀眾看的，要留著） |
| 資料標籤 | 關閉（17 根柱子放標籤會糊掉） |
| 回應式 | **關閉**（一般 → 屬性 → 進階選項） |

**驗證值**

| 年 | 長片 | Shorts | 合計 |
|---|---|---|---|
| **2015** | **530** | 0 | **530** ← 全期最高峰 |
| 2019 | 169 | 0 | 169 |
| 2022 | 179 | 4 | 183 |
| 2023 | 158 | 97 | 255 |
| 2024 | 161 | 131 | 292 |
| **2025** | **131** | **169** | **300** |
| 2026 | 63 | 71 | 134 |

**要說的話**：2019 年整根全灰；2025 年長片 131 支、Shorts 169 支，紅色已經過半。

### 🔴 2015 年那根柱子必須主動說明

全期圖表上**最高的一根是 2015 年的 530 支**，不是 2025 年的 300 支。
只說「總產能提高」會當場被打臉——**從 2015 算起其實是下降 43%**。

| 年 | 長片數 | 主力內容 |
|---|---|---|
| 2015 | 530 | **遊戲實況 435 支（82%）**、電競賽事 49 |
| 2016 | 292 | 遊戲實況 199、3C開箱 34 |
| 2019 | 169 | 人物/訪談 59、3C開箱 40 |
| 2025 | 131 | **挑戰/企劃 59**、3C開箱 22 |

**台詞**：
> 「2015 年那根特別高，因為那是日更實況時期——530 支裡有 435 支是遊戲實況。
> 一支實況錄影跟一支《Joe是要對決》的製作成本差幾十倍，**產能數字不能直接比**。
> 所以我的基準取 2019 年，那時候已經轉型成企劃型內容了。」

這段說明正好餵給側下那張「內容類型組成變遷」——2015 的高產能是遊戲實況撐起來的。

### 側上｜長片中位觀看（2023 年前可比）

- **視覺類型**：折線圖
- **座標**：高 205、寬 796、橫 1070、縱 338

| 欄位區 | 放什麼 |
|---|---|
| X 軸 | `published_year` |
| Y 軸 | `[長片中位觀看_可比]`、`[長片中位觀看_累積中]`（**兩個都放**） |

**線條格式（分別設定）**

| 序列 | 色彩 | 線條樣式 | 寬度 |
|---|---|---|---|
| 長片中位觀看_可比 | `#A0A0A0` | 實線 | 3px |
| 長片中位觀看_累積中 | `#5C5C5C` | **虛線** | 3px |

- 圖例：**關閉**（標題已說明「2023 年前可比」，圖例會佔掉本來就只有 205px 的高度）
- 工具提示：加入 `[可比性]`
- 回應式：**關閉**；軸值字級 **14**、色彩 `#AAAAAA`

⚠️ 見第 3-2 節，不能用條件式格式做這件事。

**驗證值**

| 年 | 中位觀看 | _可比 | _累積中 |
|---|---|---|---|
| 2015 | 2,450 | 2,450 | (空白) |
| 2017 | 460,993 | 460,993 | (空白) |
| **2019** | **726,019** ← 高峰 | 726,019 | (空白) |
| 2022 | 617,958 | 617,958 | (空白) |
| **2023** | **556,488** | **556,488** | **556,488** ← 兩線交接 |
| 2024 | 480,385 | (空白) | 480,385 |
| 2026 | 535,291 | (空白) | 535,291 |

講稿的「2019→2023 下滑 23.4%」**已驗證正確**（726,019 → 556,488）。

⚠️ 2010–2016 只有 2,450–10,262，跟 2017 起的 46 萬**差兩百倍**，
前七年會整段貼著地板。**這是對的訊息**——2017 年是這個頻道爆發的轉捩點，
線條「突然跳起來」正好呈現那件事。不要為此改對數軸，那會讓觀眾看不懂。

**要說的話**：兩層。表層是「2019→2023 單片戰力下滑」；深層是**虛線本身就是一句話**
——「我知道 2024 年後的影片還在累積觀看，所以我不比」。

### 側下｜內容類型組成變遷

- **視覺類型**：**100% 堆疊區域圖** 🔴（不是一般的堆疊區域圖）
- **座標**：高 205、寬 796、橫 1070、縱 661

> 🔴 **別選到「100% 堆疊橫條圖」**（2026-08-02 踩過）。名稱很像但那是橫的，
> 欄位區會變成 `Y 軸 = 類別`、`X 軸 = 值`，症狀是：
> ① **`[影片數]` 放不進 Y 軸**（Y 軸只吃欄位不吃量值）；
> ② X 軸沒有「類型」選項，只有「範圍／最小值／最大值／對數刻度」——那是**值軸**的設定。
>
> 看到這兩個症狀就是選錯視覺了，直接在視覺效果窗格換成 **100% 堆疊區域圖**。

| 欄位區 | 放什麼 |
|---|---|
| X 軸 | `published_year` |
| Y 軸 | `[影片數]` |
| 圖例 | `category` |

🔴 **必須加視覺層級篩選**：`category` → 篩選類型「前 N 項」→ **前 6 項**，依 `[影片數]` 遞減。
前 6 類是：遊戲實況、3C開箱評測、挑戰/企劃、電競賽事、未分類、人物/訪談。

#### 為什麼要用 100% 堆疊（改自原規劃）

`DAX.md` 原規劃是畫**絕對數量**的堆疊面積，但 **2015 年的 530 支會把整張圖壓垮**，
其他年份全變成貼地的細線，完全看不出組成變化。

改成 100% 堆疊後每年都拉成滿版，只看比例，四個世代的轉型一目了然：

| 年 | 主導類型 |
|---|---|
| 2010–2011 | **電競賽事** 180、108 支 |
| 2014–2016 | **遊戲實況** 35 → 435 → 199 支 |
| 2017–2022 | **3C開箱評測** 88 → 71 支 |
| 2024–2025 | **挑戰/企劃** 91、59 支 |

**而且分工才對**：主圖已經負責「總量」，側下就該負責「比例」，兩張圖不重複。

⚠️ `未分類` 排第 5（246 支），**保留不要濾掉**。它佔全頻道 11.8%，
藏起來會誤導組成，而且這份報表的一貫作風就是自我揭露。

- 圖例：置底、**12pt**（六項排一列）
- 資料標籤：關閉
- 回應式：**關閉**

**要說的話**：這頻道十六年來一直在轉型，這已經是第四次——**先拆掉「轉型＝危機」的直覺**。

### 篩選器 ×3

🔴 **背景圖的篩選器寬度已於 2026-08-02 重新分配**（380/690/420 → **260/480/754**），
因為原本年份區間只有 420px，「介於」樣式的滑桿擠不出來（第 4 頁踩過同一個坑）。

| # | 標籤（PNG 已印） | 視覺 | 高度 | 寬度 | 橫向 | 縱向 |
|---|---|---|---|---|---|---|
| 1 | 影片格式 | 按鈕交叉分析篩選器 | 56 | 260 | 232 | 964 |
| 2 | 觀看數門檻 | 按鈕交叉分析篩選器 | 56 | 480 | 552 | 964 |
| 3 | 年份區間 | 交叉分析篩選器（介於） | **72** | **610** | **1232** | **930** |

**第 3 項一樣是「標籤在左、控制項在右」**：年份區間的容器是 1092–1846，
標籤「年份區間」佔到約 x=1200，控制項從 1232 起、寬 610px（第 4 頁實測 690px 可用）。

**篩選器 1、2 用按鈕交叉分析篩選器**，配色與第 4 頁相同：

| 狀態 | 背景 | 文字 |
|---|---|---|
| 預設 | `#272727` | `#AAAAAA` |
| 暫留 | `#3F3F3F` | `#FFFFFF` |
| **已選取** | **`#F1F1F1`** | **`#0F0F0F`** |

版面配置：**橫向**，影片格式 **2 欄**、觀看數門檻 **5 欄**，間距 8，圓角半徑 18。
`一般 → 效果 → 背景` 與 `版面配置 → 背景` **兩層都要關**。

⚠️ **影片格式的「單一選取」要開啟**（三步揭露需要一次只看一種格式）。
⚠️ **觀看數門檻的「單一選取」也要開啟**，並記得 `門檻[門檻標籤]` 已做過「依資料行排序」，
否則五顆按鈕會照筆劃排成「10 萬／100 萬／200 萬／50 萬／全部」。

### 演出：格式三步揭露

**年份預設全期 2010–2026**（與第 4 頁一致，敘事從 2010 年開始逐步推進）。

| 步驟 | 動作 | 台詞 | 數字 |
|---|---|---|---|
| 0 | — | 先說明 2015 年那根柱子（見上方） | 530 支，遊戲實況 82% |
| 1 | 不選格式 | 「2019 到 2025 年，總產能增加 **78%**」 | 169 → 300 |
| 2 | 點 **長片** | 「但長片其實**少拍了 22%**」 | 169 → 131 |
| 3 | 點 **Shorts** | 「增加的全是這個，**從 0 到 169 支**」 | 0 → 169 |

🔴 **「三年」是錯的。** 78% / −22% / 0→169 來自 **2019 與 2025 的比較，橫跨六年**。
所有三年區間都對不上（2022→2025 是 +63.9%、2021→2024 是 +67.8%、2020→2023 是 +56.4%）。
`HANDOFF.md` 與 `DAX.md` 原本都寫「三年」，已一併修正。

---

## 第 4 頁｜投放規模　✅ 已完工（2026-08-02）

座標檔：`assets/layout_page4.csv`　背景：`bg_page4.png`
**這頁的一句話**：廠商投放沒有減少，長片業配率自 2022 年起穩定在 55%。

> 📌 **本節是實作驗證過的版本**，與 `DAX.md` 第四節的原始規劃有數處差異，
> 差異之處都標了理由。其他頁施工時遇到同樣狀況可直接套用。

### KPI ×4

| # | 標籤（PNG 已印） | 視覺 | 座標 | 值 | 格式 |
|---|---|---|---|---|---|
| 1 | 長片業配率 | 卡片 | 高 64、寬 339、橫 226、縱 145 | `[套用資料範圍]` | 百分比，1 位小數 |
| 2 | 偵測業配數（下限） | 卡片 | 高 64、寬 339、橫 655、縱 145 | `[業配數_受範圍影響]` | 整數，千分位 |
| 3 | 推估實際業配 | 卡片 | 高 64、寬 339、橫 1084、縱 145 | `[推估實際業配]` | 整數，千分位 |
| 4 | 判定精確率 | 卡片 | 高 64、寬 339、橫 1513、縱 145 | `[判定精確率]` | 百分比，1 位小數 |

**這組 KPI 本身就是方法論宣告**：第 2 張標「下限」、第 3 張是用召回率 85.3% 回推、
第 4 張把精確率攤在檯面上。四張合起來說的是「我給你數字，同時給你這個數字的不確定性」。

### 主圖｜業配影片數與業配率

- **視覺類型**：**折線與堆疊直條圖**
- **座標**：高 528、寬 796、橫 212、縱 338

| 欄位區 | 放什麼 |
|---|---|
| X 軸 | `published_year` |
| 資料行 Y 軸（柱） | **`[業配數_受範圍影響]`** |
| 線條 Y 軸（線） | **`[套用資料範圍]`** |

🔴 **這兩個量值跟 `DAX.md` 原本寫的不一樣。** 原規劃是 `[業配影片數]` 與 `[類型業配率]`，
那兩個寫死了「僅長片」，**不會受資料範圍切片器影響**——切「全部」時只有右上角一個
KPI 數字變，主圖紋風不動，投影幕上的說服力差很多。

改用受範圍影響的版本後，切換時**整條紅線會下沉**（2023 年 54.4%→34.9%、2025 年 55.0%→28.0%）。

**格式**

| 項目 | 設定 |
|---|---|
| 柱色彩 | `#8C8C8C`（次要——要看的是線不是柱） |
| 線色彩 | `#FF0000`、寬度 4px |
| 線標記 | 開啟、大小 5 |
| 左 Y 軸（柱） | 標題關閉 |
| 右 Y 軸（線） | 標題關閉、**範圍手動設 0–1** |
| X 軸 | 類型「**類別**」（用連續會出現小數刻度） |
| X／Y 軸值 | 字級 **12**、色彩 `#AAAAAA` |
| 資料標籤 | 關閉（17 根柱子放標籤會糊掉） |
| **圖例** | **關閉** |

🔴 **右 Y 軸一定要手動鎖 0–1。** 這頁的論點是「業配率穩定」，自動縮放會把
48%–57% 的波動放大成滿版高低起伏，**畫面直接反駁你的口白**。

**圖例為什麼要關**：它會顯示量值的內部名稱「業配數_受範圍影響」「套用資料範圍」——
那是維護用的命名，投影給評審看很怪。卡片標題「業配影片數與業配率」已按順序說明柱與線。

### 主圖的動態註記：「2026 年僅 1–7 月」🔴

柱子從 97 一路掉到 36，視覺上非常像「投放腰斬」，**跟這頁要說的「投放沒有減少」正好相反**。
而最後一根特別矮有技術原因：**資料只到 7 月底，不是完整年度**。

用**卡片（舊版）**放在主圖右上角的空白處（最高的柱在最左邊，右上是空的）：

| 高度 | 寬度 | 橫向 | 縱向 |
|---|---|---|---|
| **30** | **260** | **740** | **350** |

值 = `[最新年度提醒]`（見第 3-6 節），字級 **12**、色彩 `#717171`、顯示單位 `無`、類別標籤關閉。

⚠️ **絕對不要用文字方塊寫死「2026 年僅 1–7 月」。** 資料每日更新，八月有新片就過期，
而且會讓「本報表每日自動更新」這個賣點自相矛盾。

**要說的話**：量會隨產能起伏（2022 年達峰），但**密度是穩的**。看線不看柱。

### 側上｜業配與非業配的觀看對照

這張卡的內容區高 205px，拆成**上圖下註記**兩個物件。

**圖表：群組橫條圖**

| 高度 | 寬度 | 橫向 | 縱向 |
|---|---|---|---|
| **165** | **796** | **1070** | **338** |

| 欄位區 | 放什麼 |
|---|---|
| Y 軸 | `業配標籤`（第 3-1 節新建的計算欄位） |
| X 軸 | `[中位觀看]` |

🔴 **視覺層級篩選**：`video_format` = `長片`（`[中位觀看]` 沒有內建長片條件）

| 項目 | 設定 |
|---|---|
| 資料色彩 | 兩根**同色** `#A0A0A0` |
| 資料標籤 | 開啟、**顯示單位 `無`**、字級 14、`#F1F1F1` |
| **X 軸** | **關閉**，但**先設 `範圍 → 結束 = 800000`** 再關 |
| Y 軸 → 值 | 字級 14、`#AAAAAA` |
| 圖例 | 關閉 |

**兩根為什麼同色**：分成一深一淺會暗示「業配這根比較重要／比較好」，
那跟這張圖要**否定**的結論同方向，是反效果。同色＋註記才是正確的表達。

**註記文字方塊**

| 高度 | 寬度 | 橫向 | 縱向 |
|---|---|---|---|
| **34** | **796** | **1070** | **508** |

> 控制系列後，五個系列的 95% 信賴區間全部跨越零

字級 **12**、`#717171`、靠左、背景關閉。

**位置放最底部是刻意的**——標準腳註配置：讀者先看數字，最後才發現有但書。
往上移會變成「圖表的一部分」，讀者先讀警語再看資料，反而削弱圖表。

**這張圖存在的目的是自我反駁。**「業配影片表現更好」是一個很好用、很直覺、
而且錯的結論。正確說法是「**顯著 → 消失**」，不是「縮小到 4.3%」——
`DAX.md` 特別警告不要寫成後者，因為那等於承認還有 4.3% 的效果。

### 🔴 側上圖的年份窗口（2026-08-03 查證）

**文件裡那個「+27.4%」從來沒標註年份**，實測依起點差異極大：

| 年份起點 | 業配中位 | 非業配中位 | 差距 |
|---|---|---|---|
| **2010（頁面預設）** | 630,575 | **27,884** | **+2161%** |
| 2018 | 640,606 | 584,895 | +9.5% |
| 2021 | 640,314 | 500,976 | +27.8% |
| **2022（演出停點）** | 630,575 | 486,275 | **+29.7%** |

全期會爆到 22 倍，是因為非業配那組混進了 **696 支遊戲實況（中位 2,710）**
與 **436 支電競賽事（中位 3,924）**——**那是年代效應，不是業配效應**。

**這不是設計錯誤**：演出腳本第 ⑤ 步本來就在年份拉到 2022 之後才講側圖，
那時畫面上是 **+29.7%**，與「業配影片看起來表現更好」的講稿相符。

⚠️ **風險在開場那 25 秒**：畫面停在全期，側上圖是一根巨大長條對一根細絲。
觀眾若先看那裡會得到錯誤印象。**已決定不加視覺層級篩選**（2026-08-03），
理由是多一張「年份與頁面不同」的圖需要額外解釋，而開場觀眾的視線在主圖與 KPI。
**代價是台上絕對不能在開場提到側上圖。**

被問到時的答案現成：「全期的差距是年代效應——2016 年以前的遊戲實況把非業配那組
拉到 2.8 萬。同一年代比較才有意義，所以我在 2022 年這個窗口講它。」

### 側下｜業配影片的類型組成

**圖表：群組橫條圖**

| 高度 | 寬度 | 橫向 | 縱向 |
|---|---|---|---|
| **175** | **796** | **1070** | **661** |

| 欄位區 | 放什麼 |
|---|---|
| Y 軸 | `category` |
| X 軸 | `[業配影片數]` |

🔴 **視覺層級篩選要加兩個**：
1. `video_format` = `長片`
2. `category` → 前 N 項 → **前 5 項**，依 `[業配影片數]` 遞減

| 項目 | 設定 |
|---|---|
| 資料色彩 | 全部 `#A0A0A0`（紅色額度已給主圖的線） |
| 資料標籤 | 開啟、字級 14、`#F1F1F1` |
| **X 軸** | **關閉**，但**先設 `範圍 → 結束 = 270`** 再關 |
| Y 軸 → 值 | 字級 14、`#AAAAAA` |

**腳註文字方塊**

| 高度 | 寬度 | 橫向 | 縱向 |
|---|---|---|---|
| **30** | **796** | **1070** | **836** |

> 僅顯示前 5 類

字級 **12**、`#717171`、靠左。

**為什麼需要這行**：卡片標題寫「類型**組成**」，字面上像完整分布，
但前 5 類只佔 432／521 支——有人加總會發現對不上 KPI。
**刻意不寫百分比**，那會隨年份範圍變動、寫死會過期。

**要說的話**：挑戰企劃貢獻最多業配影片——**這是通往第 5 頁的鉤子**。

### 篩選器 ×2

| # | 標籤（PNG 已印） | 視覺 | 高度 | 寬度 | 橫向 | 縱向 |
|---|---|---|---|---|---|---|
| 1 | 資料範圍 | **按鈕交叉分析篩選器** | 56 | 720 | 232 | 964 |
| 2 | 年份區間 | 交叉分析篩選器（介於） | **72** | **690** | **1152** | **930** |

#### 資料範圍：用「按鈕交叉分析篩選器」🔴

**不要用標準交叉分析篩選器。** 標準版的圖磚樣式**沒有「已選取」狀態的色彩設定**
（選取色取自佈景主題，改起來影響全域且不一定生效），兩顆膠囊選了跟沒選幾乎一樣。

按鈕篩選器有完整的分狀態設定：`按鈕` → `將設定套用至` → **`狀態`** 下拉。

| 狀態 | 背景 | 文字 |
|---|---|---|
| 預設 | `#272727` | `#AAAAAA` |
| 暫留 | `#3F3F3F` | `#FFFFFF` |
| **已選取** | **`#F1F1F1`** | **`#0F0F0F`** |

其他：版面配置**橫向 2 欄**、間距 8、單一選取**開啟**、圖形**圓角矩形半徑 18**、
`按鈕 → 邊界` **每個狀態都要關**（只關一個狀態，另一個狀態的框線還是會露出來）。

🔴 **一般 → 效果 → 背景，以及版面配置 → 背景，兩層都要關閉**，
否則會有一個白色圓角矩形把兩顆膠囊整個包起來。

**已選取為什麼是白底不是紅底**：這頁的紅色額度已經給主圖的折線。
而且白底黑字正是 YouTube 自己的篩選膠囊樣式，跟整個版面的設計語言一致。

#### 年份區間：標籤在左、控制項在右 🔴

**「介於」樣式需要橫向空間**（兩個輸入框並排＋下面一條滑桿），
塞在標籤正下方的 56px 裡**永遠擠不出滑桿**。

背景圖的篩選器容器實際畫到 **x=1846、y=1032**，而「年份區間」原本只分配 480px 寬，
右邊還有一大片沒用到。把控制項移到標籤右側（橫向 1152）就有足夠空間。

- 橫向 1152：「年份區間」四字（27px）約佔到 x=1120，留 32px 間隙
- 縱向 930、高度 72：垂直置中於容器，與左邊的標籤並排

其他：交叉分析篩選器標頭**關閉**、值字級 **14**、`#AAAAAA`、滑桿色彩 `#A0A0A0`。

🔴 **預設範圍設為 2010–2026（全期）**，見下方演出設計。

### 演出：三個停點 ＋ 一次切換

**這頁的敘事是「從 2010 年逐步講述頻道變化」**，所以年份預設在全期。

| 停點 | KPI 長片業配率 | 業配數 | 推估 | 切「全部」後 | 落差 |
|---|---|---|---|---|---|
| **2010（預設）** | **17.0%** | 513 | 601 | 15.4% | **1.5pp** |
| 2017 | 31.4% | 508 | 596 | 25.5% | 5.9pp |
| 2020 | 45.1% | 461 | 540 | 32.5% | 12.6pp |
| **2022** | **53.3%** | 369 | 433 | **34.0%** | **19.3pp** |

#### 🔴 順序不可對調

看最右欄。**全期時切「全部」只掉 1.6pp**——Shorts 是 2021 年才開始拍的，
在全期分母裡佔比小，稀釋效果幾乎看不見。**拉到 2022 之後才有 19.3pp 的落差**，
「腰斬」的畫面才成立。這是資料結構決定的，不是編排偏好。

#### 台詞與節奏（約 110 秒）

**① 開場（停在 2010–2026，約 25 秒）**
> 「這是這個頻道十六年的完整歷史。KPI 顯示長片業配率只有 **17%**——但這個數字沒有意義。
> 你看圖，**2017 年以前這條紅線幾乎貼著地板**。那不是廠商不投，
> 是那個年代台灣還沒有 YouTube 業配這回事。」

**② 拉到 2020（約 20 秒）**
> 「往後推到 2020 年，變成 **45%**。2018 年破兩位數、2020 年疫情之後開始快速爬升。」

**③ 拉到 2022（約 20 秒）**
> 「再推到 2022 年——**53%**。而且從這裡開始這條線就走平了：54、54、48、55、57。
> **這才是這個頻道現在的水準。**」

**④ 切資料範圍（約 30 秒）**
> 「但如果我把 Shorts 也算進來——**掉到 34%**。那不是廠商跑了。
> 是我加進來的 **473 支** Shorts 裡，只測得到 **27 支**業配。
> 85.7% 的 Shorts 說明欄完全空白，文字比對測不到東西。」

**⑤ 側圖（約 15 秒）**

🔴 **側上圖只能在這一步講，年份必須已經停在 2022。** 理由見下方〈側上圖的年份窗口〉。

⚠️ **開場的 17.2% 是刻意的「反面數字」，但必須在 10 秒內解釋掉**，
否則會變成觀眾記住的數字。「但這個數字沒有意義」要緊接在報出 17% 之後，中間不能停頓。

⚠️ 覺得太趕就**砍掉第 ② 站**，直接 2010 → 2022 兩段，多出 20 秒喘息。
損失的只是「疫情後爬升」這個細節，主線不受影響。

### 本頁口白：2023/11/06 事件（不加視覺）

內容見 `DAX.md` 第四節第 4 頁段落，完整數據見 `HANDOFF.md` 第十五章。
**時間分配尚未定案**（`DAX.md` 第 9-1 節）。

---

## 第 5 頁｜投放偏好

座標檔：`assets/layout_page5.csv`　背景：`bg_page5.png`
**這頁的一句話**：業配是流量的結果，不是原因。

> ⚠️ **本頁論證權重**：主圖是配角（呈現分布），**側上圖才是主要因果證據**。
> 理由見 `HANDOFF.md` 第九章。

### KPI ×4

| # | 標籤（PNG 已印） | 視覺 | 座標 | 值 | 格式 |
|---|---|---|---|---|---|
| 1 | 業配率最高類型 | 卡片 | 高 64、寬 339、橫 226、縱 145 | `[業配率最高類型]` | 文字 |
| 2 | 業配率最低類型 | 卡片 | 高 64、寬 339、橫 655、縱 145 | `[業配率最低類型]` | 文字 |
| 3 | 類型間差距 | 卡片 | 高 64、寬 339、橫 1084、縱 145 | `[類型間差距]` | 百分比，1 位小數 |
| 4 | 樣本數（長片） | 卡片 | 高 64、寬 339、橫 1513、縱 145 | `[樣本數_長片]` | 整數，千分位 |

⚠️ KPI2 目前會顯示 **遊戲實況**、KPI3 會顯示 **45.0pp**。**這是預期行為不是錯誤**，
但必須在台上解釋（見下方年代效應）。

### 主圖｜各類型的業配率

- **視覺類型**：橫條圖
- **座標**：高 528、寬 796、橫 212、縱 338

| 欄位區 | 放什麼 |
|---|---|
| Y 軸 | `category` |
| X 軸 | `[類型業配率]` |

**視覺層級篩選（兩個）**
1. `video_format` = `長片`
2. 🔴 **`[僅長片]` → 進階篩選 → 大於或等於 `20`**（2026-08-03 新增）

**格式**

| 項目 | 設定 |
|---|---|
| 資料色彩 | 條件式格式（fx）→ 格式樣式「**欄位值**」→ `[主圖色_最高標紅]`（第 3-3 節）|
| 排序 | 依 `[類型業配率]` 遞減 |
| 資料標籤 | 開啟、百分比 1 位小數、11pt、`#F1F1F1` |
| Y 軸 → 值 | 11pt、`#AAAAAA` |
| X 軸 | 關閉（先設 `範圍 → 結束 = 0.55` 再關，避免最長那根的標籤被裁） |
| 標題／背景／視覺效果邊框／回應式 | 全關 |

### 🔴 樣本門檻 `≥20`：主圖必須跟 KPI 用同一把尺

原本「全部 14 個類型都保留」（2026-08-01 決議）**與 KPI 衝突**：主圖不設門檻時
**財經/投資 66.7%（4/6 支）會排在第一名**，而 `[業配率最高類型]` 內部有 `>=20`，
KPI1 說的是「挑戰/企劃 45.7%」。結果會是**標紅的是第二根**，而且你說「挑戰企劃最高」時
螢幕最上方寫著 66.7%。

`[僅長片]` 是基礎層既有量值（`CALCULATE(COUNTROWS('videos'), video_format="長片")`），
**不含觀看數門檻**，所以切換門檻時「哪些類型上場」不會跟著變。

**只擋掉財經/投資一個**，其餘 13 個全部保留（含業配率 0.7% 的遊戲實況），年代效應的演出不受影響。

**怎麼在台上說明這道門檻**——報 KPI 時加五個字帶過即可：

> 「**樣本 20 支以上的類型裡**，業配率最高的是挑戰／企劃，45.7%。」

被追問時給數字不給原則：財經/投資 6 支、4 支業配，66.7% 的 95% 信賴區間是
**[30.0%, 90.3%]**，寬達 **60 個百分點**（對照挑戰／企劃 490 支，區間 [41.4%, 50.1%]，
只有 8.8pp）；6 支樣本**一支影片就跳 16.7 個百分點**，那個區間同時涵蓋第一名到第五名，
**根本排不出名次**。

最後補這一句最有力：**「這道門檻砍掉的是最高的那一根，不是最低的。想讓圖更有戲劇性
我應該留著 66.7%。拿掉它是因為它不可信，不是因為它不好看。」**

⚠️ 目前沒有任何類型落在門檻邊緣（次低是寵物 30 支），資料每日更新不會讓這條線搖擺。

### 🔴 主圖的年代效應（逐項揭露時必須主動說明）

會出現兩根接近零的長條：

| 類型 | 長片數 | 業配率 | 年代集中在 |
|---|---|---|---|
| 遊戲實況 | 699 | 0.7% | **2015–2016 佔 90.7%** |
| 電競賽事 | 446 | 2.5% | 2010–2011 佔 64.6% |

**這是年代效應，不是廠商偏好**——長片業配率 2010–2016 全部在 5% 以下，
2017 年 5.2%、2018 年才首次進入兩位數 11.3%，當時業配文化尚未成形。

> 台詞：「這兩根接近零的不是廠商不投，是那個年代還沒有業配這回事。」

### 側上｜廠商進場時序　⭐ 本頁主角　✅ 已完工（2026-08-03）

**視覺類型**：群組直條圖

**① 大小及位置**

| 高 | 寬 | 橫 | 縱 |
|---|---|---|---|
| 205 | 796 | 1070 | 338 |

**② 放入的量值／欄位**

| 欄位區 | 放什麼 |
|---|---|
| X 軸 | `published_year` |
| Y 軸 | `[類型業配率]` |

視覺層級篩選：`video_format` → 勾 **長片**

**③ 詳細設定**

| 項目 | 值 |
|---|---|
| 資料色彩 | `#A0A0A0` |
| **Y 軸 → 範圍** | 🔴 **手動鎖 0–1** |
| X 軸 → 類型 | **類別** |
| X／Y 軸 → 值 | 11pt、`#AAAAAA` |
| 資料標籤 | 開啟、百分比 **0 位小數**、10pt、`#F1F1F1` |
| 圖例／Y 軸標題 | 關 |
| 標題／背景／視覺效果邊框／回應式 | 全關 |

🔴 **Y 軸鎖 0–1 是這張圖的命脈。** 不鎖的話每點一個系列軸就重新縮放，
「從 0% 爬到 92%」的戲劇性會被自動縮放整個吃掉——觀眾看到的永遠是「一根矮的、一根高的」。

**✅ 驗收（預設狀態、未選任何系列，17 根）**

```
2010  0%   2011  0%   2012  4%   2013  0%   2014  0%   2015  0%
2016  0%   2017  5%   2018 11%   2019  7%   2020 23%   2021 33%
2022 54%   2023 54%   2024 48%   2025 55%   2026 57%
```

🔴 **2010／2011／2013／2014 需要 `業配率` 的 `+ 0` 修正才會出現**——那四年業配數是 0，
`COUNTROWS` 回傳 BLANK，`DIVIDE ( BLANK, n )` 也是 BLANK，整個年份被視覺物件丟掉。
與第 6 頁側上圖 2010 消失是**同一個 BLANK 陷阱**，見 `DAX.md` 第三節。

**這張預設畫面本身就是主圖那兩根接近零長條的完整解釋**：2017 年以前七年全部貼地，
2018 年才首次進入兩位數。

**這是全報告最乾淨的因果證據**：同一系列、同一種內容、同一個主持人，
唯一在變的是「已經累積的成績」。它控制住了橫向比較控制不住的變因。

**演出：點《Joe是要看房》**

| 年 | 2020 | 2021 | 2022 | 2023 | 2024 | 2025 | 2026 |
|---|---|---|---|---|---|---|---|
| 業配率 | **0%** | **0%** | **43%** | 41% | **92%** | 33% | 75% |
| 樣本 | 13 | 8 | 21 | 22 | 12 | **9** | **4** |

🔴 **講到 2024 年的 92% 就收**，不要往後帶。2025 掉到 33%、2026 回到 75%，樣本只剩 9 支與 4 支，
那是抽樣雜訊。被問到就說「最後兩年樣本只剩個位數，不看」——跟 KPI 用 `≥20`、
側下圖用 `≥10` 是同一套標準，說法前後一致。

**備援（被質疑是特例時點）《Joe是要對決》**，樣本 307 集：

```
2018 18%(17)   2020 31%(36)   2021 40%(52)   2022 76%(53)   2023 86%(51)   2024 64%(53)   2025 73%(45)
```

### 側下｜系列的業配密度排序　✅ 已完工（2026-08-03）

**視覺類型**：群組橫條圖

**① 大小及位置**

| 高 | 寬 | 橫 | 縱 |
|---|---|---|---|
| 205 | 796 | 1070 | 661 |

**② 放入的量值／欄位**

| 欄位區 | 放什麼 |
|---|---|
| Y 軸 | `series` |
| X 軸 | **`[系列業配密度]`** |

```dax
系列業配密度 =
IF ( [系列長片數_不受門檻] >= 10, [類型業配率] )
```

⚠️ **這個量值一定要設格式**：`量值工具` → 格式 **百分比**、小數位數 **1**。
不設會顯示成 `0.7`／`0.6` 而不是 `70.0%`／`60.6%`。

**視覺層級篩選（兩個）**
1. `系列世代` → **取消勾選「無系列」**（不要用 `series` 的「不是空白」，null 與空字串只擋得掉前者）
2. `series` → 前 N 項 → **上 6**，依值 **`[系列業配密度]`**

**③ 詳細設定**

| 項目 | 值 |
|---|---|
| 資料色彩 | 全部 `#A0A0A0`（紅色額度已給主圖那根最高的） |
| 資料標籤 | 開啟、百分比 1 位小數、11pt、`#F1F1F1` |
| **X 軸** | 先設 `範圍 → 結束 = 0.85` 再關閉 |
| Y 軸 → 值 | 11pt、`#AAAAAA`；標題關 |
| 排序 | 依 `[系列業配密度]` 遞減 |
| 標題／背景／視覺效果邊框／回應式 | 全關 |

**✅ 驗收（6 列）**

| 系列 | 集數 | 業配率 |
|---|---|---|
| 我獨自穿越日本 | 10 | **70.0%** |
| Joe是要對決 | 307 | **60.6%** |
| Joe是要看房 | 89 | 39.3% |
| 靈能的挑戰 | 26 | 30.8% |
| Joeman開箱去 | 333 | 25.2% |
| 網紅觀察室 | 43 | 18.6% |

### 🔴 為什麼集數門檻要包進量值，而不是用視覺層級篩選

**不設門檻的話前五名全是 2–3 集的系列、清一色 100%**：

```
Joe是要切開 2集 100%   Joe是要闖關 2集 100%   愛吃大胃王爭霸戰 2集 100%
公車食刻表 3集 100%    加密貨幣三部曲 3集 100%
```

「前 6 項」會排出一張全是陌生小系列的圖，完全達不到「把視角從類型搬到系列」的目的。

**但把 `[系列長片數_不受門檻] >= 10` 加成視覺層級篩選會失敗**——實測只剩一列。
原因見第四節第 ⑥ 點：**Top N 先算**，它在門檻生效前就從全部系列挑出最高的 6 個
（五個 100% 的小系列 ＋ 我獨自穿越日本），門檻才把前五個砍掉。

解法是把兩個條件包進同一個量值：集數不足 10 回傳 BLANK，而 **Top N 不會挑到 BLANK**，
先後順序的問題就消失了。這與第 6 頁 `首次業配集數_顯示` 是同一個寫法。

**門檻 `≥10` 怎麼在台上說明**：跟主圖 `≥20` 是同一個道理、不同尺度——

> 「系列的規模比類型小一個數量級。**少於 10 集時，一支影片就讓業配率跳超過 10 個百分點**，
> 排不出可信的名次。所以類型我要求 20 支，系列我要求 10 集。」

被問到那些 100% 的小系列：《Joe是要切開》只有 2 集、2 支都有業配——
**那不是密度高，那是樣本只有 2**。

**功能**：把觀眾的視角從「類型」搬到「系列」，第 6 頁整頁都是系列層級的分析。

### 篩選器

> 🔧 **背景圖已於 2026-08-01 重產為三格版**（原本只有「系列」與「觀看數門檻」兩格）。

| # | 標籤（PNG 已印） | 座標 | 欄位 | 樣式 |
|---|---|---|---|---|
| 1 | 系列 | 高 56、寬 480、橫 232、縱 964 | `videos[series]` | 下拉式、**單選** |
| 2 | **類型** | **高 56、寬 480、橫 772、縱 964** | `videos[category]` | 下拉式、**多選** |
| 3 | 觀看數門檻 | 高 56、寬 534、橫 1312、縱 964 | `門檻[門檻標籤]` | 圖磚、單選 |

**類型篩選器的設定**
- 樣式：下拉式
- `選取項目` → **允許多選**：開啟
- `選取項目` → 顯示「全選」選項：**開啟**（演出結束要一鍵還原全部 14 個類型）
- 預設狀態：**全選**（主圖預設顯示全部類型，逐項加入是演出時才做的動作）

⚠️ **上台前必須實測一件事**：這個篩選器在 Y=964，下方只剩 116px。
**Power BI 下拉選單的展開方向（向下或向上）我無法在沒開 Power BI 的情況下確認**，
若展開後被畫布底緣裁掉、只看得到兩三個項目，就無法逐項點選。

實測後若真的被裁掉，兩個備案：
1. 把「類型」與「系列」對調位置無濟於事（同一列），改把類型篩選器**改放主圖卡片內部左側**
   （縮窄主圖至 X=212 寬 596，篩選器放 高 528、寬 180、橫 212、縱 338，清單樣式垂直排列 14 項）
2. 改用第八節方向 C（年份滑桿演出年代效應），不需要類型篩選器

### 演出

1. 逐項加入類型觀察分布，加到遊戲實況／電競賽事時**主動說明年代效應**
2. 點《Joe是要看房》→ 側上圖顯示 **0% → 0% → 42.9% → 40.9% → 91.7%**
   > 台詞：「廠商是在企劃證明自己之後才進場，不是廠商的錢造就了企劃。」
3. 若被質疑是特例，**當場再點《Joe是要對決》**（17.6% → 86.3%，樣本 307 集）

🔴 **不要點《Joeman開箱去》**——它 2016–17 年說明欄只有 158 字，
0% 是偵測失效不是事實。

---

## 第 6 頁｜接棒動能　🔧 施工中（方案丁）

座標檔：`assets/layout_page6.csv`　背景：`bg_page6.png`（**2026-08-02 重產兩次**）
**這頁的一句話**：招牌停了，但接棒的三件事都到位了——**速度**（停播 9 天後開播）、
**戰力**（單集中位觀看與《對決》最後一年持平）、**廠商信任**（第一集就進場，
而歷史上要等到第 91 集）。**唯一缺的是規模：19 集對 307 集。**

> 🔴 **背景圖改過兩次，Power BI 裡務必重新套用一次畫布背景。**
> ① 篩選器寬度 600/960 → **830/664**（年份滑桿需要橫向空間，見〈篩選器 ×2〉）
> ② KPI4 標籤「新系列業配率」→ **「首集就有業配」**、側下卡片標題
> 「新系列動能對照（2025 年起）」→ **「首次業配出現在第幾集」**（方案丁，見下）

### 為什麼改成方案丁（2026-08-02 決議）

原設計的收尾論點是「新系列業配率 65.4% ＞ 既有系列 56.1%，證明廠商信賴已轉移」。
**實測後這個宣稱不成立**——自助抽樣 95% 信賴區間 **[−12.0, +29.9]pp 跨越零**，
照 `HANDOFF.md` 第七章第 7 點自己訂的標準不能下結論。

改用**首集就有業配**這個二元類別觀察：

| | 系列數 | 第 1 集就有業配 |
|---|---|---|
| 既有系列（集數 ≥3） | 22 | **2 個（9%）** |
| **新系列** | **4** | **4 個（100%）** |

若兩者機率相同（9%），連續四個都命中的機率是 **0.01%**。
**這是類別觀察不是比率比較，不需要信賴區間就成立。**

考慮過的四個方案與取捨（故事力／說服力／數據呈現／流暢度）：

| | 甲 零改動 | 乙 只換側下 | 丙 換三處 | **丁 換兩處 ✅** |
|---|---|---|---|---|
| 故事力 | ★★ | ★★★ | ★★★★★ | ★★★★ |
| **說服力** | ★★ | ★★★★ | ★★★ | **★★★★★** |
| 數據呈現 | ★★ | ★★★★ | ★★★★★ | ★★★★★ |
| 流暢度 | ★★★ | ★★★ | ★★★★★ | ★★★★ |

**丙被否決的兩個理由**：① KPI2 想放「接棒間隔 9 天」，但那是 **n=1 的時間巧合＋因果推論**
（《我獨自穿越日本》是旅遊類、《對決》是挑戰企劃類，類型不同，「接棒」是詮釋不是事實）；
② KPI 列會從「2 壞 2 好」變成「1 壞 3 好」，在一份自己拆掉業配溢價、自己列 14 條限制的
報告裡失去平衡。**丁 只換掉不顯著的那一格，其餘不動。**

### KPI ×4

| # | 標籤（PNG 已印） | 視覺 | 座標 | 值 | 格式 | 屬性 |
|---|---|---|---|---|---|---|
| 1 | 《對決》佔頻道觀看 | 卡片 | 高 64、寬 339、橫 226、縱 145 | `[對決佔頻道觀看]` | 百分比 1 位 | 🔴 缺口 |
| 2 | 長片系列化比例 | 卡片 | 高 64、寬 339、橫 655、縱 145 | `[長片系列化比例]` | 百分比 1 位 | 🔴 產線鬆動 |
| 3 | 新系列中位觀看 | 卡片 | 高 64、寬 339、橫 1084、縱 145 | `[新系列中位觀看]` | 整數千分位 | ✅ 戰力 |
| 4 | 首集就有業配 | 卡片 | 高 64、寬 339、橫 1513、縱 145 | **`[首集就有業配]`** | **文字，不用設** | ✅ 信任 |

**驗收值**

| | 全期 2010–2026（預設） | 拉到 2025 起 |
|---|---|---|
| 《對決》佔頻道觀看 | **21.1%** | **21.1%** 🔴 必須不變 |
| 長片系列化比例 | **36.6%** | **55.7%** |
| 新系列中位觀看 | **634,262** | 634,262 |
| 首集就有業配 | **4 / 4** | 4 / 4 |

**兩壞兩好**是原設計「風險與希望並陳」的用意，方案丁保留了它。
KPI4 顯示「4 / 4」而不是「100%」——把分母攤在檯面上，跟第 4 頁 KPI2 標「下限」同一個作風。

### 主圖｜系列 IP 的規模與效率　✅ 已完工（2026-08-03）

**視覺類型**：**散佈圖**

**① 大小及位置**

| 高 | 寬 | 橫 | 縱 |
|---|---|---|---|
| 528 | 796 | 212 | 338 |

**② 放入的量值／欄位**

| 欄位區 | 放什麼 |
|---|---|
| 值（詳細資料） | `series` |
| X 軸 | `[系列正片數]` |
| Y 軸 | `[長片中位觀看]` |
| 大小 | `[系列總觀看]`（第 3-4 節） |
| 圖例 | `系列世代` |
| **工具提示** | `[系列正片數]`、`[長片中位觀看]`、`[系列總觀看]` |

🔴 **視覺層級篩選**：`系列世代` **取消勾選「無系列」**　＋　`[系列正片數]` **≥ 3**

⚠️ **不要用 `series` 的「不是空白」**。`series` 空白值匯入後可能是 null 也可能是空字串，
「不是空白」只擋得掉前者，漏掉的會變成第三種顏色的一大坨氣泡且不報錯（第七節第 7 點）。

**為什麼是 ≥3**：門檻 ≥10 時只有《我獨自穿越日本》一個新系列能露臉（其餘三個各 3 集），
畫面撐不起「新系列」這一群。≥3 之後 26 個系列、**四個新系列全部現身**。

| 既有系列門檻 | 總氣泡 | 灰 | 紅 |
|---|---|---|---|
| 不設 | 29 | 25 | 4 |
| **≥3（採用）** | **26** | **22** | **4** |
| ≥5 | 23 | 19 | 4 |
| ≥10 | 18 | 14 | 4 |
| ≥20 | 15 | 11 | 4 |

**③ 詳細設定**

| 項目 | 值 |
|---|---|
| 標記 → 新系列 | `#FF0000`、**菱形** |
| 標記 → 既有系列 | `#8C8C8C`、圓形 |
| **類別標籤** | 🔴 **關閉**（理由見下） |
| X 軸 → 標題 | 開，「正片集數」 |
| Y 軸 → 標題 | 開，「中位觀看」 |
| X／Y 軸 → 值 | 12pt、`#AAAAAA` |
| **圖例** | 🔴 **必留**、頂端置中、12pt、`#AAAAAA` |
| 格線 | `#272727` |
| 標題／背景／視覺效果邊框／回應式 | 全關 |

🔴 **圖例是標籤關掉後畫面上唯一保證顯示的識別資訊**（它不會碰撞、不會被隱藏），
而且圖例符號會跟著標記形狀變成菱形，辨識更直接。整張圖的可讀性靠它撐著。

### 🔴 為什麼類別標籤是關的（不要再試著打開）

**Power BI 的散佈圖無法逐點控制標籤**，只有「全開」或「全關」。全開時 26 顆點會碰撞，
Power BI 隨機隱藏放不下的——實測**四顆紅點裡有兩顆被隱藏**（《公車食刻表》被《Joeman夾娃娃》
擠掉、《我獨自穿越日本》被《Joe是要收藏》擠掉），而 14 個無關的既有系列反而有名字。
**標籤開著等於在幫既有系列宣傳。**

三條想控制標籤的路都實測失敗，不要重走：

| 方法 | 結果 |
|---|---|
| 類別標籤色彩用條件式格式（fx）分色 | ❌ `類別標籤` 只有單一色票，**沒有 fx，也沒有「將設定套用至→序列」** |
| 計算欄位讓不重要的系列回傳唯一的零寬空白 `REPT(UNICHAR(8203),rk)` | ❌ Power BI 把零寬字串視為同一個值，**18 個系列被併成一顆假氣泡**（症狀：Y 軸範圍縮成 40 萬–140 萬、x≈300 多出一顆無名大灰泡） |
| 用排序控制標籤繪製優先權 | ❌ 散佈圖**沒有「排序軸」選項** |

其餘被否決的方向：**對數 X 軸**（可讓 25/26 個標籤顯示，但會壓縮 307 集對 3 集的落差，
而那片空白正是「規模缺口」的視覺陳述）；**減少點數**（使用者決定所有系列都留著）；
**疊兩張圖**（上下層軸範圍必須手動鎖死，但觀看數門檻演出會讓中位觀看衝破 200 萬，鎖死就爆軸）。

**最終決定：標籤全關，指認交給口白與工具提示。** 需要名字的只有六個系列——四個新系列
由顏色＋菱形成群辨識，《對決》《開箱去》孤立在 x=307／333（左邊最近的鄰居在 x=89），
口說「右邊那兩顆」不會有歧義。

⚠️ **代價**：完工檢查表要求每頁存 PNG 當現場備援，**無標籤的截圖沒有識別資訊**，
真要靠圖片講完得完全憑記憶指認。

### 🔴 「大小」欄位必須保留（曾經想移除，是錯的）

一度為了替標籤騰空間而移除大小欄位、改用統一標記。**移除是錯的**——統計上
`總觀看 ≈ 集數 × 中位觀看`（《對決》307×809,917 ≈ 2.49 億，實際 2.99 億）看起來冗餘，
但**修辭上它是這頁核心句的第三個視覺通道**：

| 核心句 | 視覺通道 |
|---|---|
| 「每集的成績有 78%」 | **Y 位置**：紅點在高處 |
| 「但總量只有 5%」 | **氣泡大小**：紅點很小 |
| 「缺的是集數」 | **X 位置**：紅點在最左 |

而且面積比是忠實的：新系列 1,506 萬 ÷《對決》2.99 億 ＝ 5.0%，半徑比 0.22，與畫面相符。
移除大小會讓「總量只有 5%」失去載體，也讓《對決》不再是最大的氣泡。

紅點過小的問題**不靠改大小解決**，靠 **菱形 ＋ 既有系列壓暗到 `#8C8C8C`**：
形狀在小尺寸下的辨識度比圓形高（有尖角、輪廓不對稱）。真正有風險的只有下方兩顆
（《我獨自穿越日本》53 萬、《九讀》45 萬，落在 45–65 萬那一叢灰點裡），上方兩顆
（133 萬、121 萬）那一帶只有《第九人稱》一顆灰點，紅色跳得出來。

**驗收（工具提示滑《Joe是要對決》）**：系列正片數 **307**、長片中位觀看 **809,917**、
系列總觀看 **299,162,357**。

**關鍵資料點**

| 系列 | 集數 | 中位觀看 | 總觀看 |
|---|---|---|---|
| Joeman開箱去 | 333 | 409,300 | 1.75 億 |
| **Joe是要對決** | **307** | **809,917** | **2.99 億** |
| Joe是要看房 | 89 | 699,959 | 8,639 萬 |
| Joeman Show | 52 | 972,565 | 8,487 萬 |
| **Joe是要跟團** | **3** | **1,326,637** | 377 萬 |
| **公車食刻表** | **3** | **1,208,638** | 367 萬 |
| 我獨自穿越日本 | 10 | 534,406 | 609 萬 |
| 九讀 | 3 | 450,731 | 153 萬 |

**《Joe是要跟團》與《公車食刻表》的中位觀看（132.7 萬、120.9 萬）超過《對決》的 81.0 萬。**
新系列不是圖表左下的小紅點，是**左上角的高點**——效率不只到位，甚至更高。

⚠️ 但**各只有 3 集**，且是 2026 年的影片還在累積。講的時候要說「三集」。

⚠️ 系列集數一律用正片算（`[系列正片數]` 已限長片）。Shorts 多是同標題的宣傳短片，計入會虛增集數。

### 側上｜長片系列化比例趨勢　✅ 已完工（2026-08-03）

**視覺類型**：群組直條圖

**① 大小及位置**

| 高 | 寬 | 橫 | 縱 |
|---|---|---|---|
| 205 | 796 | 1070 | 338 |

**② 放入的量值／欄位**

| 欄位區 | 放什麼 |
|---|---|
| X 軸 | `published_year` |
| Y 軸 | `[長片系列化比例]` |

視覺層級篩選：不需要（量值內部已限長片）

**③ 詳細設定**

| 項目 | 值 |
|---|---|
| 資料色彩 | `#A0A0A0` |
| **Y 軸 → 範圍** | 🔴 手動設 **0–1** |
| X 軸 → 類型 | **類別** |
| X／Y 軸 → 值 | 12pt、`#AAAAAA` |
| 資料標籤 | 關閉（17 年放不下） |
| 圖例 | 關閉 |
| 標題／背景／視覺效果邊框／回應式 | 全關 |

🔴 **Y 軸鎖 0–1 是關鍵**：不鎖的話自動縮放會把 35%–81% 的波動拉成滿版起伏，
「產線鬆動」看起來像斷崖，而這頁的說法是 −16.9pp 的緩慢下滑，**畫面會反駁口白**。

### 🔴 2010 年會整年消失 → 量值要加 `+ 0`

2010 年有 **200 支長片、有系列歸屬 0 支**。`WithSeries` 那個 `CALCULATE` 篩不到任何列時
回傳的是 **BLANK 不是 0**，`DIVIDE ( BLANK, 200 )` 也回傳 BLANK，**整個年份被視覺物件丟掉**，
X 軸從 2011 開始——而第 3、4 頁的主圖都是 2010–2026，跨頁對不起來。

解法是在分子加 `+ 0` 強制轉成 0（`DAX.md` 第三節已同步）：

```dax
RETURN DIVIDE ( WithSeries + 0, LongTotal )
```

改完 2010 會以零高度的柱子出現、軸標籤回來。**KPI2 不受影響**（非空值加 0 不變，仍是 36.6%）。

### 動態註記：2026 年僅 1–7 月

視線一定落在最後一根柱子（**38.1%**），但口白講的終點是 2025 年的 64.1%——
觀眾看到的跟聽到的不一致會被抓。用第 3-6 節那個現成的量值補一張卡片：

**卡片（舊版）**，值 = `[最新年度提醒]`

| 高 | 寬 | 橫 | 縱 |
|---|---|---|---|
| 24 | 250 | 1140 | 348 |

12pt、`#717171`、顯示單位無、類別標籤關、標題／背景／邊框全關。位置在圖的左上角
（2010–2016 那幾根都貼著地板，那片是空的）；實作時已往右移到繪圖區右上，兩處都可以。

⚠️ **絕對不要用文字方塊寫死**，資料每日更新，八月有新片就會過期。

**逐年實測值**

```
2017 73.7%   2018 46.9%   2019 37.3%   2020 35.6%
2021 75.0%   2022 81.0%   2023 74.1%   2024 74.5%
2025 64.1%   2026 38.1%  ← 只有 63 支、7 個月
```

🔴 **要說的話改成「2022 年 81.0% → 2025 年 64.1%」，不要用 2026。**

原本的講法是「81% → 38%」，有兩個問題：① **終點是不完整年度**（2026 只有 7 個月、63 支長片）；
② **起點是全期最高峰**（2022 的 81.0% 是十七年來最高）。用最高點對半年會把落差放大。

「產線鬆動」的結論仍然成立（−16.9pp），只是幅度沒那麼戲劇化——**這樣講反而更穩**。

⚠️ 刻意只算長片——含 Shorts 會混入格式效應，把 81%→64% 誇大成更大的落差。

### 側下｜首次業配出現在第幾集　⭐ 本頁核心證據　✅ 已完工（2026-08-03）

**視覺類型**：群組橫條圖

**① 大小及位置**

| 高 | 寬 | 橫 | 縱 |
|---|---|---|---|
| 205 | 796 | 1070 | 661 |

**② 放入的量值／欄位**

| 欄位區 | 放什麼 |
|---|---|
| Y 軸 | `series` |
| X 軸 | **`[首次業配集數_顯示]`**（第 3-7 節） |
| 圖例 | `系列世代`（用來上色，比條件式格式省事） |

視覺層級篩選：`系列世代` **取消勾選「無系列」**

📌 圖例放 `系列世代` 不會變成堆疊或分組——每個系列只對應一個世代值，仍是一列一根。

**③ 詳細設定**

| 項目 | 值 |
|---|---|
| 資料色彩 → 新系列 | `#FF0000` |
| 資料色彩 → 既有系列 | `#8C8C8C`（與主圖一致，同一頁不該有兩種灰） |
| 圖例 | **關閉**（主圖已說明紅灰含義，這張只有 205px，省下 20px 給列高） |
| 資料標籤 | **開啟**、9pt、`#F1F1F1` |
| **X 軸** | 🔴 **先設 `範圍 → 結束 = 115`，再把 X 軸整個關閉**（91 × 1.26，留給標籤） |
| Y 軸 → 值 | **9pt**、`#AAAAAA` |
| **Y 軸 → 內距** | 🔴 由預設 20% 降到 **10%** |
| 排序 | 依 `[首次業配集數_顯示]` **遞增** |
| 標題／背景／視覺效果邊框／回應式 | 全關 |

### 🔴 9 列會擠出捲軸，門檻要提高到 ≥50

本節原本算 205px ÷ 9 = 22.8px／列，**實測不夠**：11pt 中文加 Y 軸內距每列需要約 26px，
於是右緣長出捲軸，**被推到摺線下的正好是《Joeman開箱去》第 91 集**——
這頁收尾台詞「《開箱去》等到第 91 集」與「9% → 100%」對比的另一半就在那一列。

⚠️ **簡報絕對不能靠捲軸**：投影幕上沒人會注意到有捲軸，捲動時觀眾視線也會斷。

三段修法（由輕到重，實測是三項一起做才乾淨）：字級 11 → **9pt**、
Y 軸內距 20% → **10%**、量值門檻 `eps >= 40` → **`>= 50`**：

```dax
首次業配集數_顯示 =
VAR eps = [系列正片數]
VAR gen = SELECTEDVALUE ( 'videos'[系列世代] )
RETURN IF ( gen = "新系列" || eps >= 50, [首次業配集數] )
```

門檻改 50 會移除《網紅觀察室》（43 集、第 13 集），**不影響任何一句台詞，而留下 91 是必要的**。

**驗收值（8 列，由上而下）**

| 系列 | 集數 | 首次業配 | 色 |
|---|---|---|---|
| Joe是要跟團 | 3 | **第 1 集** | 紅 |
| 九讀 | 3 | **第 1 集** | 紅 |
| 公車食刻表 | 3 | **第 1 集** | 紅 |
| 我獨自穿越日本 | 10 | **第 1 集** | 紅 |
| Joeman Show | 52 | 第 7 集 | 灰 |
| Joe是要對決 | 307 | 第 7 集 | 灰 |
| Joe是要看房 | 89 | 第 25 集 | 灰 |
| Joeman開箱去 | 333 | **第 91 集** | 灰 |

📌 《九件事》46 集雖然符合舊門檻，但它**從來沒有業配影片**，`[首次業配集數]` 回傳空白，
本來就會被自動排除——所以舊門檻 40 是 9 列不是 10 列。

🔴 **必須封鎖年份區間的互動**（見〈編輯互動〉）。「首次業配在第幾集」是系列的**歷史事實**，
被年份篩選後會在時間窗口內重算集序，數字直接變錯。

**這張圖與第 5 頁的「廠商進場時序」是一對**：第 5 頁講「以前廠商要等企劃證明自己」
（《看房》0%→0%→42.9%→91.7%），第 6 頁講「現在第一集就進場」。
**兩張圖說同一件事的兩半，第 5 頁埋的伏筆在第 6 頁引爆。**

### 篩選器 ×2

| # | 標籤（PNG 已印） | 視覺 | 高度 | 寬度 | 橫向 | 縱向 |
|---|---|---|---|---|---|---|
| 1 | **年份區間** | 交叉分析篩選器（介於） | **72** | **686** | **372** | **930** |
| 2 | **觀看數門檻** | 按鈕交叉分析篩選器 | **56** | **724** | **1122** | **964** |

⚠️ **兩者的縱向不同（930 vs 964）**——年份的控制項要跟左邊的標籤並排所以往上提，
門檻的膠囊維持在標籤下方那一列。實作時很容易把兩組座標貼串，貼完各自確認一次。

年份區間的容器是 232–1062，標籤佔到約 x=340，控制項從 372 起、寬 686px。

🔴 **觀看數門檻的寬度 2026-08-02 由 664 改為 724**（`make_background.py` 畫的群組寬度是 664，
但那樣右緣只到 1786，與左側 232 的內縮不對稱，整列往左偏）。改成 724 之後右緣
＝ 1122＋724 ＝ **1846**，與主圖／側圖的右緣切齊，5 顆膠囊每顆約 138px。
**背景圖不必重產**——群組寬度只影響標籤位置與分隔線，按鈕本來就是疊上去的。

⚠️ 膠囊若被裁，是高度不是寬度，見第四節第 ⑤ 點。

🔴 年份**預設全期 2010–2026**、門檻**點一下「全部」**讓它亮起來，再存檔。

### 編輯互動

| 篩選器 | 封鎖對象 | 原因 |
|---|---|---|
| **年份區間** | **KPI1《對決》佔頻道觀看** | 量值的分母已用 `ALL('videos')` 鎖住，**但分子仍會被篩**。不封鎖的話拉到 2025 起會變成 2.3%（對決 2025 年觀看 ÷ 全頻道總觀看），沒有意義 |
| **年份區間** | **側下圖（首次業配集數）** | 「第幾集」是歷史事實，被年份篩選後會在窗口內重算集序 |

觀看數門檻**完全不用封鎖**，它應該影響全頁。

### 演出

#### ① 開場鉤子（口白，不靠視覺）

> 「《對決》的最後一集叫『**是時候該放手了**』，2025 年 11 月 10 日，第 307 集。
> **九天後**，新系列開播——一趟從九州最南端開到北海道最北端、三千公里的自駕，
> 後來播了 78 天共 10 集。
> **那趟旅程不可能是那九天拍的。招牌宣布結束之前，接班的內容已經在路上了。**」

⚠️ 這段**完全可從資料驗證**（發布日 ＋ 標題裡的地名 ＋ 集數跨度），不需要外部知識。
Q&A 若被追問可再補：「那支停播宣告的影片，就是在這趟日本行程中錄的」——
**順序不能反**，先給可重現的推論、再給觀察當佐證。

#### ② 年份縮到 2025 起 →「戰力已追平」

| 2025 年起 | 集數 | 中位觀看 |
|---|---|---|
| 《Joe是要對決》最後一年 | 45 | **644,344** |
| 四個新系列 | 19 | **634,262** |

> 「把時間拉到同一年比，新系列的單集中位觀看是 63.4 萬，《對決》最後一年是 64.4 萬
> ——**相差 1.6%**。差的不是每集的成績，是集數。」

**這是同一時間窗口、同一頻道、同一批觀眾的對照**，比「新系列 vs 既有系列」乾淨得多，
因為它直接對上要被取代的那個標的。

#### ③ 觀看數門檻 →「規模缺口的具體長相」

| 門檻 | 《對決》 | 四個新系列 |
|---|---|---|
| 全部 | 307 集 | 19 集 |
| 100 萬↑ | **105 集** | **5 集** |
| 200 萬↑ | **22 集** | **0 集** |

> 「只看破兩百萬的爆款，《對決》有 22 支，新系列還是 0。**這就是缺口的具體長相**
> ——不是每集差，是還沒累積出爆款的量。時間才走了 0.7 年。」

**這一段刻意講壞消息**，主動承認缺口是真的，反而讓前面「戰力持平」的說法更可信。

#### ④ 收尾

> 「三件事都到位了：**九天就接上**、**單集戰力持平**、**廠商第一集就進場**。
> 缺的只有規模——19 集對 307 集，總觀看 5%。
> 而《開箱去》等到第 91 集才有第一支業配，《看房》等了 719 天。
> **廠商的信任對象，已經從『某個節目』變成『這個品牌』。**」

🔴 **必須主動說明的表面矛盾**：第 5 頁說「廠商在系列證明後才進場」，第 6 頁說「第一集就進場」。
解釋是**機制改變**——早期廠商評估個別系列，現在評估 Joeman 這個品牌。
**現在這個矛盾有數字了：9% → 100%。這是全報告最有力的收尾，不是漏洞。**

### 規模缺口的完整數字（備答用）

| | 集數 | 總觀看 | 單集中位 | 歷時 |
|---|---|---|---|---|
| 《對決》 | 307 | 2.99 億 | 809,917 | 7.6 年 |
| 四個新系列 | 19 | 1,506 萬 | 634,262 | 0.7 年 |
| **比例** | **6.2%** | **5.0%** | **78%** | — |

**每集的成績有 78%，但總量只有 5%。缺的是集數，不是能力。**

---

## 第 7 頁｜本報告的限制

座標檔：`assets/layout_page7.csv`　背景：`bg_page7.png`　無篩選器

| # | 物件 | 視覺類型 | 座標 | 資料 |
|---|---|---|---|---|
| 1 | 本報告的限制 | **表格** | 高 684、寬 1654、橫 212、縱 338 | `limitations` 表 |

**欄位（依序拖入）**：`類別`、`限制`、`對分析的影響`

### 🔴 排序：不能對 `類別` 設，要對 `限制` 設（2026-08-03 實測）

`類別` 只有三個值（看不到／判不準／不能推論），對它設「依資料行排序 → 索引」會被擋下來：

> 無法依 '類別' 為 '索引' 資料行排序。對於 '索引' 中的同一個值，在 '類別' 中不得有一個以上的值。

**Power BI 要求被排序的欄位與排序鍵是一對一。** `類別` 的「看不到」對應索引 1–5，違反這個規則。

**解法：改對 `限制` 設定**——它每列都不同，用同一個索引，結果一樣是原始順序。

1. `常用` → `轉換資料` → 點 `limitations` → `新增資料行` → `索引資料行` → `從 1` → `關閉並套用`
2. 資料窗格點 `limitations[限制]` → `資料行工具` → `依資料行排序` → 選 `索引`
3. 回報表，點表格的 **`限制`** 標頭一次

結果：看不到 5 列 → 判不準 5 列 → 不能推論 4 列。`類別` 那邊完全不用動。

📌 第 2 頁能對 `項目` 設排序，是因為 `項目` 每列唯一。**判準：只有唯一值的欄位才能當排序目標。**

**格式**（與第 2 頁相同，兩處差別）

| 項目 | 設定 |
|---|---|
| **資料行標題** | 🔴 **這頁要留著**（三欄需要標示）。文字色彩 `#AAAAAA`、背景色彩 `#1F1F1F`、13pt |
| 值 → 四個顏色 | 同第 2 頁（文字／替代文字 `#F1F1F1`，背景／變更背景 `#1F1F1F`） |
| 值 → 字級 | **13pt**（14 列比第 2 頁多 4 列） |
| 值 → 自動換行 | 開啟 |
| 格線 → 水平 | 開啟、`#272727`；垂直關閉；**框線關閉** |
| **列填補** | **4**（比第 2 頁的 8 小，14 列要省空間） |
| 資料行寬度 | `類別` 約 140px／`限制` 約 520px／`對分析的影響` 其餘 |

⚠️ 14 列若出現垂直捲軸，依序：列填補 4 → **2**，字級 13 → **12**。

⚠️ 14 列 × 684px 可能會出現捲軸。**簡報時捲軸是可接受的**——這頁是 Q&A 備用，
被問到才翻出來，需要時可以捲。但排練時要確認捲得順。

**這頁不是免責聲明，是論證的一部分。** 它跟第 2 頁是一對：
第 2 頁說「我知道的部分有多準」，第 7 頁說「我不知道的部分在哪裡」。

---

## 六、互動設定

Power BI 預設所有篩選器影響所有視覺物件。**有兩處必須關閉**：

| 頁 | 篩選器 | 要保護的物件 | 原因 |
|---|---|---|---|
| **3** | **影片格式** | **KPI ×4** | `[長片數]`／`[Shorts 數]`／`[長片中位觀看]` 內部用 `CALCULATE` 寫死格式**會覆蓋切片器**，只有 `[影片數]` 會變 → 一張動三張不動，看起來像壞掉 |
| **3** | **影片格式** | **側上折線圖** | 點 Shorts 時 X 軸會塌成 2021–2026（Shorts 2021 年才出現），變成標著「長片中位觀看」卻只有六年的矛盾圖 |
| **5** | **系列** | **KPI ×4 ＋ 主圖 ＋ 側下圖**（共 6 個） | 主圖是類型層級的比較，被系列篩選只剩一根柱子；側下圖是「排序圖」，塌成一列無意義；KPI1／KPI2 會**雙雙變成該系列的類型**、KPI3 顯示 **0.0pp**（標籤寫「類型間差距」卻沒有類型間） |
| **5** | **類型** | **KPI ×4 ＋ 側上圖 ＋ 側下圖**（共 6 個） | 只選一個類型時 KPI3 同樣是 0.0pp；側上圖是本頁**主要證據**，被類型篩選等於改掉它的定義；側下圖的系列業配率會被類型重算而失真 |
| 6 | 年份區間 | **KPI1《對決》佔頻道觀看** | 量值分母已用 `ALL` 鎖住，**但分子仍會被篩**；拉到 2025 起會變成 2.3%，沒有意義 |
| **6** | **年份區間** | **側下圖（首次業配集數）** | 「第幾集」是系列的歷史事實，被年份篩選後會在時間窗口內重算集序，數字直接變錯 |

**判準**：只有當某個視覺被篩選後會變得**錯誤或自相矛盾**時才封鎖。
年份區間在第 3、4 頁不需要任何封鎖（四個 KPI 都會一致變動）；
`門檻` 與 `資料範圍` 是中斷連結表，本身不篩選資料列，**也不需要封鎖**。

📌 **第 5 頁封鎖得比其他頁多，結果是「每個篩選器只驅動一張圖」**：
系列 → 側上圖、類型 → 主圖、觀看數門檻 → 全頁。KPI 列固定顯示全期事實。
這不是額外的保守，而是那頁的兩個篩選器都會讓 KPI3「類型間差距」退化成 0.0pp——
一個標籤與內容自相矛盾的畫面，正好符合上面的判準。

**設定方式**：選取要保護的視覺物件 → `格式` → `編輯互動` → 對該篩選器點「**無**」。

⚠️ 第 6 頁的 KPI1 量值本身已用 `ALL('videos')` 鎖住分母，但**篩選器仍會影響分子**，
所以編輯互動這一步不能省。

---

## 七、完工檢查表

每頁做完後逐項確認：

### 版面
- [ ] 畫布 1920×1080，背景 PNG 透明度 **0%**、影像調整「符合」
- [ ] 沒有任何視覺物件顯示自己的標題／背景／邊框
- [ ] 所有座標是**打字輸入**的，不是拖曳的
- [ ] 縮放到 100% 檢查：文字沒有壓到背景圖的卡片標題列

### 數字
- [ ] 第 4 頁年份區間**預設 2010–2026（全期）**，KPI 顯示 **17.0%**——這是刻意的「反面數字」
- [ ] 第 4 頁拉到 2022 → KPI 變 **53.3%**；再切「全部」→ 掉到 **34.0%**
- [ ] 🔴 演出順序**先拉年份、後切資料範圍**（全期時切換只差 1.6pp，演不出來）
- [ ] 第 5 頁 KPI2 顯示「遊戲實況」、KPI3 顯示 45.0pp（預期行為）
- [ ] 第 6 頁年份縮到 2025 起 → 新系列 65.4%、既有系列 56.1%
- [ ] 所有百分比都是 1 位小數，所有計數都有千分位

### 互動
- [ ] 第 3 頁格式三步揭露會動
- [ ] 第 5 頁點《看房》，側上圖出現 0→0→42.9→40.9→91.7
- [ ] 第 5 頁點系列時，主圖**不變**（編輯互動已設「無」）
- [ ] 第 6 頁拉年份時，KPI1 **不變**

### 軸鎖定 🔴
- [ ] 第 4 頁主圖右 Y 軸鎖 0–1
- [ ] 第 5 頁側上圖 Y 軸鎖 0–1
- [ ] 第 6 頁側上圖 Y 軸鎖 0–1

### 收尾
- [ ] 每頁存一張 PNG 截圖備份（現場出狀況還能用圖片講完）
- [ ] .pbix 複製一份到雲端硬碟或隨身碟（`.gitignore` 排除 `*.pbix`，**不會進版控**）

---

## 八、待決事項

### 8-1 第 5 頁的「類型」篩選器　✅ 已解決（2026-08-01）

**需求**：14 個類型全部保留，用篩選器**逐項加入觀看**，
把年代效應現場演出來而不是事先濾掉。

**原問題**：`bg_page5.png` 只印了兩格篩選器標籤，沒有留給「類型」的位置。

**已處理**：`src/make_background.py` 第 5 頁的 `slicers` 改為三格並重產背景圖與座標表。

```python
# 舊
"slicers": [("系列", 780), ("觀看數門檻", 780)],
# 新
"slicers": [("系列", 480), ("類型", 480), ("觀看數門檻", 534)],
```

```bash
python -m src.make_background 5
```

篩選器列可用寬度 1614px（起點 X=232，每格間隔 60px），
所以三格的寬度總和上限是 **1494**（480＋480＋534＝1494，剛好用滿）。
第 3 頁早就在跑三格版（380／690／420），版面是驗證過的。

⚠️ 舊版 PNG 在 git commit `f32695a` 裡，要還原隨時可以取回。

**仍待實測**：下拉選單在畫布底緣的展開方向，見第 5 頁篩選器段落的警語與兩個備案。

### 8-1b 演出方式的備選（若逐項點選在台上不順）

| 方向 | 做法 | 優點 | 缺點 |
|---|---|---|---|
| A｜下拉多選 **（現行）** | 類型篩選器下拉、多選、預設全選 | 直接對應「逐項加入」的原始想法 | 觀眾看不到你在點什麼，且有底緣裁切風險 |
| B｜書籤逐步揭露 | 做 4–5 個書籤，每個多顯示幾個類型，用按鈕切換 | 演出最順、節奏可控 | 要多做按鈕，按鈕也需要版面位置 |
| C｜年份滑桿演出 | 不用類型篩選器，改拉年份 2010→2018，看兩根長條塌下去 | **直接演出「年代效應」本身**，操作最少 | 與「逐項加入類型」的原始想法不同 |

排練時若 A 不順，C 是零成本的退路（類型篩選器留著不用即可）。

### 8-2 第 4 頁事件口白的時間分配

`DAX.md` 第 9-1 節標記為未定案。已擱置，待後續處理。

### 8-2b 兩個「孤兒量值」：有定義但版面沒位置

`DAX.md` 定義了、但背景圖的四張 KPI 卡已經排滿、沒有位置的量值：

| 量值 | 原本要做什麼 | 建議 |
|---|---|---|
| `流量貢獻度` | 第 3 頁：點「100 萬↑」顯示 46.1%，講「一成影片撐起近半流量」 | **疊小卡片在主圖右上角**（建議 高 40、寬 220、橫 780、縱 350、**15pt**、`#717171`）。堆疊柱狀圖右上本來就空，不擋資料 |
| `觀看差距` | 第 4 頁：業配 vs 非業配 +27.4% | **不放視覺、只口頭講**。那頁的重點是「這個數字是選擇效應」，做成 KPI 反而在強調一個你要否定的數字 |

`流量貢獻度` 的實測值（2026-08-01）：

```
 10 萬↑   1,957 支（55.9%）→ 佔總觀看 98.4%
 50 萬↑   1,057 支（30.2%）→ 佔總觀看 79.3%
100 萬↑     391 支（11.2%）→ 佔總觀看 46.1%   ← 一成影片撐起近半流量
200 萬↑      81 支（ 2.3%）→ 佔總觀看 16.7%
```

⚠️ 若採用小卡片方案，記得它**也要關掉標題與背景**（第四節通用設定），
否則會在主圖上出現一個突兀的深色方塊。

### 8-4 第 3 頁主圖是否加「工具提示頁」　⏸ 待全部頁面完成後再討論

**需求（2026-08-02 提出）**：滑鼠停在主圖某一年的柱子上時，
跳出該年度的**影片類型佔比**，讓觀眾看得到每年的內容組成細節。

**為什麼值得做**：主圖現在只回答「那一年拍了幾支、長片與 Shorts 各多少」，
回答不了「那一年在拍什麼」。側下的 100% 堆疊區域圖有講組成，但那是整體趨勢，
讀不出單一年度的精確數字。工具提示可以補這一層，**而且不佔任何版面**。

它也直接支援「2015 年 530 支」那段必講的說明——滑到 2015 年就能秀出
「遊戲實況 435 支（82%）」，不必只靠口頭。

**做法（Power BI 工具提示頁）**

1. 新增一頁 → `格式化頁面` → `頁面資訊` → **`允許使用工具提示`** 開啟
2. `畫布設定` → 類型選 **`工具提示`**（320×240），或自訂較大尺寸
3. 頁面上放一張**橫條圖**：Y 軸 `category`、X 軸 `[影片數]`、前 6 項
4. 回到第 3 頁主圖 → `一般` → **`工具提示`** → 類型選 **`報表頁`** → 頁面選剛才那頁
5. 該頁的頁籤要**隱藏**（對頁籤按右鍵 → `隱藏頁面`），否則簡報時會多出一頁

**為什麼延後**：工具提示頁需要自己的畫布尺寸與配色，跟目前七頁的 1920×1080
不同套規格，現在做會打斷施工節奏。**七頁都完成、確認時間預算還夠再回來評估。**

⚠️ 若採用，工具提示頁的背景要另外處理——它不能用 `bg_page{N}.png`（尺寸不符），
需要單獨設一個深色底（`#1F1F1F`）並手動加卡片邊框，或乾脆用純色背景。

### 8-5 第 3 頁側上折線是否加「成長率」工具提示　⏸ 待全部頁面完成後再討論

**需求（2026-08-02 提出）**：滑到折線圖的某一年時，直接顯示**該年影片還在成長多少**，
讓「為什麼 2024 年之後畫虛線」這件事**用看的就懂**，不必靠口頭解釋。

**為什麼值得做**：虛線只說了「不可比」，沒說「不可比到什麼程度」。
被質疑「你憑什麼認為 2024 不能比、2023 可以」時，現在只能口頭回答；
有這個工具提示就能當場滑給對方看：

```
2023   中位觀看 556,488   11 日成長 0.049%   長片數 158   可比
2024   中位觀看 480,385   11 日成長 0.168%   長片數 161   仍在累積
2025   中位觀看 564,837   11 日成長 0.552%   長片數 131   仍在累積
```

**這是把方法論的證據直接放進畫面**，跟第 4 頁「偵測業配數（下限）／推估實際／判定精確率」
那組 KPI 是同一個思路。

#### 🔴 前置條件：要先載入 `daily_stats.csv`

目前模型**沒有**這張表（第 3 步刻意跳過，因為當時沒有視覺物件用得到）。要做這個工具提示：

1. 載入 `data\processed\daily_stats.csv`（快照日 × 影片 × 觀看/讚/留言）
2. 建立關聯：`daily_stats[video_id]` → `videos[video_id]`，**多對一、單向**
3. 新增量值計算首末快照的相對成長中位數
4. 把量值放進折線圖的 `工具提示` 欄位區（不必做成報表頁，一般工具提示就夠）

⚠️ 第 2 步是這頁唯一要建的關聯，**跟 `門檻`／`資料範圍` 那兩張中斷連結表性質不同**，
不要混淆。建錯方向會讓 `videos` 被 `daily_stats` 反向篩選。

⚠️ 快照只有 3 日（2026-07-21、07-29、08-01），每天會累積。量值要寫成
取 `MIN(snapshot_date)` 與 `MAX(snapshot_date)` 動態計算，不要寫死日期。

**為什麼延後**：要載新表、建關聯、寫新量值，是模型層級的改動，
現在做會打斷施工節奏，而且萬一關聯建錯會影響已完工的第 4 頁。
**七頁都完成、確認時間預算還夠再回來評估。**

（完整的成長率實測數據與壓力測試見 `HANDOFF.md` 第七章第 5 點。）

### 8-3 尚未討論的細項

- KPI 卡的數值格式：`[中位觀看]` 要顯示 `599,498` 還是 `59.9 萬`？
  （後者需用 `DAX.md` 第五節的 `中位觀看_萬`，但會變成文字，圖表軸不能用）
- 第 6 頁氣泡圖目前篩到「正片集數 ≥ 10」共 15 個系列，要不要再收斂
